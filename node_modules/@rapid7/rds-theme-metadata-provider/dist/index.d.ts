import React, { ReactNode } from "react";
export declare const THEME_MODES: {
    readonly DARK: "dark";
    readonly LIGHT: "light";
};
export type ThemeMode = (typeof THEME_MODES)[keyof typeof THEME_MODES];
export declare const BRAND_NAMES: {
    readonly CALLISTO: "Callisto";
    readonly ORIGINAL: "Original";
};
export type BrandName = (typeof BRAND_NAMES)[keyof typeof BRAND_NAMES];
interface RdsContextType {
    theme: {
        brand: BrandName;
        mode: ThemeMode;
    };
}
interface RdsProviderProps {
    theme?: {
        brand: BrandName;
        mode: ThemeMode;
    };
    children: ReactNode;
}
export declare const RdsThemeMetadataProvider: React.FC<RdsProviderProps>;
export declare const useRdsThemeMetadataProvider: () => RdsContextType;
export {};
//# sourceMappingURL=index.d.ts.map