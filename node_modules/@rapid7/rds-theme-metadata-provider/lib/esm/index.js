import { jsx as _jsx } from "react/jsx-runtime";
import { createContext, useContext, useMemo } from "react";
export const THEME_MODES = {
    DARK: "dark",
    LIGHT: "light",
};
export const BRAND_NAMES = {
    CALLISTO: "Callisto",
    ORIGINAL: "Original",
};
const RdsContext = createContext({
    theme: {
        brand: "Callisto",
        mode: "light",
    },
});
export const RdsThemeMetadataProvider = ({ theme, children, }) => {
    const { brand = "Callisto", mode = "light" } = theme || {};
    const value = useMemo(() => ({ theme: { brand, mode } }), [brand, mode]);
    return _jsx(RdsContext.Provider, { value: value, children: children });
};
export const useRdsThemeMetadataProvider = () => {
    const context = useContext(RdsContext);
    if (!context) {
        console.error("useRdsThemeMetadataProvider must be used within a RdsThemeMetadataProvider");
    }
    return context;
};
