import { jsx as _jsx } from "react/jsx-runtime";
import AddPlusCallisto from "./Callisto/AddPlus";
import AddPlusOriginal from "./Original/AddPlus";
import { useRdsThemeMetadataProvider } from "@rapid7/rds-theme-metadata-provider";
const AddPlus = ({ brand, ...iconProps }) => {
    const { theme: { brand: themeBrand } } = useRdsThemeMetadataProvider();
    const activeBrand = brand || themeBrand || "Callisto";
    return activeBrand === "Callisto" ? _jsx(AddPlusCallisto, { ...iconProps }) : _jsx(AddPlusOriginal, { ...iconProps });
};
export { AddPlus };
