import { jsx as _jsx } from "react/jsx-runtime";
import StackedAreaChartCallisto from "./Callisto/StackedAreaChart";
import StackedAreaChartOriginal from "./Original/StackedAreaChart";
import { useRdsThemeMetadataProvider } from "@rapid7/rds-theme-metadata-provider";
const StackedAreaChart = ({ brand, ...iconProps }) => {
    const { theme: { brand: themeBrand } } = useRdsThemeMetadataProvider();
    const activeBrand = brand || themeBrand || "Callisto";
    return activeBrand === "Callisto" ? _jsx(StackedAreaChartCallisto, { ...iconProps }) : _jsx(StackedAreaChartOriginal, { ...iconProps });
};
export { StackedAreaChart };
