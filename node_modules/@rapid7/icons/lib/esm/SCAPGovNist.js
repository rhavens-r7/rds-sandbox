import { jsx as _jsx } from "react/jsx-runtime";
import SCAPGovNistCallisto from "./Callisto/SCAPGovNist";
import SCAPGovNistOriginal from "./Original/SCAPGovNist";
import { useRdsThemeMetadataProvider } from "@rapid7/rds-theme-metadata-provider";
const SCAPGovNist = ({ brand, ...iconProps }) => {
    const { theme: { brand: themeBrand } } = useRdsThemeMetadataProvider();
    const activeBrand = brand || themeBrand || "Callisto";
    return activeBrand === "Callisto" ? _jsx(SCAPGovNistCallisto, { ...iconProps }) : _jsx(SCAPGovNistOriginal, { ...iconProps });
};
export { SCAPGovNist };
