import { jsx as _jsx } from "react/jsx-runtime";
import MentionCallisto from "./Callisto/Mention";
import MentionOriginal from "./Original/Mention";
import { useRdsThemeMetadataProvider } from "@rapid7/rds-theme-metadata-provider";
const Mention = ({ brand, ...iconProps }) => {
    const { theme: { brand: themeBrand } } = useRdsThemeMetadataProvider();
    const activeBrand = brand || themeBrand || "Callisto";
    return activeBrand === "Callisto" ? _jsx(MentionCallisto, { ...iconProps }) : _jsx(MentionOriginal, { ...iconProps });
};
export { Mention };
