import { jsx as _jsx } from "react/jsx-runtime";
import ResponseRemediationCallisto from "./Callisto/ResponseRemediation";
import ResponseRemediationOriginal from "./Original/ResponseRemediation";
import { useRdsThemeMetadataProvider } from "@rapid7/rds-theme-metadata-provider";
const ResponseRemediation = ({ brand, ...iconProps }) => {
    const { theme: { brand: themeBrand } } = useRdsThemeMetadataProvider();
    const activeBrand = brand || themeBrand || "Callisto";
    return activeBrand === "Callisto" ? _jsx(ResponseRemediationCallisto, { ...iconProps }) : _jsx(ResponseRemediationOriginal, { ...iconProps });
};
export { ResponseRemediation };
