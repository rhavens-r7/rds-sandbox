import { jsx as _jsx } from "react/jsx-runtime";
import GoalsSLACallisto from "./Callisto/GoalsSLA";
import GoalsSLAOriginal from "./Original/GoalsSLA";
import { useRdsThemeMetadataProvider } from "@rapid7/rds-theme-metadata-provider";
const GoalsSLA = ({ brand, ...iconProps }) => {
    const { theme: { brand: themeBrand } } = useRdsThemeMetadataProvider();
    const activeBrand = brand || themeBrand || "Callisto";
    return activeBrand === "Callisto" ? _jsx(GoalsSLACallisto, { ...iconProps }) : _jsx(GoalsSLAOriginal, { ...iconProps });
};
export { GoalsSLA };
