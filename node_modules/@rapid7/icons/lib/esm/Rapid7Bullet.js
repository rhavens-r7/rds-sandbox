import { jsx as _jsx } from "react/jsx-runtime";
import Rapid7BulletCallisto from "./Callisto/Rapid7Bullet";
import Rapid7BulletOriginal from "./Original/Rapid7Bullet";
import { useRdsThemeMetadataProvider } from "@rapid7/rds-theme-metadata-provider";
const Rapid7Bullet = ({ brand, ...iconProps }) => {
    const { theme: { brand: themeBrand } } = useRdsThemeMetadataProvider();
    const activeBrand = brand || themeBrand || "Callisto";
    return activeBrand === "Callisto" ? _jsx(Rapid7BulletCallisto, { ...iconProps }) : _jsx(Rapid7BulletOriginal, { ...iconProps });
};
export { Rapid7Bullet };
