import { jsx as _jsx } from "react/jsx-runtime";
import AreaChartCallisto from "./Callisto/AreaChart";
import AreaChartOriginal from "./Original/AreaChart";
import { useRdsThemeMetadataProvider } from "@rapid7/rds-theme-metadata-provider";
const AreaChart = ({ brand, ...iconProps }) => {
    const { theme: { brand: themeBrand } } = useRdsThemeMetadataProvider();
    const activeBrand = brand || themeBrand || "Callisto";
    return activeBrand === "Callisto" ? _jsx(AreaChartCallisto, { ...iconProps }) : _jsx(AreaChartOriginal, { ...iconProps });
};
export { AreaChart };
