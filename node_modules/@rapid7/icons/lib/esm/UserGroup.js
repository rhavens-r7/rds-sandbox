import { jsx as _jsx } from "react/jsx-runtime";
import UserGroupCallisto from "./Callisto/UserGroup";
import UserGroupOriginal from "./Original/UserGroup";
import { useRdsThemeMetadataProvider } from "@rapid7/rds-theme-metadata-provider";
const UserGroup = ({ brand, ...iconProps }) => {
    const { theme: { brand: themeBrand } } = useRdsThemeMetadataProvider();
    const activeBrand = brand || themeBrand || "Callisto";
    return activeBrand === "Callisto" ? _jsx(UserGroupCallisto, { ...iconProps }) : _jsx(UserGroupOriginal, { ...iconProps });
};
export { UserGroup };
