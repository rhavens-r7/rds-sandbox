import { jsx as _jsx } from "react/jsx-runtime";
import SortAscendingArrowCallisto from "./Callisto/SortAscendingArrow";
import SortAscendingArrowOriginal from "./Original/SortAscendingArrow";
import { useRdsThemeMetadataProvider } from "@rapid7/rds-theme-metadata-provider";
const SortAscendingArrow = ({ brand, ...iconProps }) => {
    const { theme: { brand: themeBrand } } = useRdsThemeMetadataProvider();
    const activeBrand = brand || themeBrand || "Callisto";
    return activeBrand === "Callisto" ? _jsx(SortAscendingArrowCallisto, { ...iconProps }) : _jsx(SortAscendingArrowOriginal, { ...iconProps });
};
export { SortAscendingArrow };
