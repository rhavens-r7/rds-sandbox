import { jsx as _jsx } from "react/jsx-runtime";
import CopyLinkCallisto from "./Callisto/CopyLink";
import CopyLinkOriginal from "./Original/CopyLink";
import { useRdsThemeMetadataProvider } from "@rapid7/rds-theme-metadata-provider";
const CopyLink = ({ brand, ...iconProps }) => {
    const { theme: { brand: themeBrand } } = useRdsThemeMetadataProvider();
    const activeBrand = brand || themeBrand || "Callisto";
    return activeBrand === "Callisto" ? _jsx(CopyLinkCallisto, { ...iconProps }) : _jsx(CopyLinkOriginal, { ...iconProps });
};
export { CopyLink };
