import { jsx as _jsx } from "react/jsx-runtime";
import QuickActionsCallisto from "./Callisto/QuickActions";
import QuickActionsOriginal from "./Original/QuickActions";
import { useRdsThemeMetadataProvider } from "@rapid7/rds-theme-metadata-provider";
const QuickActions = ({ brand, ...iconProps }) => {
    const { theme: { brand: themeBrand } } = useRdsThemeMetadataProvider();
    const activeBrand = brand || themeBrand || "Callisto";
    return activeBrand === "Callisto" ? _jsx(QuickActionsCallisto, { ...iconProps }) : _jsx(QuickActionsOriginal, { ...iconProps });
};
export { QuickActions };
