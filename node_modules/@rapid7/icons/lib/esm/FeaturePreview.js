import { jsx as _jsx } from "react/jsx-runtime";
import FeaturePreviewCallisto from "./Callisto/FeaturePreview";
import FeaturePreviewOriginal from "./Original/FeaturePreview";
import { useRdsThemeMetadataProvider } from "@rapid7/rds-theme-metadata-provider";
const FeaturePreview = ({ brand, ...iconProps }) => {
    const { theme: { brand: themeBrand } } = useRdsThemeMetadataProvider();
    const activeBrand = brand || themeBrand || "Callisto";
    return activeBrand === "Callisto" ? _jsx(FeaturePreviewCallisto, { ...iconProps }) : _jsx(FeaturePreviewOriginal, { ...iconProps });
};
export { FeaturePreview };
