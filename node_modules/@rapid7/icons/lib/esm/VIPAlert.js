import { jsx as _jsx } from "react/jsx-runtime";
import VIPAlertCallisto from "./Callisto/VIPAlert";
import VIPAlertOriginal from "./Original/VIPAlert";
import { useRdsThemeMetadataProvider } from "@rapid7/rds-theme-metadata-provider";
const VIPAlert = ({ brand, ...iconProps }) => {
    const { theme: { brand: themeBrand } } = useRdsThemeMetadataProvider();
    const activeBrand = brand || themeBrand || "Callisto";
    return activeBrand === "Callisto" ? _jsx(VIPAlertCallisto, { ...iconProps }) : _jsx(VIPAlertOriginal, { ...iconProps });
};
export { VIPAlert };
