import { jsx as _jsx } from "react/jsx-runtime";
import ContainerCallisto from "./Callisto/Container";
import ContainerOriginal from "./Original/Container";
import { useRdsThemeMetadataProvider } from "@rapid7/rds-theme-metadata-provider";
const Container = ({ brand, ...iconProps }) => {
    const { theme: { brand: themeBrand } } = useRdsThemeMetadataProvider();
    const activeBrand = brand || themeBrand || "Callisto";
    return activeBrand === "Callisto" ? _jsx(ContainerCallisto, { ...iconProps }) : _jsx(ContainerOriginal, { ...iconProps });
};
export { Container };
