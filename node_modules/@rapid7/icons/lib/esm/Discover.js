import { jsx as _jsx } from "react/jsx-runtime";
import DiscoverCallisto from "./Callisto/Discover";
import DiscoverOriginal from "./Original/Discover";
import { useRdsThemeMetadataProvider } from "@rapid7/rds-theme-metadata-provider";
const Discover = ({ brand, ...iconProps }) => {
    const { theme: { brand: themeBrand } } = useRdsThemeMetadataProvider();
    const activeBrand = brand || themeBrand || "Callisto";
    return activeBrand === "Callisto" ? _jsx(DiscoverCallisto, { ...iconProps }) : _jsx(DiscoverOriginal, { ...iconProps });
};
export { Discover };
