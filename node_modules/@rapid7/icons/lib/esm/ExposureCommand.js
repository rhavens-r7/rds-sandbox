import { jsx as _jsx } from "react/jsx-runtime";
import ExposureCommandCallisto from "./Callisto/ExposureCommand";
import ExposureCommandOriginal from "./Original/ExposureCommand";
import { useRdsThemeMetadataProvider } from "@rapid7/rds-theme-metadata-provider";
const ExposureCommand = ({ brand, ...iconProps }) => {
    const { theme: { brand: themeBrand } } = useRdsThemeMetadataProvider();
    const activeBrand = brand || themeBrand || "Callisto";
    return activeBrand === "Callisto" ? _jsx(ExposureCommandCallisto, { ...iconProps }) : _jsx(ExposureCommandOriginal, { ...iconProps });
};
export { ExposureCommand };
