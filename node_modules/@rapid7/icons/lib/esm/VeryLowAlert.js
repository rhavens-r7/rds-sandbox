import { jsx as _jsx } from "react/jsx-runtime";
import VeryLowAlertCallisto from "./Callisto/VeryLowAlert";
import VeryLowAlertOriginal from "./Original/VeryLowAlert";
import { useRdsThemeMetadataProvider } from "@rapid7/rds-theme-metadata-provider";
const VeryLowAlert = ({ brand, ...iconProps }) => {
    const { theme: { brand: themeBrand } } = useRdsThemeMetadataProvider();
    const activeBrand = brand || themeBrand || "Callisto";
    return activeBrand === "Callisto" ? _jsx(VeryLowAlertCallisto, { ...iconProps }) : _jsx(VeryLowAlertOriginal, { ...iconProps });
};
export { VeryLowAlert };
