import { jsx as _jsx } from "react/jsx-runtime";
import AIGradientCallisto from "./Callisto/AIGradient";
import AIGradientOriginal from "./Original/AIGradient";
import { useRdsThemeMetadataProvider } from "@rapid7/rds-theme-metadata-provider";
const AIGradient = ({ brand, ...iconProps }) => {
    const { theme: { brand: themeBrand } } = useRdsThemeMetadataProvider();
    const activeBrand = brand || themeBrand || "Callisto";
    return activeBrand === "Callisto" ? _jsx(AIGradientCallisto, { ...iconProps }) : _jsx(AIGradientOriginal, { ...iconProps });
};
export { AIGradient };
