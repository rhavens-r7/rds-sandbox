import { jsx as _jsx } from "react/jsx-runtime";
import ExternalAssetCallisto from "./Callisto/ExternalAsset";
import ExternalAssetOriginal from "./Original/ExternalAsset";
import { useRdsThemeMetadataProvider } from "@rapid7/rds-theme-metadata-provider";
const ExternalAsset = ({ brand, ...iconProps }) => {
    const { theme: { brand: themeBrand } } = useRdsThemeMetadataProvider();
    const activeBrand = brand || themeBrand || "Callisto";
    return activeBrand === "Callisto" ? _jsx(ExternalAssetCallisto, { ...iconProps }) : _jsx(ExternalAssetOriginal, { ...iconProps });
};
export { ExternalAsset };
