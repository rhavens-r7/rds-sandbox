import { jsx as _jsx } from "react/jsx-runtime";
import TableGridCallisto from "./Callisto/TableGrid";
import TableGridOriginal from "./Original/TableGrid";
import { useRdsThemeMetadataProvider } from "@rapid7/rds-theme-metadata-provider";
const TableGrid = ({ brand, ...iconProps }) => {
    const { theme: { brand: themeBrand } } = useRdsThemeMetadataProvider();
    const activeBrand = brand || themeBrand || "Callisto";
    return activeBrand === "Callisto" ? _jsx(TableGridCallisto, { ...iconProps }) : _jsx(TableGridOriginal, { ...iconProps });
};
export { TableGrid };
