import { jsx as _jsx } from "react/jsx-runtime";
import LegacyLogSearchCallisto from "./Callisto/LegacyLogSearch";
import LegacyLogSearchOriginal from "./Original/LegacyLogSearch";
import { useRdsThemeMetadataProvider } from "@rapid7/rds-theme-metadata-provider";
const LegacyLogSearch = ({ brand, ...iconProps }) => {
    const { theme: { brand: themeBrand } } = useRdsThemeMetadataProvider();
    const activeBrand = brand || themeBrand || "Callisto";
    return activeBrand === "Callisto" ? _jsx(LegacyLogSearchCallisto, { ...iconProps }) : _jsx(LegacyLogSearchOriginal, { ...iconProps });
};
export { LegacyLogSearch };
