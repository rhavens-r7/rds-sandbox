import { jsx as _jsx } from "react/jsx-runtime";
import SupportTicketCallisto from "./Callisto/SupportTicket";
import SupportTicketOriginal from "./Original/SupportTicket";
import { useRdsThemeMetadataProvider } from "@rapid7/rds-theme-metadata-provider";
const SupportTicket = ({ brand, ...iconProps }) => {
    const { theme: { brand: themeBrand } } = useRdsThemeMetadataProvider();
    const activeBrand = brand || themeBrand || "Callisto";
    return activeBrand === "Callisto" ? _jsx(SupportTicketCallisto, { ...iconProps }) : _jsx(SupportTicketOriginal, { ...iconProps });
};
export { SupportTicket };
