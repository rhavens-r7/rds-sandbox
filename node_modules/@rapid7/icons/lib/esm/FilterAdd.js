import { jsx as _jsx } from "react/jsx-runtime";
import FilterAddCallisto from "./Callisto/FilterAdd";
import FilterAddOriginal from "./Original/FilterAdd";
import { useRdsThemeMetadataProvider } from "@rapid7/rds-theme-metadata-provider";
const FilterAdd = ({ brand, ...iconProps }) => {
    const { theme: { brand: themeBrand } } = useRdsThemeMetadataProvider();
    const activeBrand = brand || themeBrand || "Callisto";
    return activeBrand === "Callisto" ? _jsx(FilterAddCallisto, { ...iconProps }) : _jsx(FilterAddOriginal, { ...iconProps });
};
export { FilterAdd };
