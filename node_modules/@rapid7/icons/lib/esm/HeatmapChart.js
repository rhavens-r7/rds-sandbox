import { jsx as _jsx } from "react/jsx-runtime";
import HeatmapChartCallisto from "./Callisto/HeatmapChart";
import HeatmapChartOriginal from "./Original/HeatmapChart";
import { useRdsThemeMetadataProvider } from "@rapid7/rds-theme-metadata-provider";
const HeatmapChart = ({ brand, ...iconProps }) => {
    const { theme: { brand: themeBrand } } = useRdsThemeMetadataProvider();
    const activeBrand = brand || themeBrand || "Callisto";
    return activeBrand === "Callisto" ? _jsx(HeatmapChartCallisto, { ...iconProps }) : _jsx(HeatmapChartOriginal, { ...iconProps });
};
export { HeatmapChart };
