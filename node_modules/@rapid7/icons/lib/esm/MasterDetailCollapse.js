import { jsx as _jsx } from "react/jsx-runtime";
import MasterDetailCollapseCallisto from "./Callisto/MasterDetailCollapse";
import MasterDetailCollapseOriginal from "./Original/MasterDetailCollapse";
import { useRdsThemeMetadataProvider } from "@rapid7/rds-theme-metadata-provider";
const MasterDetailCollapse = ({ brand, ...iconProps }) => {
    const { theme: { brand: themeBrand } } = useRdsThemeMetadataProvider();
    const activeBrand = brand || themeBrand || "Callisto";
    return activeBrand === "Callisto" ? _jsx(MasterDetailCollapseCallisto, { ...iconProps }) : _jsx(MasterDetailCollapseOriginal, { ...iconProps });
};
export { MasterDetailCollapse };
