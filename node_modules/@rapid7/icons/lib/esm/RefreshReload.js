import { jsx as _jsx } from "react/jsx-runtime";
import RefreshReloadCallisto from "./Callisto/RefreshReload";
import RefreshReloadOriginal from "./Original/RefreshReload";
import { useRdsThemeMetadataProvider } from "@rapid7/rds-theme-metadata-provider";
const RefreshReload = ({ brand, ...iconProps }) => {
    const { theme: { brand: themeBrand } } = useRdsThemeMetadataProvider();
    const activeBrand = brand || themeBrand || "Callisto";
    return activeBrand === "Callisto" ? _jsx(RefreshReloadCallisto, { ...iconProps }) : _jsx(RefreshReloadOriginal, { ...iconProps });
};
export { RefreshReload };
