import { jsx as _jsx } from "react/jsx-runtime";
import RadioEmptyCallisto from "./Callisto/RadioEmpty";
import RadioEmptyOriginal from "./Original/RadioEmpty";
import { useRdsThemeMetadataProvider } from "@rapid7/rds-theme-metadata-provider";
const RadioEmpty = ({ brand, ...iconProps }) => {
    const { theme: { brand: themeBrand } } = useRdsThemeMetadataProvider();
    const activeBrand = brand || themeBrand || "Callisto";
    return activeBrand === "Callisto" ? _jsx(RadioEmptyCallisto, { ...iconProps }) : _jsx(RadioEmptyOriginal, { ...iconProps });
};
export { RadioEmpty };
