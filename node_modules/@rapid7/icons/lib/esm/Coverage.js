import { jsx as _jsx } from "react/jsx-runtime";
import CoverageCallisto from "./Callisto/Coverage";
import CoverageOriginal from "./Original/Coverage";
import { useRdsThemeMetadataProvider } from "@rapid7/rds-theme-metadata-provider";
const Coverage = ({ brand, ...iconProps }) => {
    const { theme: { brand: themeBrand } } = useRdsThemeMetadataProvider();
    const activeBrand = brand || themeBrand || "Callisto";
    return activeBrand === "Callisto" ? _jsx(CoverageCallisto, { ...iconProps }) : _jsx(CoverageOriginal, { ...iconProps });
};
export { Coverage };
