import { jsx as _jsx } from "react/jsx-runtime";
import CheckboxEmptyCallisto from "./Callisto/CheckboxEmpty";
import CheckboxEmptyOriginal from "./Original/CheckboxEmpty";
import { useRdsThemeMetadataProvider } from "@rapid7/rds-theme-metadata-provider";
const CheckboxEmpty = ({ brand, ...iconProps }) => {
    const { theme: { brand: themeBrand } } = useRdsThemeMetadataProvider();
    const activeBrand = brand || themeBrand || "Callisto";
    return activeBrand === "Callisto" ? _jsx(CheckboxEmptyCallisto, { ...iconProps }) : _jsx(CheckboxEmptyOriginal, { ...iconProps });
};
export { CheckboxEmpty };
