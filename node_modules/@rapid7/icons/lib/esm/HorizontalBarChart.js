import { jsx as _jsx } from "react/jsx-runtime";
import HorizontalBarChartCallisto from "./Callisto/HorizontalBarChart";
import HorizontalBarChartOriginal from "./Original/HorizontalBarChart";
import { useRdsThemeMetadataProvider } from "@rapid7/rds-theme-metadata-provider";
const HorizontalBarChart = ({ brand, ...iconProps }) => {
    const { theme: { brand: themeBrand } } = useRdsThemeMetadataProvider();
    const activeBrand = brand || themeBrand || "Callisto";
    return activeBrand === "Callisto" ? _jsx(HorizontalBarChartCallisto, { ...iconProps }) : _jsx(HorizontalBarChartOriginal, { ...iconProps });
};
export { HorizontalBarChart };
