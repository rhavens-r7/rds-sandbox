import { jsx as _jsx } from "react/jsx-runtime";
import LowAlertCallisto from "./Callisto/LowAlert";
import LowAlertOriginal from "./Original/LowAlert";
import { useRdsThemeMetadataProvider } from "@rapid7/rds-theme-metadata-provider";
const LowAlert = ({ brand, ...iconProps }) => {
    const { theme: { brand: themeBrand } } = useRdsThemeMetadataProvider();
    const activeBrand = brand || themeBrand || "Callisto";
    return activeBrand === "Callisto" ? _jsx(LowAlertCallisto, { ...iconProps }) : _jsx(LowAlertOriginal, { ...iconProps });
};
export { LowAlert };
