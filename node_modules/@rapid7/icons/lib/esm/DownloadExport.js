import { jsx as _jsx } from "react/jsx-runtime";
import DownloadExportCallisto from "./Callisto/DownloadExport";
import DownloadExportOriginal from "./Original/DownloadExport";
import { useRdsThemeMetadataProvider } from "@rapid7/rds-theme-metadata-provider";
const DownloadExport = ({ brand, ...iconProps }) => {
    const { theme: { brand: themeBrand } } = useRdsThemeMetadataProvider();
    const activeBrand = brand || themeBrand || "Callisto";
    return activeBrand === "Callisto" ? _jsx(DownloadExportCallisto, { ...iconProps }) : _jsx(DownloadExportOriginal, { ...iconProps });
};
export { DownloadExport };
