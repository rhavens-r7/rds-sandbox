import { jsx as _jsx } from "react/jsx-runtime";
import WebhookCallisto from "./Callisto/Webhook";
import WebhookOriginal from "./Original/Webhook";
import { useRdsThemeMetadataProvider } from "@rapid7/rds-theme-metadata-provider";
const Webhook = ({ brand, ...iconProps }) => {
    const { theme: { brand: themeBrand } } = useRdsThemeMetadataProvider();
    const activeBrand = brand || themeBrand || "Callisto";
    return activeBrand === "Callisto" ? _jsx(WebhookCallisto, { ...iconProps }) : _jsx(WebhookOriginal, { ...iconProps });
};
export { Webhook };
