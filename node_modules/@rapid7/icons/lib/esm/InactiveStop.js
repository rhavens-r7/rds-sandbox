import { jsx as _jsx } from "react/jsx-runtime";
import InactiveStopCallisto from "./Callisto/InactiveStop";
import InactiveStopOriginal from "./Original/InactiveStop";
import { useRdsThemeMetadataProvider } from "@rapid7/rds-theme-metadata-provider";
const InactiveStop = ({ brand, ...iconProps }) => {
    const { theme: { brand: themeBrand } } = useRdsThemeMetadataProvider();
    const activeBrand = brand || themeBrand || "Callisto";
    return activeBrand === "Callisto" ? _jsx(InactiveStopCallisto, { ...iconProps }) : _jsx(InactiveStopOriginal, { ...iconProps });
};
export { InactiveStop };
