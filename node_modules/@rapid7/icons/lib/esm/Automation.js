import { jsx as _jsx } from "react/jsx-runtime";
import AutomationCallisto from "./Callisto/Automation";
import AutomationOriginal from "./Original/Automation";
import { useRdsThemeMetadataProvider } from "@rapid7/rds-theme-metadata-provider";
const Automation = ({ brand, ...iconProps }) => {
    const { theme: { brand: themeBrand } } = useRdsThemeMetadataProvider();
    const activeBrand = brand || themeBrand || "Callisto";
    return activeBrand === "Callisto" ? _jsx(AutomationCallisto, { ...iconProps }) : _jsx(AutomationOriginal, { ...iconProps });
};
export { Automation };
