import { jsx as _jsx } from "react/jsx-runtime";
import MinusRemoveCallisto from "./Callisto/MinusRemove";
import MinusRemoveOriginal from "./Original/MinusRemove";
import { useRdsThemeMetadataProvider } from "@rapid7/rds-theme-metadata-provider";
const MinusRemove = ({ brand, ...iconProps }) => {
    const { theme: { brand: themeBrand } } = useRdsThemeMetadataProvider();
    const activeBrand = brand || themeBrand || "Callisto";
    return activeBrand === "Callisto" ? _jsx(MinusRemoveCallisto, { ...iconProps }) : _jsx(MinusRemoveOriginal, { ...iconProps });
};
export { MinusRemove };
