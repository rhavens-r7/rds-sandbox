import { jsx as _jsx } from "react/jsx-runtime";
import LeftNavOpenCallisto from "./Callisto/LeftNavOpen";
import LeftNavOpenOriginal from "./Original/LeftNavOpen";
import { useRdsThemeMetadataProvider } from "@rapid7/rds-theme-metadata-provider";
const LeftNavOpen = ({ brand, ...iconProps }) => {
    const { theme: { brand: themeBrand } } = useRdsThemeMetadataProvider();
    const activeBrand = brand || themeBrand || "Callisto";
    return activeBrand === "Callisto" ? _jsx(LeftNavOpenCallisto, { ...iconProps }) : _jsx(LeftNavOpenOriginal, { ...iconProps });
};
export { LeftNavOpen };
