import { jsx as _jsx } from "react/jsx-runtime";
import AttackSurfaceCallisto from "./Callisto/AttackSurface";
import AttackSurfaceOriginal from "./Original/AttackSurface";
import { useRdsThemeMetadataProvider } from "@rapid7/rds-theme-metadata-provider";
const AttackSurface = ({ brand, ...iconProps }) => {
    const { theme: { brand: themeBrand } } = useRdsThemeMetadataProvider();
    const activeBrand = brand || themeBrand || "Callisto";
    return activeBrand === "Callisto" ? _jsx(AttackSurfaceCallisto, { ...iconProps }) : _jsx(AttackSurfaceOriginal, { ...iconProps });
};
export { AttackSurface };
