import { jsx as _jsx } from "react/jsx-runtime";
import LineChartCallisto from "./Callisto/LineChart";
import LineChartOriginal from "./Original/LineChart";
import { useRdsThemeMetadataProvider } from "@rapid7/rds-theme-metadata-provider";
const LineChart = ({ brand, ...iconProps }) => {
    const { theme: { brand: themeBrand } } = useRdsThemeMetadataProvider();
    const activeBrand = brand || themeBrand || "Callisto";
    return activeBrand === "Callisto" ? _jsx(LineChartCallisto, { ...iconProps }) : _jsx(LineChartOriginal, { ...iconProps });
};
export { LineChart };
