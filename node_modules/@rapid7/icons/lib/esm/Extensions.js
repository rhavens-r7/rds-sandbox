import { jsx as _jsx } from "react/jsx-runtime";
import ExtensionsCallisto from "./Callisto/Extensions";
import ExtensionsOriginal from "./Original/Extensions";
import { useRdsThemeMetadataProvider } from "@rapid7/rds-theme-metadata-provider";
const Extensions = ({ brand, ...iconProps }) => {
    const { theme: { brand: themeBrand } } = useRdsThemeMetadataProvider();
    const activeBrand = brand || themeBrand || "Callisto";
    return activeBrand === "Callisto" ? _jsx(ExtensionsCallisto, { ...iconProps }) : _jsx(ExtensionsOriginal, { ...iconProps });
};
export { Extensions };
