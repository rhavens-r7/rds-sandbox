import { jsx as _jsx } from "react/jsx-runtime";
import SIEMSubtleCallisto from "../Callisto/SIEMSubtle";
import SIEMSubtleOriginal from "../Original/SIEMSubtle";
import { useRdsThemeMetadataProvider } from "@rapid7/rds-theme-metadata-provider";
const SIEMSubtle = ({ brand, ...iconProps }) => {
    const { theme: { brand: themeBrand } } = useRdsThemeMetadataProvider();
    const activeBrand = brand || themeBrand || "Callisto";
    return activeBrand === "Callisto" ? _jsx(SIEMSubtleCallisto, { ...iconProps }) : _jsx(SIEMSubtleOriginal, { ...iconProps });
};
export { SIEMSubtle };
