import { jsx as _jsx } from "react/jsx-runtime";
import ExposureCommandSubtleCallisto from "../Callisto/ExposureCommandSubtle";
import ExposureCommandSubtleOriginal from "../Original/ExposureCommandSubtle";
import { useRdsThemeMetadataProvider } from "@rapid7/rds-theme-metadata-provider";
const ExposureCommandSubtle = ({ brand, ...iconProps }) => {
    const { theme: { brand: themeBrand } } = useRdsThemeMetadataProvider();
    const activeBrand = brand || themeBrand || "Callisto";
    return activeBrand === "Callisto" ? _jsx(ExposureCommandSubtleCallisto, { ...iconProps }) : _jsx(ExposureCommandSubtleOriginal, { ...iconProps });
};
export { ExposureCommandSubtle };
