import { jsx as _jsx } from "react/jsx-runtime";
import AutomationSubtleCallisto from "../Callisto/AutomationSubtle";
import AutomationSubtleOriginal from "../Original/AutomationSubtle";
import { useRdsThemeMetadataProvider } from "@rapid7/rds-theme-metadata-provider";
const AutomationSubtle = ({ brand, ...iconProps }) => {
    const { theme: { brand: themeBrand } } = useRdsThemeMetadataProvider();
    const activeBrand = brand || themeBrand || "Callisto";
    return activeBrand === "Callisto" ? _jsx(AutomationSubtleCallisto, { ...iconProps }) : _jsx(AutomationSubtleOriginal, { ...iconProps });
};
export { AutomationSubtle };
