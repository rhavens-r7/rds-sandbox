import { jsx as _jsx } from "react/jsx-runtime";
import ManagedServicesSubtleCallisto from "../Callisto/ManagedServicesSubtle";
import ManagedServicesSubtleOriginal from "../Original/ManagedServicesSubtle";
import { useRdsThemeMetadataProvider } from "@rapid7/rds-theme-metadata-provider";
const ManagedServicesSubtle = ({ brand, ...iconProps }) => {
    const { theme: { brand: themeBrand } } = useRdsThemeMetadataProvider();
    const activeBrand = brand || themeBrand || "Callisto";
    return activeBrand === "Callisto" ? _jsx(ManagedServicesSubtleCallisto, { ...iconProps }) : _jsx(ManagedServicesSubtleOriginal, { ...iconProps });
};
export { ManagedServicesSubtle };
