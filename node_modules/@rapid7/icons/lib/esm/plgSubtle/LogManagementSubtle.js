import { jsx as _jsx } from "react/jsx-runtime";
import LogManagementSubtleCallisto from "../Callisto/LogManagementSubtle";
import LogManagementSubtleOriginal from "../Original/LogManagementSubtle";
import { useRdsThemeMetadataProvider } from "@rapid7/rds-theme-metadata-provider";
const LogManagementSubtle = ({ brand, ...iconProps }) => {
    const { theme: { brand: themeBrand } } = useRdsThemeMetadataProvider();
    const activeBrand = brand || themeBrand || "Callisto";
    return activeBrand === "Callisto" ? _jsx(LogManagementSubtleCallisto, { ...iconProps }) : _jsx(LogManagementSubtleOriginal, { ...iconProps });
};
export { LogManagementSubtle };
