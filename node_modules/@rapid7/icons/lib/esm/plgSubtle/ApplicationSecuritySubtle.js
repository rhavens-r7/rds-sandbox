import { jsx as _jsx } from "react/jsx-runtime";
import ApplicationSecuritySubtleCallisto from "../Callisto/ApplicationSecuritySubtle";
import ApplicationSecuritySubtleOriginal from "../Original/ApplicationSecuritySubtle";
import { useRdsThemeMetadataProvider } from "@rapid7/rds-theme-metadata-provider";
const ApplicationSecuritySubtle = ({ brand, ...iconProps }) => {
    const { theme: { brand: themeBrand } } = useRdsThemeMetadataProvider();
    const activeBrand = brand || themeBrand || "Callisto";
    return activeBrand === "Callisto" ? _jsx(ApplicationSecuritySubtleCallisto, { ...iconProps }) : _jsx(ApplicationSecuritySubtleOriginal, { ...iconProps });
};
export { ApplicationSecuritySubtle };
