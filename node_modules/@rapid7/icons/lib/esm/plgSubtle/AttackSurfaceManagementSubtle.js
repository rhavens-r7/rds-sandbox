import { jsx as _jsx } from "react/jsx-runtime";
import AttackSurfaceManagementSubtleCallisto from "../Callisto/AttackSurfaceManagementSubtle";
import AttackSurfaceManagementSubtleOriginal from "../Original/AttackSurfaceManagementSubtle";
import { useRdsThemeMetadataProvider } from "@rapid7/rds-theme-metadata-provider";
const AttackSurfaceManagementSubtle = ({ brand, ...iconProps }) => {
    const { theme: { brand: themeBrand } } = useRdsThemeMetadataProvider();
    const activeBrand = brand || themeBrand || "Callisto";
    return activeBrand === "Callisto" ? _jsx(AttackSurfaceManagementSubtleCallisto, { ...iconProps }) : _jsx(AttackSurfaceManagementSubtleOriginal, { ...iconProps });
};
export { AttackSurfaceManagementSubtle };
