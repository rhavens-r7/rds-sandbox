import { jsx as _jsx } from "react/jsx-runtime";
import CloudSecuritySubtleCallisto from "../Callisto/CloudSecuritySubtle";
import CloudSecuritySubtleOriginal from "../Original/CloudSecuritySubtle";
import { useRdsThemeMetadataProvider } from "@rapid7/rds-theme-metadata-provider";
const CloudSecuritySubtle = ({ brand, ...iconProps }) => {
    const { theme: { brand: themeBrand } } = useRdsThemeMetadataProvider();
    const activeBrand = brand || themeBrand || "Callisto";
    return activeBrand === "Callisto" ? _jsx(CloudSecuritySubtleCallisto, { ...iconProps }) : _jsx(CloudSecuritySubtleOriginal, { ...iconProps });
};
export { CloudSecuritySubtle };
