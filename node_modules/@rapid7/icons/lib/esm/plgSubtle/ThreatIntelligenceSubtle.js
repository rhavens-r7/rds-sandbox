import { jsx as _jsx } from "react/jsx-runtime";
import ThreatIntelligenceSubtleCallisto from "../Callisto/ThreatIntelligenceSubtle";
import ThreatIntelligenceSubtleOriginal from "../Original/ThreatIntelligenceSubtle";
import { useRdsThemeMetadataProvider } from "@rapid7/rds-theme-metadata-provider";
const ThreatIntelligenceSubtle = ({ brand, ...iconProps }) => {
    const { theme: { brand: themeBrand } } = useRdsThemeMetadataProvider();
    const activeBrand = brand || themeBrand || "Callisto";
    return activeBrand === "Callisto" ? _jsx(ThreatIntelligenceSubtleCallisto, { ...iconProps }) : _jsx(ThreatIntelligenceSubtleOriginal, { ...iconProps });
};
export { ThreatIntelligenceSubtle };
