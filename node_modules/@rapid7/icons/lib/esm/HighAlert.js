import { jsx as _jsx } from "react/jsx-runtime";
import HighAlertCallisto from "./Callisto/HighAlert";
import HighAlertOriginal from "./Original/HighAlert";
import { useRdsThemeMetadataProvider } from "@rapid7/rds-theme-metadata-provider";
const HighAlert = ({ brand, ...iconProps }) => {
    const { theme: { brand: themeBrand } } = useRdsThemeMetadataProvider();
    const activeBrand = brand || themeBrand || "Callisto";
    return activeBrand === "Callisto" ? _jsx(HighAlertCallisto, { ...iconProps }) : _jsx(HighAlertOriginal, { ...iconProps });
};
export { HighAlert };
