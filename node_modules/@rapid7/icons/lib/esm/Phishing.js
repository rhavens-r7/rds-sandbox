import { jsx as _jsx } from "react/jsx-runtime";
import PhishingCallisto from "./Callisto/Phishing";
import PhishingOriginal from "./Original/Phishing";
import { useRdsThemeMetadataProvider } from "@rapid7/rds-theme-metadata-provider";
const Phishing = ({ brand, ...iconProps }) => {
    const { theme: { brand: themeBrand } } = useRdsThemeMetadataProvider();
    const activeBrand = brand || themeBrand || "Callisto";
    return activeBrand === "Callisto" ? _jsx(PhishingCallisto, { ...iconProps }) : _jsx(PhishingOriginal, { ...iconProps });
};
export { Phishing };
