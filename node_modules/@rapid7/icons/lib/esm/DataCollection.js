import { jsx as _jsx } from "react/jsx-runtime";
import DataCollectionCallisto from "./Callisto/DataCollection";
import DataCollectionOriginal from "./Original/DataCollection";
import { useRdsThemeMetadataProvider } from "@rapid7/rds-theme-metadata-provider";
const DataCollection = ({ brand, ...iconProps }) => {
    const { theme: { brand: themeBrand } } = useRdsThemeMetadataProvider();
    const activeBrand = brand || themeBrand || "Callisto";
    return activeBrand === "Callisto" ? _jsx(DataCollectionCallisto, { ...iconProps }) : _jsx(DataCollectionOriginal, { ...iconProps });
};
export { DataCollection };
