import { jsx as _jsx } from "react/jsx-runtime";
import DiscussChatCallisto from "./Callisto/DiscussChat";
import DiscussChatOriginal from "./Original/DiscussChat";
import { useRdsThemeMetadataProvider } from "@rapid7/rds-theme-metadata-provider";
const DiscussChat = ({ brand, ...iconProps }) => {
    const { theme: { brand: themeBrand } } = useRdsThemeMetadataProvider();
    const activeBrand = brand || themeBrand || "Callisto";
    return activeBrand === "Callisto" ? _jsx(DiscussChatCallisto, { ...iconProps }) : _jsx(DiscussChatOriginal, { ...iconProps });
};
export { DiscussChat };
