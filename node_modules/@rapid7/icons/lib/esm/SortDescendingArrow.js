import { jsx as _jsx } from "react/jsx-runtime";
import SortDescendingArrowCallisto from "./Callisto/SortDescendingArrow";
import SortDescendingArrowOriginal from "./Original/SortDescendingArrow";
import { useRdsThemeMetadataProvider } from "@rapid7/rds-theme-metadata-provider";
const SortDescendingArrow = ({ brand, ...iconProps }) => {
    const { theme: { brand: themeBrand } } = useRdsThemeMetadataProvider();
    const activeBrand = brand || themeBrand || "Callisto";
    return activeBrand === "Callisto" ? _jsx(SortDescendingArrowCallisto, { ...iconProps }) : _jsx(SortDescendingArrowOriginal, { ...iconProps });
};
export { SortDescendingArrow };
