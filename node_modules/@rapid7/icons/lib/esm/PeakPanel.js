import { jsx as _jsx } from "react/jsx-runtime";
import PeakPanelCallisto from "./Callisto/PeakPanel";
import PeakPanelOriginal from "./Original/PeakPanel";
import { useRdsThemeMetadataProvider } from "@rapid7/rds-theme-metadata-provider";
const PeakPanel = ({ brand, ...iconProps }) => {
    const { theme: { brand: themeBrand } } = useRdsThemeMetadataProvider();
    const activeBrand = brand || themeBrand || "Callisto";
    return activeBrand === "Callisto" ? _jsx(PeakPanelCallisto, { ...iconProps }) : _jsx(PeakPanelOriginal, { ...iconProps });
};
export { PeakPanel };
