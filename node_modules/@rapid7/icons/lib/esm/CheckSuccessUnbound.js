import { jsx as _jsx } from "react/jsx-runtime";
import CheckSuccessUnboundCallisto from "./Callisto/CheckSuccessUnbound";
import CheckSuccessUnboundOriginal from "./Original/CheckSuccessUnbound";
import { useRdsThemeMetadataProvider } from "@rapid7/rds-theme-metadata-provider";
const CheckSuccessUnbound = ({ brand, ...iconProps }) => {
    const { theme: { brand: themeBrand } } = useRdsThemeMetadataProvider();
    const activeBrand = brand || themeBrand || "Callisto";
    return activeBrand === "Callisto" ? _jsx(CheckSuccessUnboundCallisto, { ...iconProps }) : _jsx(CheckSuccessUnboundOriginal, { ...iconProps });
};
export { CheckSuccessUnbound };
