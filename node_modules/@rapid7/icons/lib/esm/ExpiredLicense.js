import { jsx as _jsx } from "react/jsx-runtime";
import ExpiredLicenseCallisto from "./Callisto/ExpiredLicense";
import ExpiredLicenseOriginal from "./Original/ExpiredLicense";
import { useRdsThemeMetadataProvider } from "@rapid7/rds-theme-metadata-provider";
const ExpiredLicense = ({ brand, ...iconProps }) => {
    const { theme: { brand: themeBrand } } = useRdsThemeMetadataProvider();
    const activeBrand = brand || themeBrand || "Callisto";
    return activeBrand === "Callisto" ? _jsx(ExpiredLicenseCallisto, { ...iconProps }) : _jsx(ExpiredLicenseOriginal, { ...iconProps });
};
export { ExpiredLicense };
