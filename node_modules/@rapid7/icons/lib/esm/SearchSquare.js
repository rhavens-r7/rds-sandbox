import { jsx as _jsx } from "react/jsx-runtime";
import SearchSquareCallisto from "./Callisto/SearchSquare";
import SearchSquareOriginal from "./Original/SearchSquare";
import { useRdsThemeMetadataProvider } from "@rapid7/rds-theme-metadata-provider";
const SearchSquare = ({ brand, ...iconProps }) => {
    const { theme: { brand: themeBrand } } = useRdsThemeMetadataProvider();
    const activeBrand = brand || themeBrand || "Callisto";
    return activeBrand === "Callisto" ? _jsx(SearchSquareCallisto, { ...iconProps }) : _jsx(SearchSquareOriginal, { ...iconProps });
};
export { SearchSquare };
