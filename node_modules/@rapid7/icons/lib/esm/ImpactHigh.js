import { jsx as _jsx } from "react/jsx-runtime";
import ImpactHighCallisto from "./Callisto/ImpactHigh";
import ImpactHighOriginal from "./Original/ImpactHigh";
import { useRdsThemeMetadataProvider } from "@rapid7/rds-theme-metadata-provider";
const ImpactHigh = ({ brand, ...iconProps }) => {
    const { theme: { brand: themeBrand } } = useRdsThemeMetadataProvider();
    const activeBrand = brand || themeBrand || "Callisto";
    return activeBrand === "Callisto" ? _jsx(ImpactHighCallisto, { ...iconProps }) : _jsx(ImpactHighOriginal, { ...iconProps });
};
export { ImpactHigh };
