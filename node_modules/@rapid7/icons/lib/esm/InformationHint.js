import { jsx as _jsx } from "react/jsx-runtime";
import InformationHintCallisto from "./Callisto/InformationHint";
import InformationHintOriginal from "./Original/InformationHint";
import { useRdsThemeMetadataProvider } from "@rapid7/rds-theme-metadata-provider";
const InformationHint = ({ brand, ...iconProps }) => {
    const { theme: { brand: themeBrand } } = useRdsThemeMetadataProvider();
    const activeBrand = brand || themeBrand || "Callisto";
    return activeBrand === "Callisto" ? _jsx(InformationHintCallisto, { ...iconProps }) : _jsx(InformationHintOriginal, { ...iconProps });
};
export { InformationHint };
