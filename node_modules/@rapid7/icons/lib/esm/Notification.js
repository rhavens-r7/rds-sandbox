import { jsx as _jsx } from "react/jsx-runtime";
import NotificationCallisto from "./Callisto/Notification";
import NotificationOriginal from "./Original/Notification";
import { useRdsThemeMetadataProvider } from "@rapid7/rds-theme-metadata-provider";
const Notification = ({ brand, ...iconProps }) => {
    const { theme: { brand: themeBrand } } = useRdsThemeMetadataProvider();
    const activeBrand = brand || themeBrand || "Callisto";
    return activeBrand === "Callisto" ? _jsx(NotificationCallisto, { ...iconProps }) : _jsx(NotificationOriginal, { ...iconProps });
};
export { Notification };
