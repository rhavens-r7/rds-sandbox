import { jsx as _jsx } from "react/jsx-runtime";
import TrendDownArrowCallisto from "./Callisto/TrendDownArrow";
import TrendDownArrowOriginal from "./Original/TrendDownArrow";
import { useRdsThemeMetadataProvider } from "@rapid7/rds-theme-metadata-provider";
const TrendDownArrow = ({ brand, ...iconProps }) => {
    const { theme: { brand: themeBrand } } = useRdsThemeMetadataProvider();
    const activeBrand = brand || themeBrand || "Callisto";
    return activeBrand === "Callisto" ? _jsx(TrendDownArrowCallisto, { ...iconProps }) : _jsx(TrendDownArrowOriginal, { ...iconProps });
};
export { TrendDownArrow };
