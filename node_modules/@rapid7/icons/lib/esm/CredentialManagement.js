import { jsx as _jsx } from "react/jsx-runtime";
import CredentialManagementCallisto from "./Callisto/CredentialManagement";
import CredentialManagementOriginal from "./Original/CredentialManagement";
import { useRdsThemeMetadataProvider } from "@rapid7/rds-theme-metadata-provider";
const CredentialManagement = ({ brand, ...iconProps }) => {
    const { theme: { brand: themeBrand } } = useRdsThemeMetadataProvider();
    const activeBrand = brand || themeBrand || "Callisto";
    return activeBrand === "Callisto" ? _jsx(CredentialManagementCallisto, { ...iconProps }) : _jsx(CredentialManagementOriginal, { ...iconProps });
};
export { CredentialManagement };
