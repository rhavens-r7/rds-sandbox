import { jsx as _jsx } from "react/jsx-runtime";
import StandardSpacingCallisto from "./Callisto/StandardSpacing";
import StandardSpacingOriginal from "./Original/StandardSpacing";
import { useRdsThemeMetadataProvider } from "@rapid7/rds-theme-metadata-provider";
const StandardSpacing = ({ brand, ...iconProps }) => {
    const { theme: { brand: themeBrand } } = useRdsThemeMetadataProvider();
    const activeBrand = brand || themeBrand || "Callisto";
    return activeBrand === "Callisto" ? _jsx(StandardSpacingCallisto, { ...iconProps }) : _jsx(StandardSpacingOriginal, { ...iconProps });
};
export { StandardSpacing };
