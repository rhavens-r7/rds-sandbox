import { jsx as _jsx } from "react/jsx-runtime";
import CompactSpacingCallisto from "./Callisto/CompactSpacing";
import CompactSpacingOriginal from "./Original/CompactSpacing";
import { useRdsThemeMetadataProvider } from "@rapid7/rds-theme-metadata-provider";
const CompactSpacing = ({ brand, ...iconProps }) => {
    const { theme: { brand: themeBrand } } = useRdsThemeMetadataProvider();
    const activeBrand = brand || themeBrand || "Callisto";
    return activeBrand === "Callisto" ? _jsx(CompactSpacingCallisto, { ...iconProps }) : _jsx(CompactSpacingOriginal, { ...iconProps });
};
export { CompactSpacing };
