import { jsx as _jsx } from "react/jsx-runtime";
import SensitiveDataCallisto from "./Callisto/SensitiveData";
import SensitiveDataOriginal from "./Original/SensitiveData";
import { useRdsThemeMetadataProvider } from "@rapid7/rds-theme-metadata-provider";
const SensitiveData = ({ brand, ...iconProps }) => {
    const { theme: { brand: themeBrand } } = useRdsThemeMetadataProvider();
    const activeBrand = brand || themeBrand || "Callisto";
    return activeBrand === "Callisto" ? _jsx(SensitiveDataCallisto, { ...iconProps }) : _jsx(SensitiveDataOriginal, { ...iconProps });
};
export { SensitiveData };
