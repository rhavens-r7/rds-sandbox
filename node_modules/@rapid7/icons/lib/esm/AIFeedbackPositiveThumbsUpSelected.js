import { jsx as _jsx } from "react/jsx-runtime";
import AIFeedbackPositiveThumbsUpSelectedCallisto from "./Callisto/AIFeedbackPositiveThumbsUpSelected";
import AIFeedbackPositiveThumbsUpSelectedOriginal from "./Original/AIFeedbackPositiveThumbsUpSelected";
import { useRdsThemeMetadataProvider } from "@rapid7/rds-theme-metadata-provider";
const AIFeedbackPositiveThumbsUpSelected = ({ brand, ...iconProps }) => {
    const { theme: { brand: themeBrand } } = useRdsThemeMetadataProvider();
    const activeBrand = brand || themeBrand || "Callisto";
    return activeBrand === "Callisto" ? _jsx(AIFeedbackPositiveThumbsUpSelectedCallisto, { ...iconProps }) : _jsx(AIFeedbackPositiveThumbsUpSelectedOriginal, { ...iconProps });
};
export { AIFeedbackPositiveThumbsUpSelected };
