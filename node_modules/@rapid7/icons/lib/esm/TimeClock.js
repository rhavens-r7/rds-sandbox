import { jsx as _jsx } from "react/jsx-runtime";
import TimeClockCallisto from "./Callisto/TimeClock";
import TimeClockOriginal from "./Original/TimeClock";
import { useRdsThemeMetadataProvider } from "@rapid7/rds-theme-metadata-provider";
const TimeClock = ({ brand, ...iconProps }) => {
    const { theme: { brand: themeBrand } } = useRdsThemeMetadataProvider();
    const activeBrand = brand || themeBrand || "Callisto";
    return activeBrand === "Callisto" ? _jsx(TimeClockCallisto, { ...iconProps }) : _jsx(TimeClockOriginal, { ...iconProps });
};
export { TimeClock };
