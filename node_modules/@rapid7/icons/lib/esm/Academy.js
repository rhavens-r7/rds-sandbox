import { jsx as _jsx } from "react/jsx-runtime";
import AcademyCallisto from "./Callisto/Academy";
import AcademyOriginal from "./Original/Academy";
import { useRdsThemeMetadataProvider } from "@rapid7/rds-theme-metadata-provider";
const Academy = ({ brand, ...iconProps }) => {
    const { theme: { brand: themeBrand } } = useRdsThemeMetadataProvider();
    const activeBrand = brand || themeBrand || "Callisto";
    return activeBrand === "Callisto" ? _jsx(AcademyCallisto, { ...iconProps }) : _jsx(AcademyOriginal, { ...iconProps });
};
export { Academy };
