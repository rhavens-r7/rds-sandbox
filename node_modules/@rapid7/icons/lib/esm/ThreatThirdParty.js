import { jsx as _jsx } from "react/jsx-runtime";
import ThreatThirdPartyCallisto from "./Callisto/ThreatThirdParty";
import ThreatThirdPartyOriginal from "./Original/ThreatThirdParty";
import { useRdsThemeMetadataProvider } from "@rapid7/rds-theme-metadata-provider";
const ThreatThirdParty = ({ brand, ...iconProps }) => {
    const { theme: { brand: themeBrand } } = useRdsThemeMetadataProvider();
    const activeBrand = brand || themeBrand || "Callisto";
    return activeBrand === "Callisto" ? _jsx(ThreatThirdPartyCallisto, { ...iconProps }) : _jsx(ThreatThirdPartyOriginal, { ...iconProps });
};
export { ThreatThirdParty };
