import { jsx as _jsx } from "react/jsx-runtime";
import CommentCallisto from "./Callisto/Comment";
import CommentOriginal from "./Original/Comment";
import { useRdsThemeMetadataProvider } from "@rapid7/rds-theme-metadata-provider";
const Comment = ({ brand, ...iconProps }) => {
    const { theme: { brand: themeBrand } } = useRdsThemeMetadataProvider();
    const activeBrand = brand || themeBrand || "Callisto";
    return activeBrand === "Callisto" ? _jsx(CommentCallisto, { ...iconProps }) : _jsx(CommentOriginal, { ...iconProps });
};
export { Comment };
