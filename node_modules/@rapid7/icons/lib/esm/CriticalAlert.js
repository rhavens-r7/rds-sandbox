import { jsx as _jsx } from "react/jsx-runtime";
import CriticalAlertCallisto from "./Callisto/CriticalAlert";
import CriticalAlertOriginal from "./Original/CriticalAlert";
import { useRdsThemeMetadataProvider } from "@rapid7/rds-theme-metadata-provider";
const CriticalAlert = ({ brand, ...iconProps }) => {
    const { theme: { brand: themeBrand } } = useRdsThemeMetadataProvider();
    const activeBrand = brand || themeBrand || "Callisto";
    return activeBrand === "Callisto" ? _jsx(CriticalAlertCallisto, { ...iconProps }) : _jsx(CriticalAlertOriginal, { ...iconProps });
};
export { CriticalAlert };
