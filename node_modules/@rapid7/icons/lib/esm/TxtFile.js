import { jsx as _jsx } from "react/jsx-runtime";
import TxtFileCallisto from "./Callisto/TxtFile";
import TxtFileOriginal from "./Original/TxtFile";
import { useRdsThemeMetadataProvider } from "@rapid7/rds-theme-metadata-provider";
const TxtFile = ({ brand, ...iconProps }) => {
    const { theme: { brand: themeBrand } } = useRdsThemeMetadataProvider();
    const activeBrand = brand || themeBrand || "Callisto";
    return activeBrand === "Callisto" ? _jsx(TxtFileCallisto, { ...iconProps }) : _jsx(TxtFileOriginal, { ...iconProps });
};
export { TxtFile };
