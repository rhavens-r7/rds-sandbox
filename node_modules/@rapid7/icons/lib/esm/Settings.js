import { jsx as _jsx } from "react/jsx-runtime";
import SettingsCallisto from "./Callisto/Settings";
import SettingsOriginal from "./Original/Settings";
import { useRdsThemeMetadataProvider } from "@rapid7/rds-theme-metadata-provider";
const Settings = ({ brand, ...iconProps }) => {
    const { theme: { brand: themeBrand } } = useRdsThemeMetadataProvider();
    const activeBrand = brand || themeBrand || "Callisto";
    return activeBrand === "Callisto" ? _jsx(SettingsCallisto, { ...iconProps }) : _jsx(SettingsOriginal, { ...iconProps });
};
export { Settings };
