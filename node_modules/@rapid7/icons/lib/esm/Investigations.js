import { jsx as _jsx } from "react/jsx-runtime";
import InvestigationsCallisto from "./Callisto/Investigations";
import InvestigationsOriginal from "./Original/Investigations";
import { useRdsThemeMetadataProvider } from "@rapid7/rds-theme-metadata-provider";
const Investigations = ({ brand, ...iconProps }) => {
    const { theme: { brand: themeBrand } } = useRdsThemeMetadataProvider();
    const activeBrand = brand || themeBrand || "Callisto";
    return activeBrand === "Callisto" ? _jsx(InvestigationsCallisto, { ...iconProps }) : _jsx(InvestigationsOriginal, { ...iconProps });
};
export { Investigations };
