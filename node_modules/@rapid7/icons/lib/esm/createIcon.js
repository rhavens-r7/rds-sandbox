import { jsx as _jsx } from "react/jsx-runtime";
import React from "react";
import { createSvgIcon } from "@mui/material";
export const createIcon = (svgName, displayName, svg) => {
    const SvgIcon = createSvgIcon(svg, svgName);
    const Icon = Object.assign(
    // Forwards ref so icons can be tooltip anchors - https://mui.com/material-ui/react-tooltip/#custom-child-element
    React.forwardRef((props, ref) => (_jsx(SvgIcon, { ...props, "data-testid": displayName, fill: "currentColor", ref: ref }))), {
        muiName: svgName,
        displayName: `@rapid7/icons(${svgName})`,
    });
    return Icon;
};
