import { jsx as _jsx } from "react/jsx-runtime";
import StackedBarChartCallisto from "./Callisto/StackedBarChart";
import StackedBarChartOriginal from "./Original/StackedBarChart";
import { useRdsThemeMetadataProvider } from "@rapid7/rds-theme-metadata-provider";
const StackedBarChart = ({ brand, ...iconProps }) => {
    const { theme: { brand: themeBrand } } = useRdsThemeMetadataProvider();
    const activeBrand = brand || themeBrand || "Callisto";
    return activeBrand === "Callisto" ? _jsx(StackedBarChartCallisto, { ...iconProps }) : _jsx(StackedBarChartOriginal, { ...iconProps });
};
export { StackedBarChart };
