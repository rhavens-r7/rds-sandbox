import { jsx as _jsx } from "react/jsx-runtime";
import ReportingCallisto from "./Callisto/Reporting";
import ReportingOriginal from "./Original/Reporting";
import { useRdsThemeMetadataProvider } from "@rapid7/rds-theme-metadata-provider";
const Reporting = ({ brand, ...iconProps }) => {
    const { theme: { brand: themeBrand } } = useRdsThemeMetadataProvider();
    const activeBrand = brand || themeBrand || "Callisto";
    return activeBrand === "Callisto" ? _jsx(ReportingCallisto, { ...iconProps }) : _jsx(ReportingOriginal, { ...iconProps });
};
export { Reporting };
