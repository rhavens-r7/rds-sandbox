import { jsx as _jsx } from "react/jsx-runtime";
import PageNotesCallisto from "./Callisto/PageNotes";
import PageNotesOriginal from "./Original/PageNotes";
import { useRdsThemeMetadataProvider } from "@rapid7/rds-theme-metadata-provider";
const PageNotes = ({ brand, ...iconProps }) => {
    const { theme: { brand: themeBrand } } = useRdsThemeMetadataProvider();
    const activeBrand = brand || themeBrand || "Callisto";
    return activeBrand === "Callisto" ? _jsx(PageNotesCallisto, { ...iconProps }) : _jsx(PageNotesOriginal, { ...iconProps });
};
export { PageNotes };
