import { jsx as _jsx } from "react/jsx-runtime";
import EndpointCallisto from "./Callisto/Endpoint";
import EndpointOriginal from "./Original/Endpoint";
import { useRdsThemeMetadataProvider } from "@rapid7/rds-theme-metadata-provider";
const Endpoint = ({ brand, ...iconProps }) => {
    const { theme: { brand: themeBrand } } = useRdsThemeMetadataProvider();
    const activeBrand = brand || themeBrand || "Callisto";
    return activeBrand === "Callisto" ? _jsx(EndpointCallisto, { ...iconProps }) : _jsx(EndpointOriginal, { ...iconProps });
};
export { Endpoint };
