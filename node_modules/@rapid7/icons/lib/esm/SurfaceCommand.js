import { jsx as _jsx } from "react/jsx-runtime";
import SurfaceCommandCallisto from "./Callisto/SurfaceCommand";
import SurfaceCommandOriginal from "./Original/SurfaceCommand";
import { useRdsThemeMetadataProvider } from "@rapid7/rds-theme-metadata-provider";
const SurfaceCommand = ({ brand, ...iconProps }) => {
    const { theme: { brand: themeBrand } } = useRdsThemeMetadataProvider();
    const activeBrand = brand || themeBrand || "Callisto";
    return activeBrand === "Callisto" ? _jsx(SurfaceCommandCallisto, { ...iconProps }) : _jsx(SurfaceCommandOriginal, { ...iconProps });
};
export { SurfaceCommand };
