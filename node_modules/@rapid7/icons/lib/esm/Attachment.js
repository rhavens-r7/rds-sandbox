import { jsx as _jsx } from "react/jsx-runtime";
import AttachmentCallisto from "./Callisto/Attachment";
import AttachmentOriginal from "./Original/Attachment";
import { useRdsThemeMetadataProvider } from "@rapid7/rds-theme-metadata-provider";
const Attachment = ({ brand, ...iconProps }) => {
    const { theme: { brand: themeBrand } } = useRdsThemeMetadataProvider();
    const activeBrand = brand || themeBrand || "Callisto";
    return activeBrand === "Callisto" ? _jsx(AttachmentCallisto, { ...iconProps }) : _jsx(AttachmentOriginal, { ...iconProps });
};
export { Attachment };
