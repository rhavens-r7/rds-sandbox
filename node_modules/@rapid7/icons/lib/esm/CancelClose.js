import { jsx as _jsx } from "react/jsx-runtime";
import CancelCloseCallisto from "./Callisto/CancelClose";
import CancelCloseOriginal from "./Original/CancelClose";
import { useRdsThemeMetadataProvider } from "@rapid7/rds-theme-metadata-provider";
const CancelClose = ({ brand, ...iconProps }) => {
    const { theme: { brand: themeBrand } } = useRdsThemeMetadataProvider();
    const activeBrand = brand || themeBrand || "Callisto";
    return activeBrand === "Callisto" ? _jsx(CancelCloseCallisto, { ...iconProps }) : _jsx(CancelCloseOriginal, { ...iconProps });
};
export { CancelClose };
