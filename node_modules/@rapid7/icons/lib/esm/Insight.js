import { jsx as _jsx } from "react/jsx-runtime";
import InsightCallisto from "./Callisto/Insight";
import InsightOriginal from "./Original/Insight";
import { useRdsThemeMetadataProvider } from "@rapid7/rds-theme-metadata-provider";
const Insight = ({ brand, ...iconProps }) => {
    const { theme: { brand: themeBrand } } = useRdsThemeMetadataProvider();
    const activeBrand = brand || themeBrand || "Callisto";
    return activeBrand === "Callisto" ? _jsx(InsightCallisto, { ...iconProps }) : _jsx(InsightOriginal, { ...iconProps });
};
export { Insight };
