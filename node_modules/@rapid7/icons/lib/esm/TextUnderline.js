import { jsx as _jsx } from "react/jsx-runtime";
import TextunderlineiconCallisto from "./Callisto/Textunderlineicon";
import TextunderlineiconOriginal from "./Original/Textunderlineicon";
import { useRdsThemeMetadataProvider } from "@rapid7/rds-theme-metadata-provider";
const TextUnderline = ({ brand, ...iconProps }) => {
    const { theme: { brand: themeBrand } } = useRdsThemeMetadataProvider();
    const activeBrand = brand || themeBrand || "Callisto";
    return activeBrand === "Callisto" ? _jsx(TextunderlineiconCallisto, { ...iconProps }) : _jsx(TextunderlineiconOriginal, { ...iconProps });
};
export { TextUnderline };
