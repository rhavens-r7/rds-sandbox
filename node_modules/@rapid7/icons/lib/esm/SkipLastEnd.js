import { jsx as _jsx } from "react/jsx-runtime";
import SkipLastEndCallisto from "./Callisto/SkipLastEnd";
import SkipLastEndOriginal from "./Original/SkipLastEnd";
import { useRdsThemeMetadataProvider } from "@rapid7/rds-theme-metadata-provider";
const SkipLastEnd = ({ brand, ...iconProps }) => {
    const { theme: { brand: themeBrand } } = useRdsThemeMetadataProvider();
    const activeBrand = brand || themeBrand || "Callisto";
    return activeBrand === "Callisto" ? _jsx(SkipLastEndCallisto, { ...iconProps }) : _jsx(SkipLastEndOriginal, { ...iconProps });
};
export { SkipLastEnd };
