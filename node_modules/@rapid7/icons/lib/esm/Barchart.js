import { jsx as _jsx } from "react/jsx-runtime";
import BarchartCallisto from "./Callisto/Barchart";
import BarchartOriginal from "./Original/Barchart";
import { useRdsThemeMetadataProvider } from "@rapid7/rds-theme-metadata-provider";
const Barchart = ({ brand, ...iconProps }) => {
    const { theme: { brand: themeBrand } } = useRdsThemeMetadataProvider();
    const activeBrand = brand || themeBrand || "Callisto";
    return activeBrand === "Callisto" ? _jsx(BarchartCallisto, { ...iconProps }) : _jsx(BarchartOriginal, { ...iconProps });
};
export { Barchart };
