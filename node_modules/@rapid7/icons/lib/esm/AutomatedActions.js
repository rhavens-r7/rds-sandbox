import { jsx as _jsx } from "react/jsx-runtime";
import AutomatedActionsCallisto from "./Callisto/AutomatedActions";
import AutomatedActionsOriginal from "./Original/AutomatedActions";
import { useRdsThemeMetadataProvider } from "@rapid7/rds-theme-metadata-provider";
const AutomatedActions = ({ brand, ...iconProps }) => {
    const { theme: { brand: themeBrand } } = useRdsThemeMetadataProvider();
    const activeBrand = brand || themeBrand || "Callisto";
    return activeBrand === "Callisto" ? _jsx(AutomatedActionsCallisto, { ...iconProps }) : _jsx(AutomatedActionsOriginal, { ...iconProps });
};
export { AutomatedActions };
