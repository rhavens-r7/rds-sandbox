import { jsx as _jsx } from "react/jsx-runtime";
import ImportPluginCallisto from "./Callisto/ImportPlugin";
import ImportPluginOriginal from "./Original/ImportPlugin";
import { useRdsThemeMetadataProvider } from "@rapid7/rds-theme-metadata-provider";
const ImportPlugin = ({ brand, ...iconProps }) => {
    const { theme: { brand: themeBrand } } = useRdsThemeMetadataProvider();
    const activeBrand = brand || themeBrand || "Callisto";
    return activeBrand === "Callisto" ? _jsx(ImportPluginCallisto, { ...iconProps }) : _jsx(ImportPluginOriginal, { ...iconProps });
};
export { ImportPlugin };
