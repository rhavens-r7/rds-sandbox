import { jsx as _jsx } from "react/jsx-runtime";
import LogSearchCallisto from "./Callisto/LogSearch";
import LogSearchOriginal from "./Original/LogSearch";
import { useRdsThemeMetadataProvider } from "@rapid7/rds-theme-metadata-provider";
const LogSearch = ({ brand, ...iconProps }) => {
    const { theme: { brand: themeBrand } } = useRdsThemeMetadataProvider();
    const activeBrand = brand || themeBrand || "Callisto";
    return activeBrand === "Callisto" ? _jsx(LogSearchCallisto, { ...iconProps }) : _jsx(LogSearchOriginal, { ...iconProps });
};
export { LogSearch };
