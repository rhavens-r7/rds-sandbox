import { jsx as _jsx } from "react/jsx-runtime";
import GroupByCallisto from "./Callisto/GroupBy";
import GroupByOriginal from "./Original/GroupBy";
import { useRdsThemeMetadataProvider } from "@rapid7/rds-theme-metadata-provider";
const GroupBy = ({ brand, ...iconProps }) => {
    const { theme: { brand: themeBrand } } = useRdsThemeMetadataProvider();
    const activeBrand = brand || themeBrand || "Callisto";
    return activeBrand === "Callisto" ? _jsx(GroupByCallisto, { ...iconProps }) : _jsx(GroupByOriginal, { ...iconProps });
};
export { GroupBy };
