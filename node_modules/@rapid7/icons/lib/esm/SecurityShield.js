import { jsx as _jsx } from "react/jsx-runtime";
import SecurityShieldCallisto from "./Callisto/SecurityShield";
import SecurityShieldOriginal from "./Original/SecurityShield";
import { useRdsThemeMetadataProvider } from "@rapid7/rds-theme-metadata-provider";
const SecurityShield = ({ brand, ...iconProps }) => {
    const { theme: { brand: themeBrand } } = useRdsThemeMetadataProvider();
    const activeBrand = brand || themeBrand || "Callisto";
    return activeBrand === "Callisto" ? _jsx(SecurityShieldCallisto, { ...iconProps }) : _jsx(SecurityShieldOriginal, { ...iconProps });
};
export { SecurityShield };
