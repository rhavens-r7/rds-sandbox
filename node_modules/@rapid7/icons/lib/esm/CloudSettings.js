import { jsx as _jsx } from "react/jsx-runtime";
import CloudSettingsCallisto from "./Callisto/CloudSettings";
import CloudSettingsOriginal from "./Original/CloudSettings";
import { useRdsThemeMetadataProvider } from "@rapid7/rds-theme-metadata-provider";
const CloudSettings = ({ brand, ...iconProps }) => {
    const { theme: { brand: themeBrand } } = useRdsThemeMetadataProvider();
    const activeBrand = brand || themeBrand || "Callisto";
    return activeBrand === "Callisto" ? _jsx(CloudSettingsCallisto, { ...iconProps }) : _jsx(CloudSettingsOriginal, { ...iconProps });
};
export { CloudSettings };
