import { jsx as _jsx } from "react/jsx-runtime";
import ImpactLowCallisto from "./Callisto/ImpactLow";
import ImpactLowOriginal from "./Original/ImpactLow";
import { useRdsThemeMetadataProvider } from "@rapid7/rds-theme-metadata-provider";
const ImpactLow = ({ brand, ...iconProps }) => {
    const { theme: { brand: themeBrand } } = useRdsThemeMetadataProvider();
    const activeBrand = brand || themeBrand || "Callisto";
    return activeBrand === "Callisto" ? _jsx(ImpactLowCallisto, { ...iconProps }) : _jsx(ImpactLowOriginal, { ...iconProps });
};
export { ImpactLow };
