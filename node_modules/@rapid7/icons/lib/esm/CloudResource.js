import { jsx as _jsx } from "react/jsx-runtime";
import CloudResourceCallisto from "./Callisto/CloudResource";
import CloudResourceOriginal from "./Original/CloudResource";
import { useRdsThemeMetadataProvider } from "@rapid7/rds-theme-metadata-provider";
const CloudResource = ({ brand, ...iconProps }) => {
    const { theme: { brand: themeBrand } } = useRdsThemeMetadataProvider();
    const activeBrand = brand || themeBrand || "Callisto";
    return activeBrand === "Callisto" ? _jsx(CloudResourceCallisto, { ...iconProps }) : _jsx(CloudResourceOriginal, { ...iconProps });
};
export { CloudResource };
