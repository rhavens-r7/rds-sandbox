import { jsx as _jsx } from "react/jsx-runtime";
import SecurityAlertSirenCallisto from "./Callisto/SecurityAlertSiren";
import SecurityAlertSirenOriginal from "./Original/SecurityAlertSiren";
import { useRdsThemeMetadataProvider } from "@rapid7/rds-theme-metadata-provider";
const SecurityAlertSiren = ({ brand, ...iconProps }) => {
    const { theme: { brand: themeBrand } } = useRdsThemeMetadataProvider();
    const activeBrand = brand || themeBrand || "Callisto";
    return activeBrand === "Callisto" ? _jsx(SecurityAlertSirenCallisto, { ...iconProps }) : _jsx(SecurityAlertSirenOriginal, { ...iconProps });
};
export { SecurityAlertSiren };
