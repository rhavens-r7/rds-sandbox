import { jsx as _jsx } from "react/jsx-runtime";
import AgentIdleCallisto from "./Callisto/AgentIdle";
import AgentIdleOriginal from "./Original/AgentIdle";
import { useRdsThemeMetadataProvider } from "@rapid7/rds-theme-metadata-provider";
const AgentIdle = ({ brand, ...iconProps }) => {
    const { theme: { brand: themeBrand } } = useRdsThemeMetadataProvider();
    const activeBrand = brand || themeBrand || "Callisto";
    return activeBrand === "Callisto" ? _jsx(AgentIdleCallisto, { ...iconProps }) : _jsx(AgentIdleOriginal, { ...iconProps });
};
export { AgentIdle };
