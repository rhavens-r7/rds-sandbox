import { jsx as _jsx } from "react/jsx-runtime";
import AddPlusUnboundCallisto from "./Callisto/AddPlusUnbound";
import AddPlusUnboundOriginal from "./Original/AddPlusUnbound";
import { useRdsThemeMetadataProvider } from "@rapid7/rds-theme-metadata-provider";
const AddPlusUnbound = ({ brand, ...iconProps }) => {
    const { theme: { brand: themeBrand } } = useRdsThemeMetadataProvider();
    const activeBrand = brand || themeBrand || "Callisto";
    return activeBrand === "Callisto" ? _jsx(AddPlusUnboundCallisto, { ...iconProps }) : _jsx(AddPlusUnboundOriginal, { ...iconProps });
};
export { AddPlusUnbound };
