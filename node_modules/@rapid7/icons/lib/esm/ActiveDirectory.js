import { jsx as _jsx } from "react/jsx-runtime";
import ActiveDirectoryCallisto from "./Callisto/ActiveDirectory";
import ActiveDirectoryOriginal from "./Original/ActiveDirectory";
import { useRdsThemeMetadataProvider } from "@rapid7/rds-theme-metadata-provider";
const ActiveDirectory = ({ brand, ...iconProps }) => {
    const { theme: { brand: themeBrand } } = useRdsThemeMetadataProvider();
    const activeBrand = brand || themeBrand || "Callisto";
    return activeBrand === "Callisto" ? _jsx(ActiveDirectoryCallisto, { ...iconProps }) : _jsx(ActiveDirectoryOriginal, { ...iconProps });
};
export { ActiveDirectory };
