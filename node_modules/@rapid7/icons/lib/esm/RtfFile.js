import { jsx as _jsx } from "react/jsx-runtime";
import RtfFileCallisto from "./Callisto/RtfFile";
import RtfFileOriginal from "./Original/RtfFile";
import { useRdsThemeMetadataProvider } from "@rapid7/rds-theme-metadata-provider";
const RtfFile = ({ brand, ...iconProps }) => {
    const { theme: { brand: themeBrand } } = useRdsThemeMetadataProvider();
    const activeBrand = brand || themeBrand || "Callisto";
    return activeBrand === "Callisto" ? _jsx(RtfFileCallisto, { ...iconProps }) : _jsx(RtfFileOriginal, { ...iconProps });
};
export { RtfFile };
