import { jsx as _jsx } from "react/jsx-runtime";
import ShowEyeCallisto from "./Callisto/ShowEye";
import ShowEyeOriginal from "./Original/ShowEye";
import { useRdsThemeMetadataProvider } from "@rapid7/rds-theme-metadata-provider";
const ShowEye = ({ brand, ...iconProps }) => {
    const { theme: { brand: themeBrand } } = useRdsThemeMetadataProvider();
    const activeBrand = brand || themeBrand || "Callisto";
    return activeBrand === "Callisto" ? _jsx(ShowEyeCallisto, { ...iconProps }) : _jsx(ShowEyeOriginal, { ...iconProps });
};
export { ShowEye };
