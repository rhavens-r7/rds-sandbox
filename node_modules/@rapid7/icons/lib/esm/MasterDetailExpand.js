import { jsx as _jsx } from "react/jsx-runtime";
import MasterDetailExpandCallisto from "./Callisto/MasterDetailExpand";
import MasterDetailExpandOriginal from "./Original/MasterDetailExpand";
import { useRdsThemeMetadataProvider } from "@rapid7/rds-theme-metadata-provider";
const MasterDetailExpand = ({ brand, ...iconProps }) => {
    const { theme: { brand: themeBrand } } = useRdsThemeMetadataProvider();
    const activeBrand = brand || themeBrand || "Callisto";
    return activeBrand === "Callisto" ? _jsx(MasterDetailExpandCallisto, { ...iconProps }) : _jsx(MasterDetailExpandOriginal, { ...iconProps });
};
export { MasterDetailExpand };
