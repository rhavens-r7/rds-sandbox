import { jsx as _jsx } from "react/jsx-runtime";
import DashboardCallisto from "./Callisto/Dashboard";
import DashboardOriginal from "./Original/Dashboard";
import { useRdsThemeMetadataProvider } from "@rapid7/rds-theme-metadata-provider";
const Dashboard = ({ brand, ...iconProps }) => {
    const { theme: { brand: themeBrand } } = useRdsThemeMetadataProvider();
    const activeBrand = brand || themeBrand || "Callisto";
    return activeBrand === "Callisto" ? _jsx(DashboardCallisto, { ...iconProps }) : _jsx(DashboardOriginal, { ...iconProps });
};
export { Dashboard };
