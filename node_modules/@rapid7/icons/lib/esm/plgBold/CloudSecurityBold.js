import { jsx as _jsx } from "react/jsx-runtime";
import CloudSecurityBoldCallisto from "../Callisto/CloudSecurityBold";
import CloudSecurityBoldOriginal from "../Original/CloudSecurityBold";
import { useRdsThemeMetadataProvider } from "@rapid7/rds-theme-metadata-provider";
const CloudSecurityBold = ({ brand, ...iconProps }) => {
    const { theme: { brand: themeBrand } } = useRdsThemeMetadataProvider();
    const activeBrand = brand || themeBrand || "Callisto";
    return activeBrand === "Callisto" ? _jsx(CloudSecurityBoldCallisto, { ...iconProps }) : _jsx(CloudSecurityBoldOriginal, { ...iconProps });
};
export { CloudSecurityBold };
