import { jsx as _jsx } from "react/jsx-runtime";
import ApplicationSecurityBoldCallisto from "../Callisto/ApplicationSecurityBold";
import ApplicationSecurityBoldOriginal from "../Original/ApplicationSecurityBold";
import { useRdsThemeMetadataProvider } from "@rapid7/rds-theme-metadata-provider";
const ApplicationSecurityBold = ({ brand, ...iconProps }) => {
    const { theme: { brand: themeBrand } } = useRdsThemeMetadataProvider();
    const activeBrand = brand || themeBrand || "Callisto";
    return activeBrand === "Callisto" ? _jsx(ApplicationSecurityBoldCallisto, { ...iconProps }) : _jsx(ApplicationSecurityBoldOriginal, { ...iconProps });
};
export { ApplicationSecurityBold };
