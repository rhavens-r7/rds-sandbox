import { jsx as _jsx } from "react/jsx-runtime";
import ExposureCommandBoldCallisto from "../Callisto/ExposureCommandBold";
import ExposureCommandBoldOriginal from "../Original/ExposureCommandBold";
import { useRdsThemeMetadataProvider } from "@rapid7/rds-theme-metadata-provider";
const ExposureCommandBold = ({ brand, ...iconProps }) => {
    const { theme: { brand: themeBrand } } = useRdsThemeMetadataProvider();
    const activeBrand = brand || themeBrand || "Callisto";
    return activeBrand === "Callisto" ? _jsx(ExposureCommandBoldCallisto, { ...iconProps }) : _jsx(ExposureCommandBoldOriginal, { ...iconProps });
};
export { ExposureCommandBold };
