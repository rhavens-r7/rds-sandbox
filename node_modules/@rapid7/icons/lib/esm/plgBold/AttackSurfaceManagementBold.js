import { jsx as _jsx } from "react/jsx-runtime";
import AttackSurfaceManagementBoldCallisto from "../Callisto/AttackSurfaceManagementBold";
import AttackSurfaceManagementBoldOriginal from "../Original/AttackSurfaceManagementBold";
import { useRdsThemeMetadataProvider } from "@rapid7/rds-theme-metadata-provider";
const AttackSurfaceManagementBold = ({ brand, ...iconProps }) => {
    const { theme: { brand: themeBrand } } = useRdsThemeMetadataProvider();
    const activeBrand = brand || themeBrand || "Callisto";
    return activeBrand === "Callisto" ? _jsx(AttackSurfaceManagementBoldCallisto, { ...iconProps }) : _jsx(AttackSurfaceManagementBoldOriginal, { ...iconProps });
};
export { AttackSurfaceManagementBold };
