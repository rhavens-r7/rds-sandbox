import { jsx as _jsx } from "react/jsx-runtime";
import ManagedServicesBoldCallisto from "../Callisto/ManagedServicesBold";
import ManagedServicesBoldOriginal from "../Original/ManagedServicesBold";
import { useRdsThemeMetadataProvider } from "@rapid7/rds-theme-metadata-provider";
const ManagedServicesBold = ({ brand, ...iconProps }) => {
    const { theme: { brand: themeBrand } } = useRdsThemeMetadataProvider();
    const activeBrand = brand || themeBrand || "Callisto";
    return activeBrand === "Callisto" ? _jsx(ManagedServicesBoldCallisto, { ...iconProps }) : _jsx(ManagedServicesBoldOriginal, { ...iconProps });
};
export { ManagedServicesBold };
