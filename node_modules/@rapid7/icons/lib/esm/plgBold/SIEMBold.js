import { jsx as _jsx } from "react/jsx-runtime";
import SIEMBoldCallisto from "../Callisto/SIEMBold";
import SIEMBoldOriginal from "../Original/SIEMBold";
import { useRdsThemeMetadataProvider } from "@rapid7/rds-theme-metadata-provider";
const SIEMBold = ({ brand, ...iconProps }) => {
    const { theme: { brand: themeBrand } } = useRdsThemeMetadataProvider();
    const activeBrand = brand || themeBrand || "Callisto";
    return activeBrand === "Callisto" ? _jsx(SIEMBoldCallisto, { ...iconProps }) : _jsx(SIEMBoldOriginal, { ...iconProps });
};
export { SIEMBold };
