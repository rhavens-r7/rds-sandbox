import { jsx as _jsx } from "react/jsx-runtime";
import LogManagementBoldCallisto from "../Callisto/LogManagementBold";
import LogManagementBoldOriginal from "../Original/LogManagementBold";
import { useRdsThemeMetadataProvider } from "@rapid7/rds-theme-metadata-provider";
const LogManagementBold = ({ brand, ...iconProps }) => {
    const { theme: { brand: themeBrand } } = useRdsThemeMetadataProvider();
    const activeBrand = brand || themeBrand || "Callisto";
    return activeBrand === "Callisto" ? _jsx(LogManagementBoldCallisto, { ...iconProps }) : _jsx(LogManagementBoldOriginal, { ...iconProps });
};
export { LogManagementBold };
