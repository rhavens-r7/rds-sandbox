import { jsx as _jsx } from "react/jsx-runtime";
import AutomationBoldCallisto from "../Callisto/AutomationBold";
import AutomationBoldOriginal from "../Original/AutomationBold";
import { useRdsThemeMetadataProvider } from "@rapid7/rds-theme-metadata-provider";
const AutomationBold = ({ brand, ...iconProps }) => {
    const { theme: { brand: themeBrand } } = useRdsThemeMetadataProvider();
    const activeBrand = brand || themeBrand || "Callisto";
    return activeBrand === "Callisto" ? _jsx(AutomationBoldCallisto, { ...iconProps }) : _jsx(AutomationBoldOriginal, { ...iconProps });
};
export { AutomationBold };
