import { jsx as _jsx } from "react/jsx-runtime";
import DonutChartCallisto from "./Callisto/DonutChart";
import DonutChartOriginal from "./Original/DonutChart";
import { useRdsThemeMetadataProvider } from "@rapid7/rds-theme-metadata-provider";
const DonutChart = ({ brand, ...iconProps }) => {
    const { theme: { brand: themeBrand } } = useRdsThemeMetadataProvider();
    const activeBrand = brand || themeBrand || "Callisto";
    return activeBrand === "Callisto" ? _jsx(DonutChartCallisto, { ...iconProps }) : _jsx(DonutChartOriginal, { ...iconProps });
};
export { DonutChart };
