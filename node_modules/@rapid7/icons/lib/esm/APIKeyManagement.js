import { jsx as _jsx } from "react/jsx-runtime";
import APIKeyManagementCallisto from "./Callisto/APIKeyManagement";
import APIKeyManagementOriginal from "./Original/APIKeyManagement";
import { useRdsThemeMetadataProvider } from "@rapid7/rds-theme-metadata-provider";
const APIKeyManagement = ({ brand, ...iconProps }) => {
    const { theme: { brand: themeBrand } } = useRdsThemeMetadataProvider();
    const activeBrand = brand || themeBrand || "Callisto";
    return activeBrand === "Callisto" ? _jsx(APIKeyManagementCallisto, { ...iconProps }) : _jsx(APIKeyManagementOriginal, { ...iconProps });
};
export { APIKeyManagement };
