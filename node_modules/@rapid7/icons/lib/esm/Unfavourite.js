import { jsx as _jsx } from "react/jsx-runtime";
import UnfavouriteCallisto from "./Callisto/Unfavourite";
import UnfavouriteOriginal from "./Original/Unfavourite";
import { useRdsThemeMetadataProvider } from "@rapid7/rds-theme-metadata-provider";
const Unfavourite = ({ brand, ...iconProps }) => {
    const { theme: { brand: themeBrand } } = useRdsThemeMetadataProvider();
    const activeBrand = brand || themeBrand || "Callisto";
    return activeBrand === "Callisto" ? _jsx(UnfavouriteCallisto, { ...iconProps }) : _jsx(UnfavouriteOriginal, { ...iconProps });
};
export { Unfavourite };
