import { jsx as _jsx } from "react/jsx-runtime";
import ThreatCommandCallisto from "./Callisto/ThreatCommand";
import ThreatCommandOriginal from "./Original/ThreatCommand";
import { useRdsThemeMetadataProvider } from "@rapid7/rds-theme-metadata-provider";
const ThreatCommand = ({ brand, ...iconProps }) => {
    const { theme: { brand: themeBrand } } = useRdsThemeMetadataProvider();
    const activeBrand = brand || themeBrand || "Callisto";
    return activeBrand === "Callisto" ? _jsx(ThreatCommandCallisto, { ...iconProps }) : _jsx(ThreatCommandOriginal, { ...iconProps });
};
export { ThreatCommand };
