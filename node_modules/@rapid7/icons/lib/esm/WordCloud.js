import { jsx as _jsx } from "react/jsx-runtime";
import WordCloudCallisto from "./Callisto/WordCloud";
import WordCloudOriginal from "./Original/WordCloud";
import { useRdsThemeMetadataProvider } from "@rapid7/rds-theme-metadata-provider";
const WordCloud = ({ brand, ...iconProps }) => {
    const { theme: { brand: themeBrand } } = useRdsThemeMetadataProvider();
    const activeBrand = brand || themeBrand || "Callisto";
    return activeBrand === "Callisto" ? _jsx(WordCloudCallisto, { ...iconProps }) : _jsx(WordCloudOriginal, { ...iconProps });
};
export { WordCloud };
