import { jsx as _jsx } from "react/jsx-runtime";
import PublicAccessibilityVerifiedCallisto from "./Callisto/PublicAccessibilityVerified";
import PublicAccessibilityVerifiedOriginal from "./Original/PublicAccessibilityVerified";
import { useRdsThemeMetadataProvider } from "@rapid7/rds-theme-metadata-provider";
const PublicAccessibilityVerified = ({ brand, ...iconProps }) => {
    const { theme: { brand: themeBrand } } = useRdsThemeMetadataProvider();
    const activeBrand = brand || themeBrand || "Callisto";
    return activeBrand === "Callisto" ? _jsx(PublicAccessibilityVerifiedCallisto, { ...iconProps }) : _jsx(PublicAccessibilityVerifiedOriginal, { ...iconProps });
};
export { PublicAccessibilityVerified };
