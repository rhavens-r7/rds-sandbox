import { jsx as _jsx } from "react/jsx-runtime";
import HelpInformationCallisto from "./Callisto/HelpInformation";
import HelpInformationOriginal from "./Original/HelpInformation";
import { useRdsThemeMetadataProvider } from "@rapid7/rds-theme-metadata-provider";
const HelpInformation = ({ brand, ...iconProps }) => {
    const { theme: { brand: themeBrand } } = useRdsThemeMetadataProvider();
    const activeBrand = brand || themeBrand || "Callisto";
    return activeBrand === "Callisto" ? _jsx(HelpInformationCallisto, { ...iconProps }) : _jsx(HelpInformationOriginal, { ...iconProps });
};
export { HelpInformation };
