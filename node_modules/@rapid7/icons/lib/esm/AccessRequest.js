import { jsx as _jsx } from "react/jsx-runtime";
import AccessRequestCallisto from "./Callisto/AccessRequest";
import AccessRequestOriginal from "./Original/AccessRequest";
import { useRdsThemeMetadataProvider } from "@rapid7/rds-theme-metadata-provider";
const AccessRequest = ({ brand, ...iconProps }) => {
    const { theme: { brand: themeBrand } } = useRdsThemeMetadataProvider();
    const activeBrand = brand || themeBrand || "Callisto";
    return activeBrand === "Callisto" ? _jsx(AccessRequestCallisto, { ...iconProps }) : _jsx(AccessRequestOriginal, { ...iconProps });
};
export { AccessRequest };
