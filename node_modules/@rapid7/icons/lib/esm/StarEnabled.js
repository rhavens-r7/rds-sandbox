import { jsx as _jsx } from "react/jsx-runtime";
import StarEnabledCallisto from "./Callisto/StarEnabled";
import StarEnabledOriginal from "./Original/StarEnabled";
import { useRdsThemeMetadataProvider } from "@rapid7/rds-theme-metadata-provider";
const StarEnabled = ({ brand, ...iconProps }) => {
    const { theme: { brand: themeBrand } } = useRdsThemeMetadataProvider();
    const activeBrand = brand || themeBrand || "Callisto";
    return activeBrand === "Callisto" ? _jsx(StarEnabledCallisto, { ...iconProps }) : _jsx(StarEnabledOriginal, { ...iconProps });
};
export { StarEnabled };
