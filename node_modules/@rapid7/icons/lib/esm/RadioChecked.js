import { jsx as _jsx } from "react/jsx-runtime";
import RadioCheckedCallisto from "./Callisto/RadioChecked";
import RadioCheckedOriginal from "./Original/RadioChecked";
import { useRdsThemeMetadataProvider } from "@rapid7/rds-theme-metadata-provider";
const RadioChecked = ({ brand, ...iconProps }) => {
    const { theme: { brand: themeBrand } } = useRdsThemeMetadataProvider();
    const activeBrand = brand || themeBrand || "Callisto";
    return activeBrand === "Callisto" ? _jsx(RadioCheckedCallisto, { ...iconProps }) : _jsx(RadioCheckedOriginal, { ...iconProps });
};
export { RadioChecked };
