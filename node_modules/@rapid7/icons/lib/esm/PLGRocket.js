import { jsx as _jsx } from "react/jsx-runtime";
import PLGRocketCallisto from "./Callisto/PLGRocket";
import PLGRocketOriginal from "./Original/PLGRocket";
import { useRdsThemeMetadataProvider } from "@rapid7/rds-theme-metadata-provider";
const PLGRocket = ({ brand, ...iconProps }) => {
    const { theme: { brand: themeBrand } } = useRdsThemeMetadataProvider();
    const activeBrand = brand || themeBrand || "Callisto";
    return activeBrand === "Callisto" ? _jsx(PLGRocketCallisto, { ...iconProps }) : _jsx(PLGRocketOriginal, { ...iconProps });
};
export { PLGRocket };
