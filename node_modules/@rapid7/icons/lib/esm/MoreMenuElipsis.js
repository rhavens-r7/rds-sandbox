import { jsx as _jsx } from "react/jsx-runtime";
import MoreMenuElipsisCallisto from "./Callisto/MoreMenuElipsis";
import MoreMenuElipsisOriginal from "./Original/MoreMenuElipsis";
import { useRdsThemeMetadataProvider } from "@rapid7/rds-theme-metadata-provider";
const MoreMenuElipsis = ({ brand, ...iconProps }) => {
    const { theme: { brand: themeBrand } } = useRdsThemeMetadataProvider();
    const activeBrand = brand || themeBrand || "Callisto";
    return activeBrand === "Callisto" ? _jsx(MoreMenuElipsisCallisto, { ...iconProps }) : _jsx(MoreMenuElipsisOriginal, { ...iconProps });
};
export { MoreMenuElipsis };
