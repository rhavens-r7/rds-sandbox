import { jsx as _jsx } from "react/jsx-runtime";
import ProcessCogCallisto from "./Callisto/ProcessCog";
import ProcessCogOriginal from "./Original/ProcessCog";
import { useRdsThemeMetadataProvider } from "@rapid7/rds-theme-metadata-provider";
const ProcessCog = ({ brand, ...iconProps }) => {
    const { theme: { brand: themeBrand } } = useRdsThemeMetadataProvider();
    const activeBrand = brand || themeBrand || "Callisto";
    return activeBrand === "Callisto" ? _jsx(ProcessCogCallisto, { ...iconProps }) : _jsx(ProcessCogOriginal, { ...iconProps });
};
export { ProcessCog };
