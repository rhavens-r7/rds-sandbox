import { jsx as _jsx } from "react/jsx-runtime";
import AggregationCallisto from "./Callisto/Aggregation";
import AggregationOriginal from "./Original/Aggregation";
import { useRdsThemeMetadataProvider } from "@rapid7/rds-theme-metadata-provider";
const Aggregation = ({ brand, ...iconProps }) => {
    const { theme: { brand: themeBrand } } = useRdsThemeMetadataProvider();
    const activeBrand = brand || themeBrand || "Callisto";
    return activeBrand === "Callisto" ? _jsx(AggregationCallisto, { ...iconProps }) : _jsx(AggregationOriginal, { ...iconProps });
};
export { Aggregation };
