import { jsx as _jsx } from "react/jsx-runtime";
import AutomatedTicketingCallisto from "./Callisto/AutomatedTicketing";
import AutomatedTicketingOriginal from "./Original/AutomatedTicketing";
import { useRdsThemeMetadataProvider } from "@rapid7/rds-theme-metadata-provider";
const AutomatedTicketing = ({ brand, ...iconProps }) => {
    const { theme: { brand: themeBrand } } = useRdsThemeMetadataProvider();
    const activeBrand = brand || themeBrand || "Callisto";
    return activeBrand === "Callisto" ? _jsx(AutomatedTicketingCallisto, { ...iconProps }) : _jsx(AutomatedTicketingOriginal, { ...iconProps });
};
export { AutomatedTicketing };
