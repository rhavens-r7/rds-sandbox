import { jsx as _jsx } from "react/jsx-runtime";
import DetectionRulesCallisto from "./Callisto/DetectionRules";
import DetectionRulesOriginal from "./Original/DetectionRules";
import { useRdsThemeMetadataProvider } from "@rapid7/rds-theme-metadata-provider";
const DetectionRules = ({ brand, ...iconProps }) => {
    const { theme: { brand: themeBrand } } = useRdsThemeMetadataProvider();
    const activeBrand = brand || themeBrand || "Callisto";
    return activeBrand === "Callisto" ? _jsx(DetectionRulesCallisto, { ...iconProps }) : _jsx(DetectionRulesOriginal, { ...iconProps });
};
export { DetectionRules };
