import { jsx as _jsx } from "react/jsx-runtime";
import FeedbackCallisto from "./Callisto/Feedback";
import FeedbackOriginal from "./Original/Feedback";
import { useRdsThemeMetadataProvider } from "@rapid7/rds-theme-metadata-provider";
const Feedback = ({ brand, ...iconProps }) => {
    const { theme: { brand: themeBrand } } = useRdsThemeMetadataProvider();
    const activeBrand = brand || themeBrand || "Callisto";
    return activeBrand === "Callisto" ? _jsx(FeedbackCallisto, { ...iconProps }) : _jsx(FeedbackOriginal, { ...iconProps });
};
export { Feedback };
