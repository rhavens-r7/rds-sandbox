import { jsx as _jsx } from "react/jsx-runtime";
import PdfFileCallisto from "./Callisto/PdfFile";
import PdfFileOriginal from "./Original/PdfFile";
import { useRdsThemeMetadataProvider } from "@rapid7/rds-theme-metadata-provider";
const PdfFile = ({ brand, ...iconProps }) => {
    const { theme: { brand: themeBrand } } = useRdsThemeMetadataProvider();
    const activeBrand = brand || themeBrand || "Callisto";
    return activeBrand === "Callisto" ? _jsx(PdfFileCallisto, { ...iconProps }) : _jsx(PdfFileOriginal, { ...iconProps });
};
export { PdfFile };
