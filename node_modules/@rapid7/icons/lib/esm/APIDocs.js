import { jsx as _jsx } from "react/jsx-runtime";
import APIDocsCallisto from "./Callisto/APIDocs";
import APIDocsOriginal from "./Original/APIDocs";
import { useRdsThemeMetadataProvider } from "@rapid7/rds-theme-metadata-provider";
const APIDocs = ({ brand, ...iconProps }) => {
    const { theme: { brand: themeBrand } } = useRdsThemeMetadataProvider();
    const activeBrand = brand || themeBrand || "Callisto";
    return activeBrand === "Callisto" ? _jsx(APIDocsCallisto, { ...iconProps }) : _jsx(APIDocsOriginal, { ...iconProps });
};
export { APIDocs };
