import { jsx as _jsx } from "react/jsx-runtime";
import TrendUpArrowCallisto from "./Callisto/TrendUpArrow";
import TrendUpArrowOriginal from "./Original/TrendUpArrow";
import { useRdsThemeMetadataProvider } from "@rapid7/rds-theme-metadata-provider";
const TrendUpArrow = ({ brand, ...iconProps }) => {
    const { theme: { brand: themeBrand } } = useRdsThemeMetadataProvider();
    const activeBrand = brand || themeBrand || "Callisto";
    return activeBrand === "Callisto" ? _jsx(TrendUpArrowCallisto, { ...iconProps }) : _jsx(TrendUpArrowOriginal, { ...iconProps });
};
export { TrendUpArrow };
