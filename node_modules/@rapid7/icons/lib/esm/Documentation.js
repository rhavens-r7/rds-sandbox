import { jsx as _jsx } from "react/jsx-runtime";
import DocumentationCallisto from "./Callisto/Documentation";
import DocumentationOriginal from "./Original/Documentation";
import { useRdsThemeMetadataProvider } from "@rapid7/rds-theme-metadata-provider";
const Documentation = ({ brand, ...iconProps }) => {
    const { theme: { brand: themeBrand } } = useRdsThemeMetadataProvider();
    const activeBrand = brand || themeBrand || "Callisto";
    return activeBrand === "Callisto" ? _jsx(DocumentationCallisto, { ...iconProps }) : _jsx(DocumentationOriginal, { ...iconProps });
};
export { Documentation };
