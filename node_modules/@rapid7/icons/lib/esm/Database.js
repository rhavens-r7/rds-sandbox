import { jsx as _jsx } from "react/jsx-runtime";
import DatabaseCallisto from "./Callisto/Database";
import DatabaseOriginal from "./Original/Database";
import { useRdsThemeMetadataProvider } from "@rapid7/rds-theme-metadata-provider";
const Database = ({ brand, ...iconProps }) => {
    const { theme: { brand: themeBrand } } = useRdsThemeMetadataProvider();
    const activeBrand = brand || themeBrand || "Callisto";
    return activeBrand === "Callisto" ? _jsx(DatabaseCallisto, { ...iconProps }) : _jsx(DatabaseOriginal, { ...iconProps });
};
export { Database };
