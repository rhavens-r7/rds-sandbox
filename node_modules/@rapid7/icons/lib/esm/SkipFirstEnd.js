import { jsx as _jsx } from "react/jsx-runtime";
import SkipFirstEndCallisto from "./Callisto/SkipFirstEnd";
import SkipFirstEndOriginal from "./Original/SkipFirstEnd";
import { useRdsThemeMetadataProvider } from "@rapid7/rds-theme-metadata-provider";
const SkipFirstEnd = ({ brand, ...iconProps }) => {
    const { theme: { brand: themeBrand } } = useRdsThemeMetadataProvider();
    const activeBrand = brand || themeBrand || "Callisto";
    return activeBrand === "Callisto" ? _jsx(SkipFirstEndCallisto, { ...iconProps }) : _jsx(SkipFirstEndOriginal, { ...iconProps });
};
export { SkipFirstEnd };
