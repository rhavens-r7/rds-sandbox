import { jsx as _jsx } from "react/jsx-runtime";
import VerifiedCallisto from "./Callisto/Verified";
import VerifiedOriginal from "./Original/Verified";
import { useRdsThemeMetadataProvider } from "@rapid7/rds-theme-metadata-provider";
const Verified = ({ brand, ...iconProps }) => {
    const { theme: { brand: themeBrand } } = useRdsThemeMetadataProvider();
    const activeBrand = brand || themeBrand || "Callisto";
    return activeBrand === "Callisto" ? _jsx(VerifiedCallisto, { ...iconProps }) : _jsx(VerifiedOriginal, { ...iconProps });
};
export { Verified };
