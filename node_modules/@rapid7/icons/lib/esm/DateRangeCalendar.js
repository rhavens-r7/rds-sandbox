import { jsx as _jsx } from "react/jsx-runtime";
import DateRangeCalendarCallisto from "./Callisto/DateRangeCalendar";
import DateRangeCalendarOriginal from "./Original/DateRangeCalendar";
import { useRdsThemeMetadataProvider } from "@rapid7/rds-theme-metadata-provider";
const DateRangeCalendar = ({ brand, ...iconProps }) => {
    const { theme: { brand: themeBrand } } = useRdsThemeMetadataProvider();
    const activeBrand = brand || themeBrand || "Callisto";
    return activeBrand === "Callisto" ? _jsx(DateRangeCalendarCallisto, { ...iconProps }) : _jsx(DateRangeCalendarOriginal, { ...iconProps });
};
export { DateRangeCalendar };
