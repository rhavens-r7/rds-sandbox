import { jsx as _jsx } from "react/jsx-runtime";
import MediumAlertCallisto from "./Callisto/MediumAlert";
import MediumAlertOriginal from "./Original/MediumAlert";
import { useRdsThemeMetadataProvider } from "@rapid7/rds-theme-metadata-provider";
const MediumAlert = ({ brand, ...iconProps }) => {
    const { theme: { brand: themeBrand } } = useRdsThemeMetadataProvider();
    const activeBrand = brand || themeBrand || "Callisto";
    return activeBrand === "Callisto" ? _jsx(MediumAlertCallisto, { ...iconProps }) : _jsx(MediumAlertOriginal, { ...iconProps });
};
export { MediumAlert };
