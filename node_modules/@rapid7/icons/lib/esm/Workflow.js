import { jsx as _jsx } from "react/jsx-runtime";
import WorkflowCallisto from "./Callisto/Workflow";
import WorkflowOriginal from "./Original/Workflow";
import { useRdsThemeMetadataProvider } from "@rapid7/rds-theme-metadata-provider";
const Workflow = ({ brand, ...iconProps }) => {
    const { theme: { brand: themeBrand } } = useRdsThemeMetadataProvider();
    const activeBrand = brand || themeBrand || "Callisto";
    return activeBrand === "Callisto" ? _jsx(WorkflowCallisto, { ...iconProps }) : _jsx(WorkflowOriginal, { ...iconProps });
};
export { Workflow };
