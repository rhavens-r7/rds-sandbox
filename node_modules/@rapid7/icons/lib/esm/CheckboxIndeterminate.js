import { jsx as _jsx } from "react/jsx-runtime";
import CheckboxIndeterminateCallisto from "./Callisto/CheckboxIndeterminate";
import CheckboxIndeterminateOriginal from "./Original/CheckboxIndeterminate";
import { useRdsThemeMetadataProvider } from "@rapid7/rds-theme-metadata-provider";
const CheckboxIndeterminate = ({ brand, ...iconProps }) => {
    const { theme: { brand: themeBrand } } = useRdsThemeMetadataProvider();
    const activeBrand = brand || themeBrand || "Callisto";
    return activeBrand === "Callisto" ? _jsx(CheckboxIndeterminateCallisto, { ...iconProps }) : _jsx(CheckboxIndeterminateOriginal, { ...iconProps });
};
export { CheckboxIndeterminate };
