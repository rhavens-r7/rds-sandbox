import { jsx as _jsx } from "react/jsx-runtime";
import TreemapChartCallisto from "./Callisto/TreemapChart";
import TreemapChartOriginal from "./Original/TreemapChart";
import { useRdsThemeMetadataProvider } from "@rapid7/rds-theme-metadata-provider";
const TreemapChart = ({ brand, ...iconProps }) => {
    const { theme: { brand: themeBrand } } = useRdsThemeMetadataProvider();
    const activeBrand = brand || themeBrand || "Callisto";
    return activeBrand === "Callisto" ? _jsx(TreemapChartCallisto, { ...iconProps }) : _jsx(TreemapChartOriginal, { ...iconProps });
};
export { TreemapChart };
