import { jsx as _jsx } from "react/jsx-runtime";
import AIFeedbackPositiveThumbsUpCallisto from "./Callisto/AIFeedbackPositiveThumbsUp";
import AIFeedbackPositiveThumbsUpOriginal from "./Original/AIFeedbackPositiveThumbsUp";
import { useRdsThemeMetadataProvider } from "@rapid7/rds-theme-metadata-provider";
const AIFeedbackPositiveThumbsUp = ({ brand, ...iconProps }) => {
    const { theme: { brand: themeBrand } } = useRdsThemeMetadataProvider();
    const activeBrand = brand || themeBrand || "Callisto";
    return activeBrand === "Callisto" ? _jsx(AIFeedbackPositiveThumbsUpCallisto, { ...iconProps }) : _jsx(AIFeedbackPositiveThumbsUpOriginal, { ...iconProps });
};
export { AIFeedbackPositiveThumbsUp };
