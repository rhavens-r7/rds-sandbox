import { jsx as _jsx } from "react/jsx-runtime";
import LogSignOutCallisto from "./Callisto/LogSignOut";
import LogSignOutOriginal from "./Original/LogSignOut";
import { useRdsThemeMetadataProvider } from "@rapid7/rds-theme-metadata-provider";
const LogSignOut = ({ brand, ...iconProps }) => {
    const { theme: { brand: themeBrand } } = useRdsThemeMetadataProvider();
    const activeBrand = brand || themeBrand || "Callisto";
    return activeBrand === "Callisto" ? _jsx(LogSignOutCallisto, { ...iconProps }) : _jsx(LogSignOutOriginal, { ...iconProps });
};
export { LogSignOut };
