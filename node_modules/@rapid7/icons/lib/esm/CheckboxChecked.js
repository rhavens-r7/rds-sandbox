import { jsx as _jsx } from "react/jsx-runtime";
import CheckboxCheckedCallisto from "./Callisto/CheckboxChecked";
import CheckboxCheckedOriginal from "./Original/CheckboxChecked";
import { useRdsThemeMetadataProvider } from "@rapid7/rds-theme-metadata-provider";
const CheckboxChecked = ({ brand, ...iconProps }) => {
    const { theme: { brand: themeBrand } } = useRdsThemeMetadataProvider();
    const activeBrand = brand || themeBrand || "Callisto";
    return activeBrand === "Callisto" ? _jsx(CheckboxCheckedCallisto, { ...iconProps }) : _jsx(CheckboxCheckedOriginal, { ...iconProps });
};
export { CheckboxChecked };
