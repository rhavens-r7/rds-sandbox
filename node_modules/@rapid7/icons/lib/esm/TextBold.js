import { jsx as _jsx } from "react/jsx-runtime";
import TextBoldCallisto from "./Callisto/TextBold";
import TextBoldOriginal from "./Original/TextBold";
import { useRdsThemeMetadataProvider } from "@rapid7/rds-theme-metadata-provider";
const TextBold = ({ brand, ...iconProps }) => {
    const { theme: { brand: themeBrand } } = useRdsThemeMetadataProvider();
    const activeBrand = brand || themeBrand || "Callisto";
    return activeBrand === "Callisto" ? _jsx(TextBoldCallisto, { ...iconProps }) : _jsx(TextBoldOriginal, { ...iconProps });
};
export { TextBold };
