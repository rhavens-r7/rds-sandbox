import { jsx as _jsx } from "react/jsx-runtime";
import FirewallCallisto from "./Callisto/Firewall";
import FirewallOriginal from "./Original/Firewall";
import { useRdsThemeMetadataProvider } from "@rapid7/rds-theme-metadata-provider";
const Firewall = ({ brand, ...iconProps }) => {
    const { theme: { brand: themeBrand } } = useRdsThemeMetadataProvider();
    const activeBrand = brand || themeBrand || "Callisto";
    return activeBrand === "Callisto" ? _jsx(FirewallCallisto, { ...iconProps }) : _jsx(FirewallOriginal, { ...iconProps });
};
export { Firewall };
