import { jsx as _jsx } from "react/jsx-runtime";
import InventoryCallisto from "./Callisto/Inventory";
import InventoryOriginal from "./Original/Inventory";
import { useRdsThemeMetadataProvider } from "@rapid7/rds-theme-metadata-provider";
const Inventory = ({ brand, ...iconProps }) => {
    const { theme: { brand: themeBrand } } = useRdsThemeMetadataProvider();
    const activeBrand = brand || themeBrand || "Callisto";
    return activeBrand === "Callisto" ? _jsx(InventoryCallisto, { ...iconProps }) : _jsx(InventoryOriginal, { ...iconProps });
};
export { Inventory };
