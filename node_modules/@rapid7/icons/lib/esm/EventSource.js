import { jsx as _jsx } from "react/jsx-runtime";
import EventSourceCallisto from "./Callisto/EventSource";
import EventSourceOriginal from "./Original/EventSource";
import { useRdsThemeMetadataProvider } from "@rapid7/rds-theme-metadata-provider";
const EventSource = ({ brand, ...iconProps }) => {
    const { theme: { brand: themeBrand } } = useRdsThemeMetadataProvider();
    const activeBrand = brand || themeBrand || "Callisto";
    return activeBrand === "Callisto" ? _jsx(EventSourceCallisto, { ...iconProps }) : _jsx(EventSourceOriginal, { ...iconProps });
};
export { EventSource };
