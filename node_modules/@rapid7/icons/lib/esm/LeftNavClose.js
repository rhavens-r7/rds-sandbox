import { jsx as _jsx } from "react/jsx-runtime";
import LeftNavCloseCallisto from "./Callisto/LeftNavClose";
import LeftNavCloseOriginal from "./Original/LeftNavClose";
import { useRdsThemeMetadataProvider } from "@rapid7/rds-theme-metadata-provider";
const LeftNavClose = ({ brand, ...iconProps }) => {
    const { theme: { brand: themeBrand } } = useRdsThemeMetadataProvider();
    const activeBrand = brand || themeBrand || "Callisto";
    return activeBrand === "Callisto" ? _jsx(LeftNavCloseCallisto, { ...iconProps }) : _jsx(LeftNavCloseOriginal, { ...iconProps });
};
export { LeftNavClose };
