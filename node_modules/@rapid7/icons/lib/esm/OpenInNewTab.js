import { jsx as _jsx } from "react/jsx-runtime";
import OpenInNewTabCallisto from "./Callisto/OpenInNewTab";
import OpenInNewTabOriginal from "./Original/OpenInNewTab";
import { useRdsThemeMetadataProvider } from "@rapid7/rds-theme-metadata-provider";
const OpenInNewTab = ({ brand, ...iconProps }) => {
    const { theme: { brand: themeBrand } } = useRdsThemeMetadataProvider();
    const activeBrand = brand || themeBrand || "Callisto";
    return activeBrand === "Callisto" ? _jsx(OpenInNewTabCallisto, { ...iconProps }) : _jsx(OpenInNewTabOriginal, { ...iconProps });
};
export { OpenInNewTab };
