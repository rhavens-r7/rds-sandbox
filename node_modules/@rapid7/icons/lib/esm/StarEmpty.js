import { jsx as _jsx } from "react/jsx-runtime";
import StarEmptyCallisto from "./Callisto/StarEmpty";
import StarEmptyOriginal from "./Original/StarEmpty";
import { useRdsThemeMetadataProvider } from "@rapid7/rds-theme-metadata-provider";
const StarEmpty = ({ brand, ...iconProps }) => {
    const { theme: { brand: themeBrand } } = useRdsThemeMetadataProvider();
    const activeBrand = brand || themeBrand || "Callisto";
    return activeBrand === "Callisto" ? _jsx(StarEmptyCallisto, { ...iconProps }) : _jsx(StarEmptyOriginal, { ...iconProps });
};
export { StarEmpty };
