import { createThemedFormControlLabel } from "../../../muiComponents/formControlLabel/createThemedFormControlLabel";
const overrides = {
    styleOverrides: {
        root: ({ ownerState }) => ({
            ...(ownerState.variant === "outlined" && {
                borderRadius: "8px",
            }),
        }),
    },
};
export const ThemedFormControlLabel = createThemedFormControlLabel(overrides);
