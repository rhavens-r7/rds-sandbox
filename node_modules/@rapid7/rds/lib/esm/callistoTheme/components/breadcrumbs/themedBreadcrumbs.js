export const ThemedBreadcrumbs = {
    defaultProps: {
        separator: "/",
    },
    styleOverrides: {
        li: ({ theme }) => ({
            "& a": {
                display: "inline-block",
                maxWidth: "200px",
                overflow: "hidden",
                textOverflow: "ellipsis",
            },
            "&:last-child": {
                "&::after": {
                    color: theme.palette.text.primary,
                    content: '"/"',
                    marginLeft: theme.spacing(1),
                    marginRight: theme.spacing(1),
                },
            },
            alignItems: "center",
            display: "inline-flex",
            whiteSpace: "nowrap",
        }),
        separator: ({ theme }) => ({
            color: theme.palette.text.primary,
        }),
    },
};
