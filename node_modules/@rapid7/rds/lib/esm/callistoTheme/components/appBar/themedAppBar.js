import "@mui/material/AppBar";
import { ThemedAppBarBase } from "../../../muiComponents/appBar/themedAppBarBase";
export const ThemedAppBar = {
    ...ThemedAppBarBase,
    styleOverrides: {
        root: ({ ownerState, theme }) => ({
            ...(ownerState.color === "navPrimary" && {
                borderBottomStyle: "solid",
                borderBottomWidth: 1,
                borderColor: theme.palette.mode === "light"
                    ? theme.palette.divider
                    : "rgba(73, 78, 81, 0.50)",
            }),
        }),
    },
};
