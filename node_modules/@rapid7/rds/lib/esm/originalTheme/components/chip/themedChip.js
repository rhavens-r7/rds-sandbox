import { jsx as _jsx } from "react/jsx-runtime";
import { Clear } from "@rapid7/icons";
const deleteIconFontSize = {
    medium: "24px",
    nano: "14px",
    small: "18px",
};
const iconFontSize = {
    medium: "24px",
    nano: "14px",
    small: "18px",
};
const iconMarginLeft = {
    medium: "8px",
    nano: "6px",
    small: "6px",
};
export const ThemedChip = {
    defaultProps: {
        deleteIcon: _jsx(Clear, {}),
    },
    styleOverrides: {
        deleteIcon: ({ theme }) => ({
            variants: [
                {
                    style: (props) => ({
                        fontSize: deleteIconFontSize[props.size || "medium"],
                        opacity: 0.7,
                    }),
                },
                {
                    props: ({ color, variant }) => color !== "primary" && color !== "secondary" && variant !== "plg",
                    style: (props) => {
                        const variant = props.variant || "filled";
                        const color = (props.color || "default");
                        return {
                            color: theme.palette.components?.chip[variant][color].deleteIcon,
                        };
                    },
                },
            ],
        }),
        icon: ({ theme }) => ({
            variants: [
                {
                    style: (props) => {
                        const size = props.size || "medium";
                        return {
                            fontSize: iconFontSize[size],
                            marginLeft: iconMarginLeft[size],
                        };
                    },
                },
                {
                    props: ({ color, variant }) => color !== "primary" && color !== "secondary" && variant !== "plg",
                    style: (props) => {
                        const variant = props.variant || "filled";
                        const color = (props.color || "default");
                        return {
                            color: theme.palette.components?.chip[variant][color].icon,
                        };
                    },
                },
            ],
        }),
        label: () => ({
            lineHeight: "18px",
            overflow: "hidden",
        }),
        root: ({ theme }) => ({
            ":focus-visible": {
                outline: `2px solid ${theme.palette.primary.main}`,
                outlineOffset: "2px",
            },
            variants: [
                {
                    props: { size: "nano" },
                    style: {
                        height: "22px",
                    },
                },
                {
                    props: ({ color, variant }) => color !== "primary" && color !== "secondary" && variant !== "plg",
                    style: (props) => {
                        const variant = props.variant || "filled";
                        const color = (props.color || "default");
                        return {
                            "&.MuiChip-clickable:hover": {
                                background: theme.palette.components?.chip[variant][color].hover,
                            },
                            ":focus-visible": {
                                background: theme.palette.components?.chip[variant][color].focus,
                            },
                            background: variant === "outlined"
                                ? "transparent"
                                : theme.palette.components?.chip.filled[color].background,
                            border: variant === "filled"
                                ? "none"
                                : `1px solid ${theme.palette.components?.chip.outlined[color].border}`,
                            color: theme.palette.components?.chip[variant][color].text,
                        };
                    },
                },
                {
                    props: { AIStyled: true },
                    style: () => ({
                        ":before": {
                            WebkitMask: "linear-gradient(#fff 0 0) content-box, linear-gradient(#fff 0 0)",
                            WebkitMaskComposite: "xor",
                            background: "linear-gradient(90deg, rgb(237, 124, 79) 42%, rgb(229, 121, 166), rgb(140, 127, 207), rgb(78, 218, 202))",
                            borderRadius: "16px",
                            content: '""',
                            inset: 0,
                            mask: "linear-gradient(#fff 0 0) content-box, linear-gradient(#fff 0 0)",
                            maskComposite: "exclude",
                            padding: "2px",
                            position: "absolute",
                        },
                        background: "transparent",
                        position: "relative",
                        zIndex: 0,
                    }),
                },
                {
                    props: { variant: "plg" },
                    style: () => ({
                        "& .MuiSvgIcon-root": {
                            alignItems: "center",
                            backgroundColor: theme.palette.components?.chip.plg.iconFill,
                            borderRadius: "100%",
                            height: "16px",
                            marginLeft: "4px",
                            width: "16px",
                        },
                        ":hover": {
                            cursor: "default",
                        },
                        background: `linear-gradient(91deg, ${theme.palette.components?.chip.plg.background} 1.13%,transparent 97.62%)`,
                        border: `1px solid ${theme.palette.components?.chip.plg.border}`,
                    }),
                },
            ],
        }),
    },
};
