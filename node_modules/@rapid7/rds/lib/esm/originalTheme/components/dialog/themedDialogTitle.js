export const ThemedDialogTitle = {
    defaultProps: {
        variant: "h2",
    },
    styleOverrides: {
        root: ({ theme }) => ({
            "& .MuiIconButton-root": {
                position: "absolute",
                right: theme.spacing(1),
                top: theme.spacing(1),
            },
            alignItems: "center",
            display: "flex",
        }),
    },
};
