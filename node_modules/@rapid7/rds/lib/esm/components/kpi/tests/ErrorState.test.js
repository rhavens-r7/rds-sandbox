import { jsx as _jsx } from "react/jsx-runtime";
import { describe, it, expect } from "vitest";
import { screen, render } from "@testing-library/react";
import { KpiErrorState } from "../index";
describe("Kpi Error state", () => {
    it("Should render the expected elements", () => {
        render(_jsx(KpiErrorState, {}));
        expect(screen.getByTestId("ReportProblemOutlinedIcon")).toBeInTheDocument();
        expect(screen.getByText("There was an issue while loading.")).toBeInTheDocument();
    });
});
