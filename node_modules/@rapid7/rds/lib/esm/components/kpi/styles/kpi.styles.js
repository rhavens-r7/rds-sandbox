import { styled as MUIStyled } from "@mui/material/styles";
import { Box } from "@mui/material";
export const KpiIcon = MUIStyled(Box)(({ theme }) => ({
    color: theme.palette.components?.kpi?.iconFill,
}));
