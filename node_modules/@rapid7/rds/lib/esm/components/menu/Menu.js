import { jsx as _jsx } from "react/jsx-runtime";
import { Fade, Grow, Menu } from "@mui/material";
import { useEffect } from "react";
export const RDSMenu = ({ anchorOrigin = { horizontal: "left", vertical: "bottom" }, TransitionComponent = Grow, autoWidth = false, maxHeight = false, ...props }) => {
    const menuSx = Array.isArray(props.sx) ? props.sx : [props.sx];
    useEffect(() => {
        if (TransitionComponent === Fade) {
            console.warn("Warning: Using Fade as the TransitionComponent in RDSMenu is deprecated and will be removed as an option in the next major release. Please use the default (Grow) transition instead.");
        }
    }, [TransitionComponent]);
    return (_jsx(Menu, { anchorOrigin: anchorOrigin, sx: [
            (theme) => ({
                "& .MuiBackdrop-root": {
                    backgroundColor: "transparent",
                },
                "& .MuiMenuItem-root:focus-visible": {
                    paddingLeft: theme.spacing(1.5),
                    paddingRight: theme.spacing(1.5),
                },
                ...(!autoWidth && {
                    "& .MuiPaper-root": {
                        border: `1px solid ${theme.palette.components.menu.border}`,
                        minWidth: 200,
                    },
                }),
                ...(maxHeight
                    ? !autoWidth
                        ? {
                            "& .MuiPaper-root": {
                                border: `1px solid ${theme.palette.components.menu.border}`,
                                maxHeight: 125,
                                minWidth: 200,
                            },
                        }
                        : {
                            "& .MuiPaper-root": {
                                border: `1px solid ${theme.palette.components.menu.border}`,
                            },
                            maxHeight: 125,
                        }
                    : {
                        "& .MuiPaper-root": {
                            border: `1px solid ${theme.palette.components.menu.border}`,
                        },
                    }),
            }),
            ...menuSx,
        ], TransitionComponent: TransitionComponent, ...props, MenuListProps: { autoFocus: true }, children: props.children }));
};
