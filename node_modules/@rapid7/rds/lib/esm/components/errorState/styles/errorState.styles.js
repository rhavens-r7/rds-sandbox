import { styled as MUIStyled } from "@mui/material/styles";
import { Button, Grid } from "@mui/material";
// eslint-disable-next-line @typescript-eslint/no-unused-vars
export const StyledContainer = MUIStyled("div")(({ theme }) => ({
    containerName: "error-state-grid",
    containerType: "normal",
}));
export const StyledGrid = MUIStyled(Grid, {
    name: "Grid",
    slot: "div",
})(() => ({
    "> div:first-of-type": {
        "@container error-state-grid (max-width: 560px)": {
            display: "grid",
            justifyContent: "center",
            marginTop: "0",
        },
    },
    "@container error-state-grid (max-width: 560px)": {
        ".stack-button": {
            justifyContent: "center",
        },
        display: "grid",
        justifyContent: "center",
        textAlign: "center",
    },
    maxWidth: "675px",
    width: "100%",
}));
export const StyledErrorCode = MUIStyled("div")(({ theme }) => ({
    color: theme.palette.text.secondary,
    fontSize: "12px",
}));
export const StyledButtonWrapper = MUIStyled("div")(({ theme }) => ({
    paddingTop: theme.spacing(1.5),
}));
export const StyledButton = MUIStyled(Button) `
  text-transform: none;
`;
export const StyledDescription = MUIStyled("div")(({ theme }) => ({
    margin: theme.spacing(2, 0),
}));
export const StyledIcon = MUIStyled("div")(({ hasErrorIcon }) => ({
    "@container error-state-grid (max-width: 560px)": {
        height: "64px",
        width: "64px",
    },
    alignItems: "center",
    backgroundColor: hasErrorIcon ? "#F5EBED" : "#DEEDF6",
    borderRadius: "100%",
    display: "flex",
    height: "88px",
    justifyContent: "center",
    svg: {
        color: hasErrorIcon ? "#D43132" : "#44768F",
        height: "64px",
        width: "64px",
    },
    width: "88px",
}));
