import { jsx as _jsx, Fragment as _Fragment, jsxs as _jsxs } from "react/jsx-runtime";
import { describe, it, expect } from "vitest";
import { screen, render } from "@testing-library/react";
import { Button, Chip, Typography } from "@mui/material";
import { SectionHeader } from "../SectionHeader";
describe("SectionHeader", () => {
    it("should render with only the required header prop", () => {
        render(_jsx(SectionHeader, { header: "Test Header" }));
        expect(screen.getByText("Test Header")).toBeInTheDocument();
    });
    it("should render with header and statusSlot", () => {
        render(_jsx(SectionHeader, { header: "Test Header", statusSlot: _jsx(Chip, { label: "Active", size: "small" }) }));
        expect(screen.getByText("Test Header")).toBeInTheDocument();
        expect(screen.getByText("Active")).toBeInTheDocument();
    });
    it("should render with header and actionsSlot", () => {
        render(_jsx(SectionHeader, { actionsSlot: _jsx(Button, { children: "Save" }), header: "Test Header" }));
        expect(screen.getByText("Test Header")).toBeInTheDocument();
        expect(screen.getByRole("button", { name: "Save" })).toBeInTheDocument();
    });
    it("should render with header and subheaderSlot", () => {
        render(_jsx(SectionHeader, { header: "Test Header", subheaderSlot: _jsx(Typography, { children: "Subheader text" }) }));
        expect(screen.getByText("Test Header")).toBeInTheDocument();
        expect(screen.getByText("Subheader text")).toBeInTheDocument();
    });
    it("should render with divider when divider prop is true", () => {
        const { container } = render(_jsx(SectionHeader, { divider: true, header: "Test Header" }));
        expect(screen.getByText("Test Header")).toBeInTheDocument();
        const divider = container.querySelector("hr");
        expect(divider).toBeInTheDocument();
    });
    it("should not render divider when divider prop is false", () => {
        const { container } = render(_jsx(SectionHeader, { divider: false, header: "Test Header" }));
        expect(screen.getByText("Test Header")).toBeInTheDocument();
        const divider = container.querySelector("hr");
        expect(divider).not.toBeInTheDocument();
    });
    it("should not render divider by default", () => {
        const { container } = render(_jsx(SectionHeader, { header: "Test Header" }));
        expect(screen.getByText("Test Header")).toBeInTheDocument();
        const divider = container.querySelector("hr");
        expect(divider).not.toBeInTheDocument();
    });
    it("should render with header, statusSlot, and actionsSlot", () => {
        render(_jsx(SectionHeader, { actionsSlot: _jsx(Button, { children: "Cancel" }), header: "Test Header", statusSlot: _jsx(Chip, { label: "Pending", size: "small" }) }));
        expect(screen.getByText("Test Header")).toBeInTheDocument();
        expect(screen.getByText("Pending")).toBeInTheDocument();
        expect(screen.getByRole("button", { name: "Cancel" })).toBeInTheDocument();
    });
    it("should render with header, statusSlot, and subheaderSlot", () => {
        render(_jsx(SectionHeader, { header: "Test Header", statusSlot: _jsx(Chip, { label: "Draft", size: "small" }), subheaderSlot: _jsx(Typography, { children: "Additional info" }) }));
        expect(screen.getByText("Test Header")).toBeInTheDocument();
        expect(screen.getByText("Draft")).toBeInTheDocument();
        expect(screen.getByText("Additional info")).toBeInTheDocument();
    });
    it("should render with header, actionsSlot, and subheaderSlot", () => {
        render(_jsx(SectionHeader, { actionsSlot: _jsx(Button, { children: "Apply" }), header: "Test Header", subheaderSlot: _jsx(Typography, { children: "Description text" }) }));
        expect(screen.getByText("Test Header")).toBeInTheDocument();
        expect(screen.getByRole("button", { name: "Apply" })).toBeInTheDocument();
        expect(screen.getByText("Description text")).toBeInTheDocument();
    });
    it("should render with header, statusSlot, actionsSlot, and subheaderSlot", () => {
        render(_jsx(SectionHeader, { actionsSlot: _jsx(Button, { children: "Submit" }), header: "Test Header", statusSlot: _jsx(Chip, { label: "Info", size: "small" }), subheaderSlot: _jsx(Typography, { children: "Full description" }) }));
        expect(screen.getByText("Test Header")).toBeInTheDocument();
        expect(screen.getByText("Info")).toBeInTheDocument();
        expect(screen.getByRole("button", { name: "Submit" })).toBeInTheDocument();
        expect(screen.getByText("Full description")).toBeInTheDocument();
    });
    it("should render with all props including divider", () => {
        const { container } = render(_jsx(SectionHeader, { actionsSlot: _jsxs(_Fragment, { children: [_jsx(Button, { children: "Cancel" }), _jsx(Button, { children: "Save" })] }), divider: true, header: "Complete Header", statusSlot: _jsx(Chip, { label: "Success", size: "small" }), subheaderSlot: _jsx(Typography, { children: "Complete example with divider" }) }));
        expect(screen.getByText("Complete Header")).toBeInTheDocument();
        expect(screen.getByText("Success")).toBeInTheDocument();
        expect(screen.getByRole("button", { name: "Cancel" })).toBeInTheDocument();
        expect(screen.getByRole("button", { name: "Save" })).toBeInTheDocument();
        expect(screen.getByText("Complete example with divider")).toBeInTheDocument();
        const divider = container.querySelector("hr");
        expect(divider).toBeInTheDocument();
    });
    it("should render multiple action buttons", () => {
        render(_jsx(SectionHeader, { actionsSlot: _jsxs(_Fragment, { children: [_jsx(Button, { children: "Reset" }), _jsx(Button, { children: "Cancel" }), _jsx(Button, { children: "Save" })] }), header: "Test Header" }));
        expect(screen.getByText("Test Header")).toBeInTheDocument();
        expect(screen.getByRole("button", { name: "Reset" })).toBeInTheDocument();
        expect(screen.getByRole("button", { name: "Cancel" })).toBeInTheDocument();
        expect(screen.getByRole("button", { name: "Save" })).toBeInTheDocument();
    });
});
