/* eslint-disable @typescript-eslint/no-unused-vars */
import { styled as MUIStyled } from "@mui/material/styles";
import { Box } from "@mui/material";
import { getValue } from "../../../tokens";
const sizeWidthMap = {
    large: "50%",
    medium: "33.333%",
    small: "25%",
};
export const PanelRoot = MUIStyled(Box, {
    name: "Panel",
    shouldForwardProp: (prop) => prop !== "size" && prop !== "variant" && prop !== "isLoading",
    slot: "root",
})(({ theme, size, variant, isLoading }) => {
    const color = getValue("color", theme.palette.brand, theme.palette.mode);
    const borderGradient = `${color.deepOrange[400]} 25%, ${color.pink[400]} 50%, ${color.deepPurple[400]} 75%, ${color.green[400]} 100%`;
    const background = `radial-gradient(121.29% 63.97% at 95.87% 105.11%, rgba(140, 127, 207, 0.15) 0%, rgba(140, 127, 207, 0.00) 100%), radial-gradient(112.73% 128.25% at -4.31% 3.64%, rgba(237, 124, 79, 0.15) 0%, rgba(237, 124, 79, 0.00) 100%)`;
    return {
        ...(variant === "AI"
            ? {
                ...(isLoading && {
                    "&::before": {
                        WebkitMask: "linear-gradient(#fff 0 0) content-box, linear-gradient(#fff 0 0)",
                        WebkitMaskComposite: "xor",
                        background: `linear-gradient(125deg, ${borderGradient})`,
                        borderRadius: 4,
                        content: '""',
                        inset: 0,
                        mask: "linear-gradient(#fff 0 0) content-box, linear-gradient(#fff 0 0)",
                        maskComposite: "exclude",
                        padding: "1px",
                        pointerEvents: "none",
                        position: "absolute",
                        zIndex: 0,
                    },
                }),
                background,
                border: isLoading ? "none" : `1px solid ${theme.palette.divider}`,
                borderRadius: 4,
            }
            : {
                backgroundColor: theme.palette.background.default,
                borderLeft: `1px solid ${theme.palette.divider}`,
            }),
        display: "flex",
        flexDirection: "column",
        gap: theme.spacing(2),
        height: "100%",
        padding: theme.spacing(2),
        position: "relative",
        width: sizeWidthMap[size],
    };
});
export const PanelScrollableContent = MUIStyled(Box, {
    name: "Panel",
    slot: "scrollableContent",
})(() => ({
    flex: 1,
    overflowY: "auto",
}));
export const PanelContentRoot = MUIStyled(Box, {
    name: "Panel",
    slot: "content",
})(() => ({
    flex: 1,
    overflowY: "auto",
}));
