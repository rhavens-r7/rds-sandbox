import { jsx as _jsx, jsxs as _jsxs } from "react/jsx-runtime";
import React from "react";
import { PanelRoot } from "./styles/panel.styles";
import { Box, Skeleton } from "@mui/material";
import { CircularProgress } from "../circularProgress";
const Loading = () => (_jsxs(Box, { sx: {
        alignItems: "center",
        display: "flex",
        flexDirection: "row",
        gap: 4,
        padding: 1,
    }, children: [_jsx(Box, { sx: {
                flex: 1,
            }, children: _jsx(CircularProgress, { size: 30, variant: "AI" }) }), _jsxs(Box, { sx: {
                display: "flex",
                flexDirection: "column",
                gap: 1,
                height: "100%",
                width: "100%",
            }, children: [_jsx(Skeleton, { animation: "wave", height: 12, sx: {
                        maxWidth: 189,
                        width: "100%",
                    }, variant: "rectangular" }), _jsx(Skeleton, { animation: "wave", height: 12, sx: {
                        maxWidth: 220,
                        width: "100%",
                    }, variant: "rectangular" }), _jsx(Skeleton, { animation: "wave", height: 12, sx: {
                        maxWidth: 175,
                        width: "100%",
                    }, variant: "rectangular" })] })] }));
export const Panel = React.forwardRef(function Panel(props, ref) {
    const { "aria-labelledby": ariaLabelledby, children, open, returnFocusOnClose = true, size = "medium", triggerRef, PanelRootProps, variant, isLoading, } = props;
    const prevOpenRef = React.useRef(open);
    React.useEffect(() => {
        if (prevOpenRef.current &&
            !open &&
            returnFocusOnClose &&
            triggerRef?.current) {
            triggerRef.current.focus();
        }
        prevOpenRef.current = open;
    }, [open, returnFocusOnClose, triggerRef]);
    if (!open) {
        return null;
    }
    return (_jsx(PanelRoot, { "aria-labelledby": ariaLabelledby, isLoading: isLoading, ref: ref, role: "region", size: size, variant: variant, ...PanelRootProps, children: isLoading ? _jsx(Loading, {}) : children }));
});
Panel.displayName = "Panel";
