import { useEffect, useRef } from "react";
/**
 * Behaves the same as {@link useEffect}, except it will not trigger on the first render
 * @param {Function} callback - The side effect to be invoked.
 * @param {React.DependencyList} dependencies - The dependency array that triggers this side effect
 */
const useDidUpdateEffect = (callback, dependencies) => {
    const initial = useRef(true);
    useEffect(() => {
        if (initial.current) {
            initial.current = false;
            return;
        }
        callback();
        // eslint-disable-next-line react-hooks/exhaustive-deps
    }, dependencies);
};
export default useDidUpdateEffect;
