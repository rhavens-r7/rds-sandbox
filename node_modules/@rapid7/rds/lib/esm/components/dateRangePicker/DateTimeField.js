import { jsx as _jsx, jsxs as _jsxs } from "react/jsx-runtime";
import { Box } from "@mui/material";
import { DateField, TimeField } from "@mui/x-date-pickers";
const DateTimeField = ({ labelPrefix, minDate, maxDate, onChange, onChangeTime, timezone, valueDate, valueTime, }) => {
    return (_jsxs(Box, { sx: { display: "flex", gap: 1 }, children: [_jsx(DateField, { "data-testid": `${labelPrefix}-date-input`.toLowerCase(), format: "DD/MM/YYYY", fullWidth: true, label: `${labelPrefix} Date`, maxDate: maxDate, minDate: minDate, onChange: onChange, size: "small", timezone: timezone, value: valueDate }), _jsx(TimeField, { "data-testid": `${labelPrefix}-time-input`.toLowerCase(), disableIgnoringDatePartForTimeValidation: true, format: "HH:mm:ss", label: `${labelPrefix} Time`, maxTime: maxDate, minTime: minDate, onChange: onChangeTime, size: "small", timezone: timezone, value: valueTime })] }));
};
export default DateTimeField;
