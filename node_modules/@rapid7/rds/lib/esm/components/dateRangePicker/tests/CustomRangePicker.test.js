import { jsx as _jsx } from "react/jsx-runtime";
import { describe, it, expect, vi, beforeAll, afterAll } from "vitest";
import { screen, render } from "@testing-library/react";
import { userEvent } from "@testing-library/user-event";
import dayjs from "dayjs";
import CustomRangePicker from "../CustomRangePicker";
import { RDSConfigProvider } from "../../../rdsConfigProvider";
import { RDSThemeProvider } from "../../../rdsThemeProvider";
import { THEME_MODES } from "@rapid7/rds-theme-metadata-provider";
describe("<CustomRangePicker />", () => {
    beforeAll(() => {
        vi.useFakeTimers();
        vi.setSystemTime(dayjs("1 January 2025").startOf("day").toDate());
    });
    afterAll(() => {
        vi.useRealTimers();
    });
    it("preselects the start and end times in the calendar and displays them in text fields", () => {
        // Setup fixture
        const value = [
            dayjs.tz("10 January 2025 23:59:59", "Africa/Johannesburg"),
            dayjs.tz("15 January 2025 01:02:03", "Africa/Johannesburg"),
        ];
        // Exercise SUT
        render(_jsx(RDSThemeProvider, { themeMode: THEME_MODES.LIGHT, children: _jsx(RDSConfigProvider, { children: _jsx(CustomRangePicker, { ianaTimezone: "Africa/Johannesburg", onChange: vi.fn(), onNowClick: undefined, onReset: undefined, onSubmit: vi.fn(), value: value }) }) }));
        // Verify results
        expect(screen.getByText("10")).toHaveAttribute("aria-selected", "true");
        expect(screen.getByDisplayValue("10/01/2025"));
        expect(screen.getByText("15")).toHaveAttribute("aria-selected", "true");
        expect(screen.getByDisplayValue("15/01/2025"));
    });
    it("updates the time range text fields with the date selected in the calendar", async () => {
        // Setup fixture
        const user = userEvent.setup({ advanceTimers: vi.advanceTimersByTime });
        render(_jsx(RDSThemeProvider, { themeMode: THEME_MODES.LIGHT, children: _jsx(RDSConfigProvider, { children: _jsx(CustomRangePicker, { ianaTimezone: "Africa/Johannesburg", onChange: vi.fn(), onNowClick: undefined, onReset: undefined, onSubmit: vi.fn(), value: [null, null] }) }) }));
        // Exercise SUT
        await user.click(screen.getByText("12"));
        await user.click(screen.getByText("24"));
        // Verify results
        expect(screen.getByDisplayValue("12/01/2025"));
        expect(screen.getByDisplayValue("24/01/2025"));
        expect(screen.getByDisplayValue("00:00:00"));
        expect(screen.getByDisplayValue("23:59:59"));
    });
    it("returns the correct time range when the selection is updated through the calendar and 'Ok' is clicked", async () => {
        // Setup fixture
        const onChangeSpy = vi.fn();
        const onSubmitSpy = vi.fn();
        const user = userEvent.setup({ advanceTimers: vi.advanceTimersByTime });
        render(_jsx(RDSThemeProvider, { themeMode: THEME_MODES.LIGHT, children: _jsx(RDSConfigProvider, { children: _jsx(CustomRangePicker, { ianaTimezone: "America/Havana", onChange: onChangeSpy, onNowClick: undefined, onReset: undefined, onSubmit: onSubmitSpy, value: [null, null] }) }) }));
        // Exercise SUT
        await user.click(screen.getByText("1"));
        await user.click(screen.getByText("31"));
        await user.click(screen.getByText("Ok"));
        // Verify results
        expect(onChangeSpy).toHaveBeenCalledWith([
            expect.any(dayjs),
            expect.any(dayjs),
        ]);
        expect(onSubmitSpy).toHaveBeenCalledTimes(1);
        const callArgs = onChangeSpy.mock.lastCall?.[0];
        // timezone America/Havana results in December calendar being displayed as the default
        expect(callArgs[0].format()).toBe(dayjs.tz("01 December 2024 00:00:00.00", "America/Havana").format());
        expect(callArgs[1].format()).toBe(dayjs.tz("31 December 2024 23:59:59.00", "America/Havana").format());
    });
    it("returns the correct time range when the selection is updated through the text fields and 'Ok' is clicked", async () => {
        // Setup fixture
        const onChangeSpy = vi.fn();
        const onSubmitSpy = vi.fn();
        const user = userEvent.setup({ advanceTimers: vi.advanceTimersByTime });
        render(_jsx(RDSThemeProvider, { themeMode: THEME_MODES.LIGHT, children: _jsx(RDSConfigProvider, { children: _jsx(CustomRangePicker, { ianaTimezone: "Asia/Jakarta", onChange: onChangeSpy, onNowClick: undefined, onReset: undefined, onSubmit: onSubmitSpy, value: [null, null] }) }) }));
        // Exercise SUT
        await user.type(screen.getByTestId("start-date-input").querySelector("input"), "12/10/2024");
        await user.type(screen.getByTestId("start-time-input").querySelector("input"), "07:18:00");
        await user.type(screen.getByTestId("end-date-input").querySelector("input"), "24/10/2024");
        await user.type(screen.getByTestId("end-time-input").querySelector("input"), "08:20:00");
        await user.click(screen.getByText("Ok"));
        // Verify results
        expect(onChangeSpy).toHaveBeenCalledWith([
            expect.any(dayjs),
            expect.any(dayjs),
        ]);
        expect(onSubmitSpy).toHaveBeenCalledTimes(1);
        const callArgs = onChangeSpy.mock.lastCall?.[0];
        expect(callArgs[0].format()).toBe(dayjs.tz("12 October 2024 07:18:00", "Asia/Jakarta").format());
        expect(callArgs[1].format()).toBe(dayjs.tz("24 October 2024 08:20:00", "Asia/Jakarta").format());
    });
    it("invokes the provided handler when reset is clicked", async () => {
        // Setup fixture
        const onResetSpy = vi.fn();
        const user = userEvent.setup({ advanceTimers: vi.advanceTimersByTime });
        render(_jsx(RDSThemeProvider, { themeMode: THEME_MODES.LIGHT, children: _jsx(RDSConfigProvider, { children: _jsx(CustomRangePicker, { ianaTimezone: "UTC", onChange: vi.fn(), onNowClick: undefined, onReset: onResetSpy, onSubmit: vi.fn(), value: [
                        dayjs.tz("10 January 2025 00:00:00", "America/Havana"),
                        dayjs.tz("15 January 2025 00:00:00", "America/Havana"),
                    ] }) }) }));
        // update the initial selection
        await user.click(screen.getByText("12"));
        await user.click(screen.getByText("24"));
        // Exercise SUT
        await user.click(screen.getByText("Reset"));
        // Verify behaviour
        expect(onResetSpy).toHaveBeenCalledTimes(1);
    });
    it("preselects start and end times that fall within the provided minimum and maximum dates when a date range is selected", async () => {
        // Setup fixture
        const onChangeSpy = vi.fn();
        const onSubmitSpy = vi.fn();
        const user = userEvent.setup({ advanceTimers: vi.advanceTimersByTime });
        render(_jsx(RDSThemeProvider, { themeMode: THEME_MODES.LIGHT, children: _jsx(RDSConfigProvider, { children: _jsx(CustomRangePicker, { ianaTimezone: "Europe/Dublin", maxDate: dayjs.tz("20 January 2025 12:34:56", "Europe/Dublin"), minDate: dayjs.tz("5 January 2025 06:22:49", "Europe/Dublin"), onChange: onChangeSpy, onNowClick: undefined, onReset: undefined, onSubmit: onSubmitSpy, value: [null, null] }) }) }));
        // Exercise SUT
        await user.click(screen.getByText("5"));
        await user.click(screen.getByText("20"));
        await user.click(screen.getByText("Ok"));
        // Verify results
        const callArgs = onChangeSpy.mock.lastCall?.[0];
        expect(callArgs[0].format()).toBe(dayjs.tz("5 January 2025 06:22:49", "Europe/Dublin").format());
        expect(callArgs[1].format()).toBe(dayjs.tz("20 January 2025 12:34:56", "Europe/Dublin").format());
        expect(onSubmitSpy).toHaveBeenCalledTimes(1);
    });
    it("does not leave an invalid time selection when the time range is changed to dates on the boundry and no existing selection exists", async () => {
        // Setup fixture
        const user = userEvent.setup({ advanceTimers: vi.advanceTimersByTime });
        render(_jsx(RDSThemeProvider, { themeMode: THEME_MODES.LIGHT, children: _jsx(RDSConfigProvider, { children: _jsx(CustomRangePicker, { ianaTimezone: "UTC", maxDate: dayjs.utc("10 January 2025 17:00:00"), minDate: dayjs.utc("1 January 2025 08:00:00"), onChange: vi.fn(), onNowClick: undefined, onReset: undefined, onSubmit: vi.fn(), value: [null, null] }) }) }));
        // chose an arbitrary date range within the boundary defaulting to start and end of day
        await user.click(screen.getByText("4"));
        await user.click(screen.getByText("8"));
        // Exercise SUT by choosing a date on the boundary
        await user.click(screen.getByText("1"));
        await user.click(screen.getByText("10"));
        // Verify results
        expect(screen.getByDisplayValue("01/01/2025"));
        expect(screen.getByDisplayValue("10/01/2025"));
        expect(screen.getByDisplayValue("08:00:00"));
        expect(screen.getByDisplayValue("17:00:00"));
    });
    it("only displays the 'Now' action after the start date has been selected", async () => {
        // Setup fixture
        const user = userEvent.setup({ advanceTimers: vi.advanceTimersByTime });
        render(_jsx(RDSThemeProvider, { themeMode: THEME_MODES.LIGHT, children: _jsx(RDSConfigProvider, { children: _jsx(CustomRangePicker, { ianaTimezone: "UTC", onChange: vi.fn(), onNowClick: vi.fn(), onReset: undefined, onSubmit: vi.fn(), value: [null, null] }) }) }));
        expect(screen.queryByText("Now")).not.toBeInTheDocument();
        // Exercise SUT
        await user.click(screen.getByText("12"));
        // Verify results
        expect(screen.queryByText("Now")).toBeInTheDocument();
    });
});
