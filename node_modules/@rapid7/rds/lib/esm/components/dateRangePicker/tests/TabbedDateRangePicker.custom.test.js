import { jsx as _jsx } from "react/jsx-runtime";
import { describe, it, expect, vi, beforeAll, afterAll } from "vitest";
import { screen, render } from "@testing-library/react";
import { userEvent } from "@testing-library/user-event";
import dayjs from "dayjs";
import TabbedDateRangePicker from "../TabbedDateRangePicker";
import { RDSConfigProvider } from "../../../rdsConfigProvider";
import { RDSThemeProvider, THEME_MODES } from "../../../rdsThemeProvider";
describe("<TabbedDateRangePicker /> custom date tab", () => {
    const accessPointPlaceholder = "DD/MM/YYYY hh:mm:ss – DD/MM/YYYY hh:mm:ss";
    beforeAll(() => {
        vi.useFakeTimers();
        vi.setSystemTime(dayjs("1 January 2025").startOf("day").toDate());
    });
    afterAll(() => {
        vi.useRealTimers();
    });
    it("hides the reset and now action, and uses 'UTC' timezone by default", async () => {
        // Setup fixture
        const user = userEvent.setup({ advanceTimers: vi.advanceTimersByTime });
        render(_jsx(RDSThemeProvider, { themeMode: THEME_MODES.LIGHT, children: _jsx(RDSConfigProvider, { children: _jsx(TabbedDateRangePicker, { onChange: vi.fn(), tabs: ["custom"], timezone: undefined, value: undefined }) }) }));
        // Exercise SUT
        await user.click(screen.getByPlaceholderText(accessPointPlaceholder));
        // Verify results
        expect(screen.queryByText("Reset")).not.toBeInTheDocument();
        expect(screen.queryByText("Now")).not.toBeInTheDocument();
        expect(screen.getByText("UTC+00:00"));
    });
    it("shows the reset action when enabled", async () => {
        // Setup fixture
        const user = userEvent.setup({ advanceTimers: vi.advanceTimersByTime });
        render(_jsx(RDSThemeProvider, { themeMode: THEME_MODES.LIGHT, children: _jsx(RDSConfigProvider, { children: _jsx(TabbedDateRangePicker, { onChange: vi.fn(), slots: { actions: ["reset"] }, tabs: ["custom"], timezone: undefined, value: undefined }) }) }));
        // Exercise SUT
        await user.click(screen.getByPlaceholderText(accessPointPlaceholder));
        // Verify results
        expect(screen.getByText("Reset"));
    });
    it("shows the now action when enabled", async () => {
        // Setup fixture
        const user = userEvent.setup({ advanceTimers: vi.advanceTimersByTime });
        render(_jsx(RDSThemeProvider, { themeMode: THEME_MODES.LIGHT, children: _jsx(RDSConfigProvider, { children: _jsx(TabbedDateRangePicker, { onChange: vi.fn(), slots: { actions: ["now"] }, tabs: ["custom"], timezone: undefined, value: undefined }) }) }));
        // Exercise SUT
        await user.click(screen.getByPlaceholderText(accessPointPlaceholder));
        await user.click(screen.getByText("12"));
        // Verify results
        expect(screen.getByText("Now"));
    });
    it("reverts the selection back to the initial value when reset is clicked", async () => {
        // Setup fixture
        const user = userEvent.setup({ advanceTimers: vi.advanceTimersByTime });
        render(_jsx(RDSThemeProvider, { themeMode: THEME_MODES.LIGHT, children: _jsx(RDSConfigProvider, { children: _jsx(TabbedDateRangePicker, { onChange: vi.fn(), slots: { actions: ["reset"] }, tabs: ["custom"], timezone: undefined, value: {
                        from: dayjs
                            .tz("10 January 2025 00:00:00", "America/Havana")
                            .valueOf(),
                        to: dayjs
                            .tz("15 January 2025 00:00:00", "America/Havana")
                            .valueOf(),
                    } }) }) }));
        // update the initial selection
        await user.click(screen.getByPlaceholderText(accessPointPlaceholder));
        await user.click(screen.getByText("12"));
        await user.click(screen.getByText("24"));
        // Exercise SUT
        await user.click(screen.getByText("Reset"));
        // Verify results
        expect(screen.getByText("10")).toHaveAttribute("aria-selected", "true");
        expect(screen.getByDisplayValue("10/01/2025"));
        expect(screen.getByText("15")).toHaveAttribute("aria-selected", "true");
        expect(screen.getByDisplayValue("15/01/2025"));
        expect(screen.getByDisplayValue("10/01/2025 05:00:00 – 15/01/2025 05:00:00")).toBeInTheDocument();
    });
    it("clears the selection when reset is clicked and there is no initial value", async () => {
        // Setup fixture
        const user = userEvent.setup({ advanceTimers: vi.advanceTimersByTime });
        render(_jsx(RDSThemeProvider, { themeMode: THEME_MODES.LIGHT, children: _jsx(RDSConfigProvider, { children: _jsx(TabbedDateRangePicker, { onChange: vi.fn(), slots: { actions: ["reset"] }, tabs: ["custom"], timezone: undefined, value: undefined }) }) }));
        // update the initial selection
        await user.click(screen.getByPlaceholderText(accessPointPlaceholder));
        await user.click(screen.getByText("12"));
        await user.click(screen.getByText("24"));
        // Exercise SUT
        await user.click(screen.getByText("Reset"));
        // Verify results
        expect(document.querySelectorAll('[aria-selected="true"]')).toHaveLength(0);
        expect(screen.getByTestId("start-date-input").querySelector("input")?.value).toBe("");
        expect(screen.getByTestId("end-date-input").querySelector("input")?.value).toBe("");
    });
    it("prevents submission of a date range that falls before the provided minimum date", async () => {
        // Setup fixture
        const user = userEvent.setup({ advanceTimers: vi.advanceTimersByTime });
        render(_jsx(RDSThemeProvider, { themeMode: THEME_MODES.LIGHT, children: _jsx(RDSConfigProvider, { children: _jsx(TabbedDateRangePicker, { minDate: dayjs.tz("10 January 2025", "Europe/Dublin").valueOf(), onChange: vi.fn(), tabs: ["custom"], timezone: "Europe/Dublin", value: undefined }) }) }));
        // Exercise SUT
        await user.click(screen.getByPlaceholderText(accessPointPlaceholder));
        await user.click(screen.getByTestId("start-date-input").querySelector("input"));
        await user.paste("09/01/2025");
        await user.click(screen.getByTestId("end-date-input").querySelector("input"));
        await user.paste("11/01/2025");
        // Verify results
        expect(screen.getByText("Ok").getAttribute("disabled")).not.toBeNull();
    });
    it("prevents submission of a date range that falls before the provided minimum date provided through the 'now' keyword", async () => {
        // Setup fixture
        const user = userEvent.setup({ advanceTimers: vi.advanceTimersByTime });
        const fakeNow = dayjs("12 January 2025");
        vi.setSystemTime(fakeNow.toDate());
        render(_jsx(RDSThemeProvider, { themeMode: THEME_MODES.LIGHT, children: _jsx(RDSConfigProvider, { children: _jsx(TabbedDateRangePicker, { minDate: "now", onChange: vi.fn(), tabs: ["custom"], timezone: "Europe/Dublin", value: undefined }) }) }));
        // Exercise SUT
        await user.click(screen.getByPlaceholderText(accessPointPlaceholder));
        await user.click(screen.getByTestId("start-date-input").querySelector("input"));
        await user.paste("10/01/2025");
        await user.click(screen.getByTestId("end-date-input").querySelector("input"));
        await user.paste("21/01/2025");
        // Verify results
        expect(screen.getByText("Ok").getAttribute("disabled")).not.toBeNull();
        expect(screen.getAllByText("11")[0].getAttribute("disabled")).not.toBeNull();
    });
    it("prevents submission of a date range that falls after the provided maximum date", async () => {
        // Setup fixture
        const user = userEvent.setup({ advanceTimers: vi.advanceTimersByTime });
        render(_jsx(RDSThemeProvider, { themeMode: THEME_MODES.LIGHT, children: _jsx(RDSConfigProvider, { children: _jsx(TabbedDateRangePicker, { maxDate: dayjs.tz("10 January 2025", "Europe/Dublin").valueOf(), onChange: vi.fn(), tabs: ["custom"], timezone: "Europe/Dublin", value: undefined }) }) }));
        // Exercise SUT
        await user.click(screen.getByPlaceholderText(accessPointPlaceholder));
        await user.click(screen.getByTestId("start-date-input").querySelector("input"));
        await user.paste("11/01/2025");
        await user.click(screen.getByTestId("end-date-input").querySelector("input"));
        await user.paste("12/01/2025");
        // Verify results
        expect(screen.getByText("Ok").getAttribute("disabled")).not.toBeNull();
    });
    it("prevents submission of a date range that falls after the provided maximum date provided through the 'now' keyword", async () => {
        // Setup fixture
        const user = userEvent.setup({ advanceTimers: vi.advanceTimersByTime });
        const fakeNow = dayjs("12 January 2025");
        vi.setSystemTime(fakeNow.toDate());
        render(_jsx(RDSThemeProvider, { themeMode: THEME_MODES.LIGHT, children: _jsx(RDSConfigProvider, { children: _jsx(TabbedDateRangePicker, { maxDate: "now", onChange: vi.fn(), tabs: ["custom"], timezone: "Europe/Dublin", value: undefined }) }) }));
        // Exercise SUT
        await user.click(screen.getByPlaceholderText(accessPointPlaceholder));
        await user.click(screen.getByTestId("start-date-input").querySelector("input"));
        await user.paste("09/01/2025");
        await user.click(screen.getByTestId("end-date-input").querySelector("input"));
        await user.paste("15/01/2025");
        // Verify results
        expect(screen.getByText("Ok").getAttribute("disabled")).not.toBeNull();
        expect(screen.getAllByText("13")[0].getAttribute("disabled")).not.toBeNull();
    });
    it("prevents submission of a date range that has the same start and end dates", async () => {
        // Setup fixture
        const user = userEvent.setup({ advanceTimers: vi.advanceTimersByTime });
        render(_jsx(RDSThemeProvider, { themeMode: THEME_MODES.LIGHT, children: _jsx(RDSConfigProvider, { children: _jsx(TabbedDateRangePicker, { onChange: vi.fn(), tabs: ["custom"], timezone: "Europe/Dublin", value: undefined }) }) }));
        // Exercise SUT
        await user.click(screen.getByPlaceholderText(accessPointPlaceholder));
        await user.click(screen.getByTestId("start-date-input").querySelector("input"));
        await user.paste("11/01/2025");
        await user.type(screen.getByTestId("start-time-input").querySelector("input"), "00:20:00");
        await user.click(screen.getByTestId("end-date-input").querySelector("input"));
        await user.paste("11/01/2025");
        await user.type(screen.getByTestId("end-time-input").querySelector("input"), "00:20:00");
        // Verify results
        expect(screen.getByText("Ok").getAttribute("disabled")).not.toBeNull();
    });
    it("prevents submission of a date range that is smaller than the allowed minimum range", async () => {
        // Setup fixture
        const user = userEvent.setup({ advanceTimers: vi.advanceTimersByTime });
        render(_jsx(RDSThemeProvider, { themeMode: THEME_MODES.LIGHT, children: _jsx(RDSConfigProvider, { children: _jsx(TabbedDateRangePicker, { minCustomRange: { number: 3, unit: "day" }, onChange: vi.fn(), tabs: ["custom"], timezone: "Europe/Dublin", value: undefined }) }) }));
        // Exercise SUT
        await user.click(screen.getByPlaceholderText(accessPointPlaceholder));
        await user.click(screen.getByText("10"));
        await user.click(screen.getByText("12"));
        // Verify results
        expect(screen.getByText("Ok").getAttribute("disabled")).not.toBeNull();
        expect(screen.getByText("Date range must be greater than 3 days")).toBeInTheDocument();
    });
    it("prevents submission of a date range that is larger than the allowed maximum range", async () => {
        // Setup fixture
        const user = userEvent.setup({ advanceTimers: vi.advanceTimersByTime });
        render(_jsx(RDSThemeProvider, { themeMode: THEME_MODES.LIGHT, children: _jsx(RDSConfigProvider, { children: _jsx(TabbedDateRangePicker, { maxCustomRange: { number: 3, unit: "day" }, onChange: vi.fn(), tabs: ["custom"], timezone: "Europe/Dublin", value: undefined }) }) }));
        // Exercise SUT
        await user.click(screen.getByPlaceholderText(accessPointPlaceholder));
        await user.click(screen.getByText("10"));
        await user.click(screen.getByText("20"));
        // Verify results
        expect(screen.getByText("Ok").getAttribute("disabled")).not.toBeNull();
        expect(screen.getByText("Date range must be less than 3 days")).toBeInTheDocument();
    });
    it("preselects start and end times that fall within the provided minimum and maximum dates when a date range is selected", async () => {
        // Setup fixture
        const onChangeSpy = vi.fn();
        const user = userEvent.setup({ advanceTimers: vi.advanceTimersByTime });
        render(_jsx(RDSConfigProvider, { children: _jsx(TabbedDateRangePicker, { maxDate: dayjs
                    .tz("20 January 2025 12:34:56", "Europe/Dublin")
                    .valueOf(), minDate: dayjs
                    .tz("5 January 2025 06:22:49", "Europe/Dublin")
                    .valueOf(), onChange: onChangeSpy, tabs: ["custom"], timezone: "Europe/Dublin", value: undefined }) }));
        // Exercise SUT
        await user.click(screen.getByPlaceholderText(accessPointPlaceholder));
        await user.click(screen.getByText("5"));
        await user.click(screen.getByText("20"));
        await user.click(screen.getByText("Ok"));
        // Verify results
        expect(onChangeSpy).toHaveBeenCalledOnce();
        expect(onChangeSpy).toHaveBeenCalledWith({
            from: dayjs.tz("5 January 2025 06:22:49", "Europe/Dublin").valueOf(),
            to: dayjs.tz("20 January 2025 12:34:56", "Europe/Dublin").valueOf(),
        });
    });
    it("does not leave an invalid time selection when the time range is changed to dates on the boundry", async () => {
        // Setup fixture
        const user = userEvent.setup({ advanceTimers: vi.advanceTimersByTime });
        render(_jsx(RDSConfigProvider, { children: _jsx(TabbedDateRangePicker, { maxDate: dayjs.utc("10 January 2025 17:00:00").valueOf(), minDate: dayjs.utc("1 January 2025 08:00:00").valueOf(), onChange: vi.fn(), tabs: ["custom"], value: undefined }) }));
        // chose an arbitrary date range within the boundary defaulting to start and end of day
        await user.click(screen.getByPlaceholderText(accessPointPlaceholder));
        await user.click(screen.getByText("4"));
        await user.click(screen.getByText("8"));
        // Exercise SUT by choosing a date on the boundary
        await user.click(screen.getByText("1"));
        await user.click(screen.getByText("10"));
        // Verify results
        expect(screen.getByDisplayValue("01/01/2025"));
        expect(screen.getByDisplayValue("10/01/2025"));
        expect(screen.getByDisplayValue("08:00:00"));
        expect(screen.getByDisplayValue("17:00:00"));
    });
    it("allows the start and end time to be set to a valid times that fall within the provided boundary", async () => {
        // Setup fixture
        const user = userEvent.setup({ advanceTimers: vi.advanceTimersByTime });
        const minDate = dayjs.utc("1 January 2025 08:00:00");
        const maxDate = dayjs.utc("10 January 2025 17:00:00");
        render(_jsx(RDSConfigProvider, { children: _jsx(TabbedDateRangePicker, { maxDate: maxDate.valueOf(), minDate: minDate.valueOf(), onChange: vi.fn(), tabs: ["custom"], timezone: "UTC", value: { from: minDate.valueOf(), to: maxDate.valueOf() } }) }));
        await user.click(screen.getByPlaceholderText(accessPointPlaceholder));
        // Exercise SUT by choosing a time range within the boundary
        await user.click(screen.getByTestId("start-time-input").querySelector("input"));
        await user.paste("09:34:00");
        await user.click(screen.getByTestId("end-time-input").querySelector("input"));
        await user.paste("16:12:00");
        // Verify results
        expect(screen.getByDisplayValue("01/01/2025"));
        expect(screen.getByDisplayValue("09:34:00"));
        expect(screen.getByDisplayValue("10/01/2025"));
        expect(screen.getByDisplayValue("16:12:00"));
    });
    it("reverts the selection back to the initial value when reset is clicked", async () => {
        // Setup fixture
        const user = userEvent.setup({ advanceTimers: vi.advanceTimersByTime });
        render(_jsx(RDSThemeProvider, { themeMode: THEME_MODES.LIGHT, children: _jsx(RDSConfigProvider, { children: _jsx(TabbedDateRangePicker, { onChange: vi.fn(), slots: { actions: ["reset"] }, tabs: ["custom"], timezone: "Europe/Dublin", value: {
                        from: dayjs
                            .tz("10 January 2025 00:00:00", "America/Havana")
                            .valueOf(),
                        to: dayjs
                            .tz("15 January 2025 00:00:00", "America/Havana")
                            .valueOf(),
                    } }) }) }));
        // update the initial selection
        await user.click(screen.getByPlaceholderText(accessPointPlaceholder));
        await user.click(screen.getByText("12"));
        await user.click(screen.getByText("24"));
        // Exercise SUT
        await user.click(screen.getByText("Reset"));
        // Verify results
        expect(screen.getByText("10")).toHaveAttribute("aria-selected", "true");
        expect(screen.getByDisplayValue("10/01/2025"));
        expect(screen.getByText("15")).toHaveAttribute("aria-selected", "true");
        expect(screen.getByDisplayValue("15/01/2025"));
    });
    it("clears the selection when reset is clicked and there is no initial value", async () => {
        // Setup fixture
        const user = userEvent.setup({ advanceTimers: vi.advanceTimersByTime });
        render(_jsx(RDSThemeProvider, { themeMode: THEME_MODES.LIGHT, children: _jsx(RDSConfigProvider, { children: _jsx(TabbedDateRangePicker, { onChange: vi.fn(), slots: { actions: ["reset"] }, tabs: ["custom"], value: undefined }) }) }));
        // update the initial selection
        await user.click(screen.getByPlaceholderText(accessPointPlaceholder));
        await user.click(screen.getByText("12"));
        await user.click(screen.getByText("24"));
        // Exercise SUT
        await user.click(screen.getByText("Reset"));
        // Verify results
        expect(document.querySelectorAll('[aria-selected="true"]')).toHaveLength(0);
        expect(screen.getByTestId("start-date-input").querySelector("input")?.value).toBe("");
        expect(screen.getByTestId("end-date-input").querySelector("input")?.value).toBe("");
    });
});
