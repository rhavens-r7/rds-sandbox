import { jsx as _jsx } from "react/jsx-runtime";
import { describe, it, expect, vi, beforeAll, afterAll } from "vitest";
import { screen, render } from "@testing-library/react";
import { userEvent } from "@testing-library/user-event";
import dayjs from "dayjs";
import TabbedDateRangePicker from "../TabbedDateRangePicker";
import { RDSConfigProvider } from "../../../rdsConfigProvider";
import { RDSThemeProvider } from "../../../rdsThemeProvider";
import { THEME_MODES } from "@rapid7/rds-theme-metadata-provider";
describe("<TabbedDateRangePicker />", () => {
    const presets = [
        {
            label: "Last 20 minutes",
            onParse: (now) => ({
                from: now.subtract(20, "minute").valueOf(),
                timeFrame: "Last 20 minutes",
                to: now.valueOf(),
            }),
        },
        {
            label: "Last 1 hour",
            onParse: (now) => ({
                from: now.subtract(1, "hour").valueOf(),
                timeFrame: "Last 1 hour",
                to: now.valueOf(),
            }),
        },
        {
            label: "Yesterday",
            onParse: (now) => ({
                from: now.startOf("day").subtract(1, "day").valueOf(),
                timeFrame: "Yesterday",
                to: now.startOf("day").subtract(1, "day").endOf("day").valueOf(),
            }),
        },
        {
            label: "Today",
            onParse: (now) => ({
                from: now.startOf("day").valueOf(),
                timeFrame: "Today",
                to: now.valueOf(),
            }),
        },
        {
            label: "Last 1 week",
            onParse: (now) => ({
                from: now.startOf("day").subtract(1, "week").valueOf(),
                timeFrame: "Last 1 week",
                to: now.valueOf(),
            }),
        },
        {
            label: "Last 30 days",
            onParse: (now) => ({
                from: now.subtract(30, "day").valueOf(),
                timeFrame: "Last 30 days",
                to: now.valueOf(),
            }),
        },
    ];
    beforeAll(() => {
        vi.useFakeTimers();
        vi.setSystemTime(dayjs("1 January 2025").startOf("day").toDate());
    });
    afterAll(() => {
        vi.useRealTimers();
    });
    it("defaults to UTC when an invalid timezone is provided", async () => {
        // Setup fixture
        const user = userEvent.setup({ advanceTimers: vi.advanceTimersByTime });
        render(_jsx(RDSThemeProvider, { themeMode: THEME_MODES.LIGHT, children: _jsx(RDSConfigProvider, { children: _jsx(TabbedDateRangePicker, { onChange: vi.fn(), tabs: ["custom"], timezone: "Bad/Ting", value: undefined }) }) }));
        // Exercise SUT
        await user.click(screen.getByPlaceholderText("DD/MM/YYYY hh:mm:ss – DD/MM/YYYY hh:mm:ss"));
        // Verify results
        expect(screen.getByText("UTC+00:00"));
    });
    it("shows the provided timezone in the correct format on the custom tab when one is provided", async () => {
        // Setup fixture
        const user = userEvent.setup({ advanceTimers: vi.advanceTimersByTime });
        render(_jsx(RDSThemeProvider, { themeMode: THEME_MODES.LIGHT, children: _jsx(RDSConfigProvider, { children: _jsx(TabbedDateRangePicker, { onChange: vi.fn(), tabs: ["preset"], timezone: "America/Havana", value: undefined }) }) }));
        // Exercise SUT
        await user.click(screen.getByPlaceholderText("Date Range"));
        // Verify results
        expect(screen.getByText("UTC-05:00"));
        expect(screen.getByLabelText("Current time zone configured in Administration > User Management > Users > Edit User Details"));
    });
    it("shows the provided timezone in the correct format on the preset tab when one is provided", async () => {
        // Setup fixture
        const user = userEvent.setup({ advanceTimers: vi.advanceTimersByTime });
        render(_jsx(RDSThemeProvider, { themeMode: THEME_MODES.LIGHT, children: _jsx(RDSConfigProvider, { children: _jsx(TabbedDateRangePicker, { onChange: vi.fn(), presets: [{ presets }], tabs: ["preset"], timezone: "America/Havana", value: undefined }) }) }));
        // Exercise SUT
        await user.click(screen.getByPlaceholderText("Date Range"));
        // Verify results
        expect(screen.getByText("UTC-05:00"));
        expect(screen.getByLabelText("Current time zone configured in Administration > User Management > Users > Edit User Details"));
    });
    it("overrides the tooltip title when one is provided", async () => {
        // Setup fixture
        const user = userEvent.setup({ advanceTimers: vi.advanceTimersByTime });
        render(_jsx(RDSThemeProvider, { themeMode: THEME_MODES.LIGHT, children: _jsx(RDSConfigProvider, { children: _jsx(TabbedDateRangePicker, { onChange: vi.fn(), slots: { timezoneTooltipTitle: "Some custom tooltip" }, tabs: ["custom"], timezone: "America/Havana", value: undefined }) }) }));
        // Exercise SUT
        await user.click(screen.getByPlaceholderText("DD/MM/YYYY hh:mm:ss – DD/MM/YYYY hh:mm:ss"));
        // Verify results
        expect(screen.getByLabelText("Some custom tooltip"));
    });
    it("does not show the default tooltip if it's been explicitly set to an empty string", async () => {
        // Setup fixture
        const user = userEvent.setup({ advanceTimers: vi.advanceTimersByTime });
        render(_jsx(RDSThemeProvider, { themeMode: THEME_MODES.LIGHT, children: _jsx(RDSConfigProvider, { children: _jsx(TabbedDateRangePicker, { onChange: vi.fn(), slots: { timezoneTooltipTitle: "" }, tabs: ["custom"], timezone: "America/Havana", value: undefined }) }) }));
        // Exercise SUT
        await user.click(screen.getByPlaceholderText("DD/MM/YYYY hh:mm:ss – DD/MM/YYYY hh:mm:ss"));
        // Verify results
        expect(screen.queryByLabelText("Current time zone configured in Administration > User Management > Users > Edit User Details")).not.toBeInTheDocument();
    });
    it("opens the date picker dropdown when the input is focussed having a custom date range value", async () => {
        // Setup fixture
        const user = userEvent.setup({ advanceTimers: vi.advanceTimersByTime });
        render(_jsx(RDSThemeProvider, { themeMode: THEME_MODES.LIGHT, children: _jsx(RDSConfigProvider, { children: _jsx(TabbedDateRangePicker, { onChange: vi.fn(), timezone: "America/Havana", value: {
                        from: dayjs("2024-12-12T00:00:00").valueOf(),
                        to: dayjs("2024-12-24T23:59:59").valueOf(),
                    } }) }) }));
        // Exercise SUT
        await user.click(screen.getByPlaceholderText("DD/MM/YYYY hh:mm:ss – DD/MM/YYYY hh:mm:ss"));
        // Verify results
        expect(screen.getByText("Custom Date").getAttribute("aria-selected")).toEqual("true");
        expect(document.querySelector(".MuiPopper-root")).toBeInTheDocument();
    });
    it("opens the date picker dropdown when the input is focussed having a time frame value", async () => {
        // Setup fixture
        const user = userEvent.setup({ advanceTimers: vi.advanceTimersByTime });
        render(_jsx(RDSThemeProvider, { themeMode: THEME_MODES.LIGHT, children: _jsx(RDSConfigProvider, { children: _jsx(TabbedDateRangePicker, { onChange: vi.fn(), presets: [{ presets }], timezone: "America/Havana", value: { timeFrame: "Last 1 hour" } }) }) }));
        // Exercise SUT
        await user.click(screen.getByPlaceholderText("Date Range"));
        // Verify results
        expect(screen.getByText("Time Frame").getAttribute("aria-selected")).toEqual("true");
        expect(document.querySelector(".MuiPopper-root")).toBeInTheDocument();
    });
    it("switches back and forth between the custom and preset tabs", async () => {
        // Setup fixture
        const user = userEvent.setup({ advanceTimers: vi.advanceTimersByTime });
        render(_jsx(RDSThemeProvider, { themeMode: THEME_MODES.LIGHT, children: _jsx(RDSConfigProvider, { children: _jsx(TabbedDateRangePicker, { onChange: vi.fn(), presets: [{ presets }], tabs: ["preset", "custom"], timezone: "America/Havana", value: undefined }) }) }));
        // Exercise SUT
        await user.click(screen.getByPlaceholderText("Date Range"));
        expect(screen.getByText("Time Frame").getAttribute("aria-selected")).toEqual("true");
        await user.click(screen.getByText("Custom Date"));
        expect(screen.getByText("Custom Date").getAttribute("aria-selected")).toEqual("true");
        await user.click(screen.getByText("Time Frame"));
        expect(screen.getByText("Time Frame").getAttribute("aria-selected")).toEqual("true");
    });
    it("closes the date picker dropdown when a time frame is selected", async () => {
        // Setup fixture
        const user = userEvent.setup({ advanceTimers: vi.advanceTimersByTime });
        render(_jsx(RDSThemeProvider, { themeMode: THEME_MODES.LIGHT, children: _jsx(RDSConfigProvider, { children: _jsx(TabbedDateRangePicker, { onChange: vi.fn(), presets: [{ presets }], tabs: ["preset"], timezone: "America/Havana", value: undefined }) }) }));
        // Exercise SUT
        await user.click(screen.getByPlaceholderText("Date Range"));
        await user.click(screen.getByText("Last 1 hour"));
        await user.click(screen.getByText("Ok"));
        // Verify results
        expect(screen.getByDisplayValue("Last 1 hour")).toBeInTheDocument();
        expect(document.querySelector(".MuiPopper-root")).not.toBeInTheDocument();
    });
    it("closes the date picker dropdown when a custom date range is selected", async () => {
        // Setup fixture
        const user = userEvent.setup({ advanceTimers: vi.advanceTimersByTime });
        render(_jsx(RDSThemeProvider, { themeMode: THEME_MODES.LIGHT, children: _jsx(RDSConfigProvider, { children: _jsx(TabbedDateRangePicker, { onChange: vi.fn(), tabs: ["custom"], timezone: "America/Havana", value: undefined }) }) }));
        // Exercise SUT
        await user.click(screen.getByPlaceholderText("DD/MM/YYYY hh:mm:ss – DD/MM/YYYY hh:mm:ss"));
        await user.click(screen.getByText("12"));
        await user.click(screen.getByText("24"));
        await user.click(screen.getByText("Ok"));
        // Verify results
        expect(screen.getByDisplayValue("12/12/2024 00:00:00 – 24/12/2024 23:59:59")).toBeInTheDocument();
        expect(document.querySelector(".MuiPopper-root")).not.toBeInTheDocument();
    });
    it("closes the date picker dropdown when an element outside the picker is clicked", async () => {
        // Setup fixture
        const user = userEvent.setup({ advanceTimers: vi.advanceTimersByTime });
        render(_jsx(RDSThemeProvider, { themeMode: THEME_MODES.LIGHT, children: _jsx(RDSConfigProvider, { children: _jsx(TabbedDateRangePicker, { onChange: vi.fn(), tabs: ["custom"], timezone: "America/Havana", value: undefined }) }) }));
        // Exercise SUT
        await user.click(screen.getByPlaceholderText("DD/MM/YYYY hh:mm:ss – DD/MM/YYYY hh:mm:ss"));
        await user.click(document.body);
        // Verify results
        expect(document.querySelector(".MuiPopper-root")).not.toBeInTheDocument();
    });
    it("submits the time frame typed into the access point when the Enter key is pressed", async () => {
        // Setup fixture
        const user = userEvent.setup({ advanceTimers: vi.advanceTimersByTime });
        const onChange = vi.fn();
        render(_jsx(RDSThemeProvider, { themeMode: THEME_MODES.LIGHT, children: _jsx(RDSConfigProvider, { children: _jsx(TabbedDateRangePicker, { onChange: onChange, presets: [{ presets }], tabs: ["preset"], timezone: "Asia/Kolkata", value: undefined }) }) }));
        // Exercise SUT
        await user.click(screen.getByPlaceholderText("Date Range"));
        await user.type(screen.getByPlaceholderText("Date Range"), "Last 20 minutes");
        await user.keyboard("{Enter}");
        // Verify behaviour and results
        expect(document.querySelector(".MuiPopper-root")).not.toBeInTheDocument();
        expect(onChange).toHaveBeenCalledWith({
            from: expect.any(Number),
            timeFrame: "Last 20 minutes",
            to: expect.any(Number),
        });
    });
    it("submits the custom date range typed into the access point when the Enter key is pressed", async () => {
        // Setup fixture
        const user = userEvent.setup({ advanceTimers: vi.advanceTimersByTime });
        const onChange = vi.fn();
        render(_jsx(RDSThemeProvider, { themeMode: THEME_MODES.LIGHT, children: _jsx(RDSConfigProvider, { children: _jsx(TabbedDateRangePicker, { onChange: onChange, tabs: ["custom"], timezone: "Asia/Kolkata", value: undefined }) }) }));
        // Exercise SUT
        await user.click(screen.getByPlaceholderText("DD/MM/YYYY hh:mm:ss – DD/MM/YYYY hh:mm:ss"));
        await user.paste("12/12/2024 00:00:00 – 24/12/2024 23:59:59");
        await user.keyboard("{Enter}");
        // Verify behaviour and results
        expect(document.querySelector(".MuiPopper-root")).not.toBeInTheDocument();
        expect(onChange).toHaveBeenCalledWith({
            from: dayjs.tz("2024-12-12T00:00:00", "Asia/Kolkata").valueOf(),
            to: dayjs.tz("2024-12-24T23:59:59", "Asia/Kolkata").valueOf(),
        });
    });
    it("prevents submission of an invalid time frame when the Enter key is pressed", async () => {
        // Setup fixture
        const user = userEvent.setup({ advanceTimers: vi.advanceTimersByTime });
        const onChange = vi.fn();
        render(_jsx(RDSThemeProvider, { themeMode: THEME_MODES.LIGHT, children: _jsx(RDSConfigProvider, { children: _jsx(TabbedDateRangePicker, { onChange: onChange, presets: [{ presets }], tabs: ["preset"], timezone: "Asia/Kolkata", value: undefined }) }) }));
        // Exercise SUT
        await user.click(screen.getByPlaceholderText("Date Range"));
        await user.paste("Last 30 eons");
        await user.keyboard("{Enter}");
        // Verify behaviour and results
        expect(document.querySelector(".MuiPopper-root")).toBeInTheDocument();
        expect(onChange).not.toHaveBeenCalled();
    });
    it("prevents submission of an invalid time frame when the time frame entered does not match any of the valid units", async () => {
        // Setup fixture
        const user = userEvent.setup({ advanceTimers: vi.advanceTimersByTime });
        const onChange = vi.fn();
        render(_jsx(RDSThemeProvider, { themeMode: THEME_MODES.LIGHT, children: _jsx(RDSConfigProvider, { children: _jsx(TabbedDateRangePicker, { onChange: onChange, presets: [{ presets }], tabs: ["preset"], timezone: "Asia/Kolkata", units: ["day", "week"], value: undefined }) }) }));
        // Exercise SUT
        await user.click(screen.getByPlaceholderText("Date Range"));
        await user.paste("Last 48 hours");
        await user.keyboard("{Enter}");
        // Verify behaviour and results
        expect(screen.getByText("Invalid time frame")).toBeInTheDocument();
        expect(document.querySelector(".MuiPopper-root")).toBeInTheDocument();
        expect(onChange).not.toHaveBeenCalled();
    });
    it("prevents submission of an invalid custom date range when the Enter key is pressed", async () => {
        // Setup fixture
        const user = userEvent.setup({ advanceTimers: vi.advanceTimersByTime });
        const onChange = vi.fn();
        render(_jsx(RDSThemeProvider, { themeMode: THEME_MODES.LIGHT, children: _jsx(RDSConfigProvider, { children: _jsx(TabbedDateRangePicker, { onChange: onChange, tabs: ["custom"], timezone: "Asia/Kolkata", value: undefined }) }) }));
        // Exercise SUT
        await user.click(screen.getByPlaceholderText("DD/MM/YYYY hh:mm:ss – DD/MM/YYYY hh:mm:ss"));
        await user.paste("12/12/2024 00:00:00 – 61/12/2024 23:59:59");
        await user.keyboard("{Enter}");
        // Verify behaviour and results
        expect(document.querySelector(".MuiPopper-root")).toBeInTheDocument();
        expect(onChange).not.toHaveBeenCalled();
    });
    it("sets the end date to the current time when the 'now' button is clicked", async () => {
        // Setup fixture
        const user = userEvent.setup({ advanceTimers: vi.advanceTimersByTime });
        const onChange = vi.fn();
        render(_jsx(RDSThemeProvider, { themeMode: THEME_MODES.LIGHT, children: _jsx(RDSConfigProvider, { children: _jsx(TabbedDateRangePicker, { onChange: onChange, slots: { actions: ["now"] }, tabs: ["custom"], value: undefined }) }) }));
        // Exercise SUT
        await user.click(screen.getByPlaceholderText("DD/MM/YYYY hh:mm:ss – DD/MM/YYYY hh:mm:ss"));
        await user.click(screen.getByText("1"));
        await user.click(screen.getByText("Now"));
        await user.click(screen.getByText("Ok"));
        // Verify results
        expect(onChange).toHaveBeenCalledWith({
            from: dayjs.tz("2025-01-01T00:00:00").valueOf(),
            to: dayjs.tz("2025-01-01T23:59:59").valueOf(),
        });
    });
    it("allows the time frame value to be cleared when enabled", async () => {
        // Setup fixture
        const user = userEvent.setup({ advanceTimers: vi.advanceTimersByTime });
        const onChange = vi.fn();
        render(_jsx(RDSThemeProvider, { themeMode: THEME_MODES.LIGHT, children: _jsx(RDSConfigProvider, { children: _jsx(TabbedDateRangePicker, { clearable: true, onChange: onChange, presets: [{ presets }], tabs: ["preset"], value: { timeFrame: "Last 1 hour" } }) }) }));
        // Exercise SUT
        await user.click(screen.getByPlaceholderText("Date Range"));
        await user.click(screen.getByTitle("Clear"));
        await user.click(screen.getByText("Ok"));
        // Verify results
        expect(onChange).toHaveBeenCalledWith(undefined);
    });
    it("allows the custom date range value to be cleared when enabled", async () => {
        // Setup fixture
        const user = userEvent.setup({ advanceTimers: vi.advanceTimersByTime });
        const onChange = vi.fn();
        render(_jsx(RDSThemeProvider, { themeMode: THEME_MODES.LIGHT, children: _jsx(RDSConfigProvider, { children: _jsx(TabbedDateRangePicker, { clearable: true, onChange: onChange, tabs: ["custom"], value: {
                        from: dayjs("2024-12-12T00:00:00").valueOf(),
                        to: dayjs("2024-12-24T23:59:59").valueOf(),
                    } }) }) }));
        // Exercise SUT
        await user.click(screen.getByPlaceholderText("DD/MM/YYYY hh:mm:ss – DD/MM/YYYY hh:mm:ss"));
        await user.click(screen.getByTitle("Clear"));
        await user.click(screen.getByText("Ok"));
        // Verify results
        expect(onChange).toHaveBeenCalledWith(undefined);
    });
    it("displays the provided units as options in the custom preset picker", async () => {
        // Setup fixture
        const units = ["hour", "month", "year"];
        const user = userEvent.setup({ advanceTimers: vi.advanceTimersByTime });
        render(_jsx(RDSThemeProvider, { themeMode: THEME_MODES.LIGHT, children: _jsx(RDSConfigProvider, { children: _jsx(TabbedDateRangePicker, { onChange: vi.fn(), tabs: ["preset"], timezone: "UTC", units: units, value: undefined }) }) }));
        // Exercise SUT (open the unit picker)
        await user.click(screen.getByPlaceholderText("Date Range"));
        await user.click(screen.getByLabelText("Unit of time"));
        // Verify results
        const options = document.querySelectorAll(".MuiMenuItem-root");
        expect(options).toHaveLength(units.length);
        expect(options[0].textContent).toBe("Hour");
        expect(options[1].textContent).toBe("Month");
        expect(options[2].textContent).toBe("Year");
    });
    it("displays units less than and equal to the provided single unit in the custom preset picker", async () => {
        // Setup fixture
        const unit = "minute";
        const user = userEvent.setup({ advanceTimers: vi.advanceTimersByTime });
        render(_jsx(RDSThemeProvider, { themeMode: THEME_MODES.LIGHT, children: _jsx(RDSConfigProvider, { children: _jsx(TabbedDateRangePicker, { onChange: vi.fn(), tabs: ["preset"], timezone: "UTC", units: unit, value: undefined }) }) }));
        // Exercise SUT (open the unit picker)
        await user.click(screen.getByPlaceholderText("Date Range"));
        await user.click(screen.getByLabelText("Unit of time"));
        // Verify results
        const options = document.querySelectorAll(".MuiMenuItem-root");
        expect(options).toHaveLength(2);
        expect(options[0].textContent).toBe("Second");
        expect(options[1].textContent).toBe("Minute");
    });
    it("falls back to the default units when an invalid single unit is provided", async () => {
        // Setup fixture
        const unit = "aeon";
        const user = userEvent.setup({ advanceTimers: vi.advanceTimersByTime });
        render(_jsx(RDSThemeProvider, { themeMode: THEME_MODES.LIGHT, children: _jsx(RDSConfigProvider, { children: _jsx(TabbedDateRangePicker, { onChange: vi.fn(), tabs: ["preset"], timezone: "UTC", units: unit, value: undefined }) }) }));
        // Exercise SUT (open the unit picker)
        await user.click(screen.getByPlaceholderText("Date Range"));
        await user.click(screen.getByLabelText("Unit of time"));
        // Verify results
        const options = document.querySelectorAll(".MuiMenuItem-root");
        expect(options).toHaveLength(7);
        expect(options[0].textContent).toBe("Second");
        expect(options[1].textContent).toBe("Minute");
        expect(options[2].textContent).toBe("Hour");
        expect(options[3].textContent).toBe("Day");
        expect(options[4].textContent).toBe("Week");
        expect(options[5].textContent).toBe("Month");
        expect(options[6].textContent).toBe("Year");
    });
    it("does not re-initialize the date picker when it received an updated initial time range with the same values", async () => {
        // Setup fixture
        const user = userEvent.setup({ advanceTimers: vi.advanceTimersByTime });
        const { rerender } = render(_jsx(RDSThemeProvider, { themeMode: THEME_MODES.LIGHT, children: _jsx(RDSConfigProvider, { children: _jsx(TabbedDateRangePicker, { onChange: vi.fn(), timezone: "America/Havana", value: {
                        from: dayjs("2024-12-12T00:00:00").valueOf(),
                        to: dayjs("2024-12-24T23:59:59").valueOf(),
                    } }) }) }));
        await user.click(screen.getByPlaceholderText("DD/MM/YYYY hh:mm:ss – DD/MM/YYYY hh:mm:ss"));
        await user.click(screen.getByText("Time Frame"));
        // Exercise SUT by updating the initial value
        rerender(_jsx(RDSThemeProvider, { themeMode: THEME_MODES.LIGHT, children: _jsx(RDSConfigProvider, { children: _jsx(TabbedDateRangePicker, { onChange: vi.fn(), timezone: "America/Havana", value: {
                        from: dayjs("2024-12-12T00:00:00").valueOf(),
                        to: dayjs("2024-12-24T23:59:59").valueOf(),
                    } }) }) }));
        // Verify results ensuring the time frame tab is still selected i.e did not switch back to "Custom Date" based on the initial value change
        expect(screen.getByText("Time Frame").getAttribute("aria-selected")).toEqual("true");
        expect(document.querySelector(".MuiPopper-root")).toBeInTheDocument();
    });
});
