import { jsx as _jsx } from "react/jsx-runtime";
import { Chip, Tooltip } from "@mui/material";
import { Box } from "@mui/system";
import { Global } from "@rapid7/icons";
import useSafeTimezone from "./hooks/useSafeTimezone";
const TimezoneChip = ({ timezone, tooltipTitle }) => {
    const { formattedTimezone } = useSafeTimezone(timezone);
    return (_jsx(Tooltip, { arrow: true, placement: "right", slotProps: { tooltip: { sx: { maxWidth: 170 } } }, title: tooltipTitle, children: _jsx(Box, { sx: { alignSelf: "flex-start" }, children: _jsx(Chip, { color: "primary", disabled: true, icon: _jsx(Global, {}), label: formattedTimezone }) }) }));
};
export default TimezoneChip;
