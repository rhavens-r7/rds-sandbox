import { jsx as _jsx, Fragment as _Fragment, jsxs as _jsxs } from "react/jsx-runtime";
import { Box, Skeleton, Stack, useTheme } from "@mui/material";
import { AttributeRoot, AttributeLabel, getIconStyled, } from "./styles/attribute.styles";
import React from "react";
import { getContent } from "./utils/attribute.utils";
/**
 The Attribute is meant to be used as a standard way to display a bit of info, such as a username or id, a numeric value, a tag (aka chip), a link or a button.
 One of its main use cases will be as part of the DetailsPageHeader component.

 These can live individually, in a row, or in a column.
*/
export const Attribute = React.forwardRef(
// Must specify props' type in order to show the full API in Storybook
function Attribute(props, ref) {
    const { AttributeRootProps, icon: Icon, label, content, variant, LabelProps = {}, IconProps = {}, isLoading, } = props;
    const theme = useTheme();
    const ownerState = { ...props };
    const { sx = {}, ...iconProps } = IconProps;
    const iconSx = {
        ...getIconStyled(theme),
        ...sx,
    };
    const iconAndLabelSlot = (_jsxs(_Fragment, { children: [!!Icon && _jsx(Icon, { sx: iconSx, ...iconProps }), label && (_jsx(AttributeLabel, { noWrap: true, ownerState: ownerState, ...LabelProps, children: label }))] }));
    if (variant === "vertical") {
        return (_jsx(AttributeRoot, { ownerState: ownerState, ref: ref, ...AttributeRootProps, children: _jsxs(Stack, { direction: "column", children: [_jsx(Box, { sx: { alignItems: "center", display: "flex" }, children: iconAndLabelSlot }), isLoading ? (_jsx(Skeleton, { height: 20, variant: "text", width: 100 })) : (getContent(content))] }) }));
    }
    return (_jsxs(AttributeRoot, { ownerState: ownerState, ref: ref, ...AttributeRootProps, children: [iconAndLabelSlot, isLoading ? (_jsx(Skeleton, { height: 20, variant: "text", width: 100 })) : (getContent(content))] }));
});
export const AttributeForArgs = (props) => (_jsx(Attribute, { ...props }));
