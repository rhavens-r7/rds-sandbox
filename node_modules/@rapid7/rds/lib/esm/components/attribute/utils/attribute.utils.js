import { jsx as _jsx } from "react/jsx-runtime";
import { Box } from "@mui/material";
import { TypographySlot } from "../styles/attribute.styles";
export const getContent = (content) => {
    const isContentString = typeof content === "string";
    const isContentArray = Array.isArray(content);
    let contentSlot = content;
    if (isContentString) {
        contentSlot = _jsx(TypographySlot, { children: content });
    }
    if (isContentArray) {
        contentSlot = (_jsx(Box, { display: "flex", flexDirection: "row", children: content }));
    }
    return contentSlot;
};
