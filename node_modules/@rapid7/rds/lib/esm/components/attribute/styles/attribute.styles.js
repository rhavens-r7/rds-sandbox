import { styled as MUIStyled } from "@mui/material/styles";
import { Box, Typography } from "@mui/material";
export const getIconStyled = (theme) => ({
    color: theme.palette.text.secondary,
    fontSize: "16px",
    marginBottom: theme.spacing(0.1),
    marginRight: theme.spacing(1),
});
export const AttributeRoot = MUIStyled(Box, {
    name: "Attribute",
    slot: "root",
})(({ theme }) => ({
    alignItems: "center",
    display: "flex",
    fontWeight: 400,
    padding: theme.spacing(0.5, 0),
}));
export const AttributeLabel = MUIStyled(Typography, {
    name: "Attribute",
    slot: "label",
})(({ theme, ownerState }) => ({
    color: theme.palette.text.secondary,
    fontSize: "14px",
    marginRight: theme.spacing(1),
    width: ownerState.labelWidth || "auto",
    ...(ownerState.variant === "column" && {
        width: ownerState.labelWidth || "200px",
    }),
}));
export const TypographySlot = MUIStyled(Typography, {
    name: "Attribute",
    slot: "content",
})(({ theme }) => ({
    color: theme.palette.text.primary,
    fontSize: "14px",
}));
