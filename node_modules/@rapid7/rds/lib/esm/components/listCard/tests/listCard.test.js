import { jsx as _jsx } from "react/jsx-runtime";
import { describe, it, expect, beforeAll, vitest } from "vitest";
import { render, waitFor } from "@testing-library/react";
import { userEvent } from "@testing-library/user-event";
import { Star } from "@mui/icons-material";
import mediaQuery from "css-mediaquery";
import { ListCard } from "..";
import { RDSThemeProvider, THEME_MODES } from "../../../rdsThemeProvider";
function createMatchMedia(width) {
    return (query) => ({
        addEventListener: () => { },
        matches: mediaQuery.match(query, {
            width,
        }),
        removeEventListener: () => { },
    });
}
describe("ListCard", () => {
    beforeAll(() => {
        Object.defineProperty(window, "innerWidth", {
            configurable: true,
            value: 500,
            writable: true,
        });
        // @ts-ignore mocking matchMedia
        window.matchMedia = createMatchMedia(window.innerWidth);
    });
    it("has correct layout and click event triggers", async () => {
        const onClick = vitest.fn();
        const { getByLabelText, getAllByLabelText, getAllByRole, getAllByText } = render(_jsx(RDSThemeProvider, { themeMode: THEME_MODES.LIGHT, children: _jsx(ListCard, { contentSlots: [
                    {
                        Component: () => _jsx("div", { children: "Content" }),
                    },
                ], ListCardHeaderProps: {
                    Icon: Star,
                    Title: () => _jsx("div", { children: "Title" }),
                }, LocalActionsProps: {
                    actions: [
                        {
                            Icon: Star,
                            onClick: () => { },
                            text: "Star",
                        },
                        {
                            Icon: Star,
                            onClick: () => { },
                            text: "Star1",
                        },
                        {
                            Icon: Star,
                            onClick: () => { },
                            text: "Star2",
                        },
                        {
                            Icon: Star,
                            onClick: () => { },
                            text: "Star3",
                        },
                    ],
                }, onClick: onClick }) }));
        const user = userEvent.setup();
        const title = getAllByText("Title");
        expect(title).toHaveLength(1);
        await user.click(title[0]);
        expect(onClick).toBeCalledTimes(1);
        const expandIcon = getByLabelText("expand");
        const icons = getAllByLabelText("star", { exact: false });
        expect(icons).toHaveLength(2);
        expect(expandIcon).toBeInTheDocument();
        await user.click(expandIcon);
        const menuItems = await waitFor(() => getAllByRole("menuitem"));
        expect(menuItems).toHaveLength(2);
        expect(onClick).toBeCalledTimes(1);
    });
    it("has small screen so content is hidden", async () => {
        const onClick = vitest.fn();
        const { queryByText } = render(_jsx(RDSThemeProvider, { themeMode: THEME_MODES.LIGHT, children: _jsx(ListCard, { contentSlots: [
                    {
                        Component: () => _jsx("div", { children: "Content" }),
                    },
                ], ListCardHeaderProps: {
                    Icon: Star,
                    Title: () => _jsx("div", { children: "Title" }),
                }, LocalActionsProps: {
                    actions: [
                        {
                            Icon: Star,
                            onClick: () => { },
                            text: "Star",
                        },
                        {
                            Icon: Star,
                            onClick: () => { },
                            text: "Star1",
                        },
                        {
                            Icon: Star,
                            onClick: () => { },
                            text: "Star2",
                        },
                        {
                            Icon: Star,
                            onClick: () => { },
                            text: "Star3",
                        },
                    ],
                }, onClick: onClick }) }));
        const content = queryByText("Content");
        expect(content).toBeNull();
    });
    it("has selection state and selection event triggers", async () => {
        const onSelection = vitest.fn();
        const { getByLabelText } = render(_jsx(RDSThemeProvider, { themeMode: THEME_MODES.LIGHT, children: _jsx(ListCard, { contentSlots: [], isSelectable: true, ListCardHeaderProps: {
                    Title: () => _jsx("div", { children: "Title" }),
                }, LocalActionsProps: {
                    actions: [
                        {
                            Icon: Star,
                            onClick: () => { },
                            text: "Star",
                        },
                        {
                            Icon: Star,
                            onClick: () => { },
                            text: "Star1",
                        },
                        {
                            Icon: Star,
                            onClick: () => { },
                            text: "Star2",
                        },
                        {
                            Icon: Star,
                            onClick: () => { },
                            text: "Star3",
                        },
                    ],
                }, onSelection: onSelection }) }));
        const checkbox = getByLabelText("controlled");
        const user = userEvent.setup();
        await user.click(checkbox);
        expect(onSelection).toBeCalledTimes(1);
    });
});
describe("ListCard with larger screen", () => {
    beforeAll(() => {
        Object.defineProperty(window, "innerWidth", {
            configurable: true,
            value: 1000,
            writable: true,
        });
        // @ts-ignore mocking matchMedia
        window.matchMedia = createMatchMedia(window.innerWidth);
    });
    it("has the correct content when screen is large enough", async () => {
        const { getByLabelText, getAllByLabelText, getAllByRole, getAllByText } = render(_jsx(RDSThemeProvider, { themeMode: THEME_MODES.LIGHT, children: _jsx(ListCard, { contentSlots: [
                    {
                        Component: () => _jsx("div", { children: "Content" }),
                    },
                ], ListCardHeaderProps: {
                    Subitem: () => _jsx("div", { children: "Subitem" }),
                    Title: () => _jsx("div", { children: "Title" }),
                }, LocalActionsProps: {
                    actions: [
                        {
                            Icon: Star,
                            onClick: () => { },
                            text: "Star",
                        },
                        {
                            Icon: Star,
                            onClick: () => { },
                            text: "Star1",
                        },
                        {
                            Icon: Star,
                            onClick: () => { },
                            text: "Star2",
                        },
                        {
                            Icon: Star,
                            onClick: () => { },
                            text: "Star3",
                        },
                    ],
                } }) }));
        const title = getAllByText("Title");
        expect(title).toHaveLength(1);
        const subitem = getAllByText("Subitem");
        expect(subitem).toHaveLength(1);
        const content = getAllByText("Content");
        expect(content).toHaveLength(1);
        const expandIcon = getByLabelText("expand");
        const icons = getAllByLabelText("star", { exact: false });
        expect(icons).toHaveLength(2);
        expect(expandIcon).toBeInTheDocument();
        const user = userEvent.setup();
        await user.click(expandIcon);
        const menuItems = await waitFor(() => getAllByRole("menuitem"));
        expect(menuItems).toHaveLength(2);
    });
});
