import { jsx as _jsx } from "react/jsx-runtime";
import { Grid } from "@mui/material";
export const ListCardContent = ({ contentSlots = [], }) => {
    return (_jsx(Grid, { alignItems: "center", columnGap: 2, container: true, justifyContent: "flex-end", children: contentSlots.map(({ Component }, idx) => (_jsx(Grid, { item: true, children: _jsx(Component, {}) }, idx))) }));
};
export default ListCardContent;
