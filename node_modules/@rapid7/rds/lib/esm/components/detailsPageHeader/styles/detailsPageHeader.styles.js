/* eslint-disable @typescript-eslint/no-unused-vars */
import { styled as MUIStyled } from "@mui/material/styles";
import { Box, Divider, Link, Typography } from "@mui/material";
export const DetailsPageHeaderRoot = MUIStyled(Box, {
    name: "DetailsPageHeader",
    slot: "root",
})(({ theme }) => ({
    background: theme.palette.background.default,
    display: "flex",
    flexDirection: "column",
    position: "relative",
}));
export const RefBox = MUIStyled(Box) ``;
export const SlotContainer = MUIStyled(Box)(({ theme }) => ({
    display: "flex",
    flexDirection: "row",
    flexWrap: "wrap",
    [theme.breakpoints.down("sm")]: {
        maxWidth: "320px",
    },
}));
export const ActionsContainer = MUIStyled(Box)(({ theme }) => ({
    "& :not(:last-child)": {
        marginRight: theme.spacing(1),
    },
    flexDirection: "row",
    flexWrap: "wrap",
}));
// Breadcrumbs
export const BreadcrumbLink = MUIStyled(Link)(({ theme }) => ({
    color: theme.palette.text.primary,
    fontSize: "12px",
}));
export const CurrentLocation = MUIStyled(Typography)(({ theme }) => ({
    color: theme.palette.text.primary,
    fontSize: "12px",
    marginLeft: theme.spacing(0.5),
}));
// Page Title
export const PageTitle = MUIStyled(Typography, {
    name: "DetailsPageHeader",
    slot: "pageTitle",
})(({ theme }) => ({
    color: theme.palette.text.primary,
    [theme.breakpoints.down("sm")]: {
        fontSize: "1.25rem",
    },
    lineHeight: 1,
    marginRight: "8px",
}));
export const TitleAndTagContainer = MUIStyled(Box, {
    shouldForwardProp: (prop) => prop !== "hasBreadcrumbs",
})(({ theme, hasBreadcrumbs }) => ({
    alignItems: "center",
    display: "flex",
    flexDirection: "row",
    flexWrap: "wrap",
    marginBottom: theme.spacing(1),
    marginTop: hasBreadcrumbs ? theme.spacing(1) : theme.spacing(0),
}));
// Divider
export const StyledDivider = MUIStyled(Divider)(({ theme }) => ({
    marginTop: theme.spacing(1),
}));
