import { jsx as _jsx, jsxs as _jsxs } from "react/jsx-runtime";
import { Box, Button, Typography } from "@mui/material";
import { PreviousChevronLeftArrow, RefreshReload } from "@rapid7/icons";
import { buttonText, description, headers, } from "./errorStates.consts";
import { Illustration } from "../illustration";
const defaultSx = {
    alignItems: "center",
    display: "flex",
    flexDirection: "column",
    height: "100%",
    justifyContent: "center",
    textAlign: "center",
    width: "100%",
};
export const Error = ({ errorCode = "default", onClick, size = "small", slots, sx, }) => {
    return (_jsxs(Box, { sx: sx
            ? Array.isArray(sx)
                ? [defaultSx, ...sx]
                : [defaultSx, sx]
            : defaultSx, children: [_jsx(Illustration, { illustrationName: errorCode, size: size }), _jsx(Typography, { sx: { lineHeight: 1.25 }, variant: "h2", children: slots?.header || headers[errorCode] }), _jsx(Typography, { children: slots?.description || description[errorCode] }), slots?.action || (_jsx(Button, { onClick: onClick, startIcon: buttonText[errorCode] === "Refresh" ? (_jsx(RefreshReload, {})) : (_jsx(PreviousChevronLeftArrow, { sx: { fontSize: "20px" } })), sx: { mt: 2 }, variant: "contained", children: buttonText[errorCode] }))] }));
};
