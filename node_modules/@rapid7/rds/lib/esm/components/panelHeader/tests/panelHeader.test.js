import { jsx as _jsx } from "react/jsx-runtime";
import { describe, it, expect } from "vitest";
import { screen, render } from "@testing-library/react";
import { PanelHeader } from "../PanelHeader";
import { Skeleton, Typography } from "@mui/material";
describe("Rapid7 PanelHeader", () => {
    it("Should render the PanelHeader with the correct label and content string", () => {
        render(_jsx(PanelHeader, { pageTitle: "test" }));
        expect(screen.getByText("test")).toBeInTheDocument();
    });
    it("Should render correctly when component is passed to pageTitle prop", () => {
        render(_jsx(PanelHeader, { pageTitle: _jsx(Skeleton, { children: _jsx(Typography, { children: "Loading..." }) }) }));
        expect(screen.getByText("Loading...")).toBeInTheDocument();
    });
});
