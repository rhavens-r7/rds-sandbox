/* eslint-disable @typescript-eslint/no-unused-vars */
import { styled as MUIStyled } from "@mui/material/styles";
import { Box, Divider, Typography } from "@mui/material";
import { BackArrow, CloseIcon } from "../../ExampleIcon";
export const PanelHeaderRoot = MUIStyled(Box, {
    name: "DetailsPageHeader",
    slot: "root",
})(({ theme }) => ({
    background: theme.palette.background.default,
    display: "flex",
    flexDirection: "column",
    position: "relative",
    zIndex: "10",
}));
export const SlotContainer = MUIStyled(Box)(({ theme }) => ({
    display: "flex",
    flexDirection: "row",
    flexWrap: "wrap",
    [theme.breakpoints.down("sm")]: {
        maxWidth: "320px",
    },
}));
export const ActionsContainer = MUIStyled(Box)(({ theme }) => ({
    "& :not(:last-child)": {
        marginRight: theme.spacing(1),
    },
    flexDirection: "row",
    flexWrap: "wrap",
    marginLeft: "auto",
}));
// Page Title
export const PageTitle = MUIStyled(Typography, {
    name: "DetailsPageHeader",
    slot: "pageTitle",
})(({ theme }) => ({
    color: theme.palette.text.primary,
    [theme.breakpoints.down("sm")]: {
        fontSize: "1.25rem",
    },
    lineHeight: 1,
}));
export const TitleAndTagContainer = MUIStyled(Box)(({ theme }) => ({
    alignItems: "center",
    display: "flex",
    flexDirection: "row",
    flexWrap: "wrap",
    gap: theme.spacing(1),
}));
// Divider
export const StyledDivider = MUIStyled(Divider)(({ theme }) => ({
    marginTop: theme.spacing(1),
}));
// Icons
export const StyledCloseIcon = MUIStyled(CloseIcon)(({ theme }) => ({
    color: theme.palette.text.secondary,
}));
export const StyledBackIcon = MUIStyled(BackArrow)(({ theme }) => ({
    color: theme.palette.text.secondary,
}));
