import { jsx as _jsx } from "react/jsx-runtime";
import "../../theme";
import Popper from "@mui/material/Popper";
const RdsPopper = ({ arrow, modifiers = [], ...rest }) => {
    const mergedModifiers = arrow
        ? [{ name: "offset", options: { offset: [0, 9] } }, ...modifiers]
        : modifiers;
    return _jsx(Popper, { arrow: arrow, modifiers: mergedModifiers, ...rest });
};
export { RdsPopper as Popper };
