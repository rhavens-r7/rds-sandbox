import { jsx as _jsx } from "react/jsx-runtime";
import { Box } from "@mui/material";
import { illustrationMap } from "./illustrations.const";
export const Illustration = ({ illustrationName, size = "small", sx, }) => {
    const illustration = illustrationMap[illustrationName]?.[size];
    if (!illustration)
        return null;
    return (_jsx(Box, { "data-testid": illustrationName, sx: sx, children: illustration }));
};
