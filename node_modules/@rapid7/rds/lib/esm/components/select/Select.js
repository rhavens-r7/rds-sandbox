import { jsx as _jsx } from "react/jsx-runtime";
import { forwardRef, useMemo } from "react";
import { Select } from "@mui/material";
import { ExpandChevronDownArrow } from "@rapid7/icons";
export const RDSSelect = forwardRef((props, ref) => {
    const { IconComponent = ExpandChevronDownArrow, variant = "outlined", MenuProps, ...rest } = props;
    const selectSx = useMemo(() => (Array.isArray(props.sx) ? props.sx : [props.sx]), [props.sx]);
    const menuPropsSx = useMemo(() => (Array.isArray(MenuProps?.sx) ? MenuProps.sx : [MenuProps?.sx]), [MenuProps?.sx]);
    const mergedMenuProps = useMemo(() => {
        if (props.multiple) {
            return {
                anchorOrigin: {
                    horizontal: "left",
                    vertical: "bottom",
                },
                autoFocus: false,
                transformOrigin: {
                    horizontal: "left",
                    vertical: "top",
                },
                ...MenuProps,
                sx: [
                    {
                        "& .MuiBackdrop-root": {
                            backgroundColor: "transparent",
                        },
                        "& .MuiMenuItem-root": {
                            gap: "0px",
                            padding: "0px",
                        },
                    },
                    ...menuPropsSx,
                ],
            };
        }
        return {
            autoFocus: false,
            ...MenuProps,
            sx: [
                {
                    "& .MuiBackdrop-root": {
                        backgroundColor: "transparent",
                    },
                },
                ...menuPropsSx,
            ],
        };
    }, [props.multiple, MenuProps, menuPropsSx]);
    return (_jsx(Select, { ...rest, IconComponent: IconComponent, MenuProps: mergedMenuProps, ref: ref, sx: [
            (theme) => ({
                "& .MuiSelect-icon": {
                    alignItems: "center",
                    display: "flex",
                    fontSize: theme.typography.pxToRem(20),
                    height: theme.spacing(2.5),
                    justifyContent: "center",
                    transform: "none",
                    width: theme.spacing(2.5),
                },
                "&.MuiInputBase-root .MuiSelect-select": {
                    alignItems: "center",
                    boxSizing: "border-box",
                    ...(!props.multiple && {
                        height: theme.spacing(4.5),
                        maxHeight: theme.spacing(4.5),
                        paddingBottom: theme.spacing(1),
                        paddingTop: theme.spacing(1),
                    }),
                    ...(props.multiple && {
                        minHeight: theme.spacing(4.5),
                        paddingBottom: "7px",
                        paddingTop: "7px",
                    }),
                },
                marginTop: "1px",
            }),
            ...selectSx,
        ], variant: variant }));
});
