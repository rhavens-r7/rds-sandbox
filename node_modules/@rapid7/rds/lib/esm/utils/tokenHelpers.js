import { getValue } from "../tokens";
export const getBorderRadius = (theme) => getValue("borderRadius", theme.palette.brand, theme.palette.mode);
export const getTypography = (theme) => getValue("typography", theme.palette.brand, theme.palette.mode);
