export const ThemedAccordionDetails = {
    styleOverrides: {
        root: ({ theme }) => ({
            ...theme.typography.body1,
            "&:has(+ .MuiAccordionActions-root)": {
                paddingBottom: theme.spacing(1),
            },
            paddingBottom: theme.spacing(2),
        }),
    },
};
