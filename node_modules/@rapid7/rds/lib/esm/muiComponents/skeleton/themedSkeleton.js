import { getBorderRadius } from "../../utils/tokenHelpers";
export const ThemedSkeleton = {
    styleOverrides: {
        root: {
            variants: [
                {
                    props: { variant: "circular" },
                    style: ({ theme }) => ({
                        backgroundColor: theme.palette.action.disabledBackground,
                        borderRadius: getBorderRadius(theme).radial,
                    }),
                },
                {
                    props: { variant: "text" },
                    style: ({ theme }) => ({
                        backgroundColor: theme.palette.action.disabledBackground,
                        borderRadius: getBorderRadius(theme).small,
                    }),
                },
                {
                    props: { variant: "rectangular" },
                    style: ({ theme }) => ({
                        backgroundColor: theme.palette.action.disabledBackground,
                        borderRadius: getBorderRadius(theme).small,
                    }),
                },
            ],
        },
    },
};
