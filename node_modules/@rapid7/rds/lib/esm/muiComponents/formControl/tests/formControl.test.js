import { jsx as _jsx, jsxs as _jsxs } from "react/jsx-runtime";
import { render } from "@testing-library/react";
import { Checkbox, FormControl, FormControlLabel, FormGroup, FormHelperText, FormLabel, } from "@mui/material";
import { describe, it, expect } from "vitest";
import { RDSThemeProvider } from "../../../rdsThemeProvider";
import { OriginalThemeMap } from "../../../originalTheme";
import { CallistoThemeMap } from "../../../callistoTheme";
import "@testing-library/jest-dom/vitest";
export const testThemeConfigurations = [
    {
        themeMap: OriginalThemeMap,
        themeName: "OriginalThemeMap",
    },
    {
        themeMap: CallistoThemeMap,
        themeName: "CallistoThemeMap",
    },
];
testThemeConfigurations.forEach(({ themeName, themeMap }) => {
    const renderThemedFormControl = ({ withOutlined = false } = {}) => render(_jsx(RDSThemeProvider, { themeMap: themeMap, themeMode: "light", children: _jsxs(FormControl, { children: [_jsx(FormLabel, { children: "Form Label" }), _jsxs(FormGroup, { children: [_jsx(FormControlLabel, { control: _jsx(Checkbox, {}), label: "Checkbox 1", variant: withOutlined ? "outlined" : "standard" }), _jsx(FormControlLabel, { control: _jsx(Checkbox, {}), label: "Checkbox 2", variant: withOutlined ? "outlined" : "standard" })] }), _jsx(FormHelperText, { children: "Helper Text" })] }) }));
    describe(`${themeName} ThemedFormControl`, () => {
        it("does not apply extra spacing when no outlined variant is present", () => {
            renderThemedFormControl({});
            const legend = document.querySelector(".MuiFormLabel-root");
            const helperText = document.querySelector(".MuiFormHelperText-root");
            expect(legend).not.toHaveStyle("margin-bottom: 8px");
            expect(helperText).not.toHaveStyle("margin-top: 8px");
        });
        it("applies 8px margin when an outlined FormControlLabel is present", () => {
            renderThemedFormControl({ withOutlined: true });
            const legend = document.querySelector(".MuiFormLabel-root");
            const helperText = document.querySelector(".MuiFormHelperText-root");
            expect(legend).toHaveStyle("margin-bottom: 8px");
            expect(helperText).toHaveStyle("margin-top: 8px");
        });
    });
});
