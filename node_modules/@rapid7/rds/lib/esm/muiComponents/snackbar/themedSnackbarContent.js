import { alpha } from "@mui/material/styles";
import "@mui/material/SnackbarContent";
export const ThemedSnackbarContent = {
    styleOverrides: {
        action: ({ theme }) => ({
            "& .MuiButton-text": {
                padding: 0,
            },
            "& button": {
                "&:hover": {
                    backgroundColor: alpha(theme.palette.components.snackbar.text, 0.1),
                },
                color: theme.palette.components.snackbar.text,
            },
            alignItems: "center",
            gap: theme.spacing(1),
            margin: 0,
            padding: theme.spacing(0.5, 0, 0.5, 2),
        }),
        message: ({ theme }) => ({
            ".MuiBox-root": {
                gap: theme.spacing(1.5),
            },
        }),
        root: ({ theme }) => ({
            backgroundColor: theme.palette.components.snackbar.background,
            color: theme.palette.components.snackbar.text,
        }),
    },
};
