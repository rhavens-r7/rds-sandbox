import "@mui/material/Snackbar";
export const ThemedSnackbar = {
    styleOverrides: {
        root: ({ theme }) => {
            return {
                "& .MuiPaper-root": {
                    minWidth: `${theme.spacing(16)}`,
                },
            };
        },
    },
};
