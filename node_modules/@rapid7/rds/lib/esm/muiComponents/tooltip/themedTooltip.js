import "@mui/material/Tooltip";
export const ThemedTooltip = {
    styleOverrides: {
        arrow: ({ theme }) => ({
            "::before": {
                outline: `1px solid ${theme.palette.components.tooltip.border}`,
                outlineOffset: "-1px",
            },
            color: theme.palette.components.tooltip.background,
        }),
        tooltip: ({ theme }) => ({
            backgroundColor: theme.palette.components.tooltip.background,
            border: `1px solid ${theme.palette.components.tooltip.border}`,
            borderRadius: theme.shape.borderRadius,
            color: theme.palette.components.tooltip.text,
        }),
    },
};
