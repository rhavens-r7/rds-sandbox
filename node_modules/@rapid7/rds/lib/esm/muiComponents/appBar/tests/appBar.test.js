import { jsx as _jsx } from "react/jsx-runtime";
import { describe, it, expect } from "vitest";
import { render } from "@testing-library/react";
import { AppBar, Toolbar, Typography } from "@mui/material";
import { RDSThemeProvider } from "../../../rdsThemeProvider";
import { CallistoThemeMap } from "../../../callistoTheme";
import { OriginalThemeMap } from "../../../originalTheme";
describe("AppBar", () => {
    describe("Callisto theme", () => {
        it("has the correct default background color in light mode", () => {
            const { getByTestId } = render(_jsx(RDSThemeProvider, { themeMap: CallistoThemeMap, themeMode: "light", children: _jsx(AppBar, { "data-testid": "navbar", position: "static", children: _jsx(Toolbar, { children: _jsx(Typography, { variant: "h6", children: "Styled Chrome" }) }) }) }));
            const navbar = getByTestId("navbar");
            // Assuming the background color is set to blue by default
            expect(navbar).toHaveStyle("background-color: #F8FAFA");
        });
        it("has the correct default background color in dark mode", () => {
            const { getByTestId } = render(_jsx(RDSThemeProvider, { themeMap: CallistoThemeMap, themeMode: "dark", children: _jsx(AppBar, { "data-testid": "navbar", position: "static", children: _jsx(Toolbar, { children: _jsx(Typography, { variant: "h6", children: "Styled Chrome" }) }) }) }));
            const navbar = getByTestId("navbar");
            // Assuming the background color is set to blue by default
            expect(navbar).toHaveStyle("background-color: #242b2e");
        });
    });
    describe("Original theme", () => {
        it("has the correct default background color", () => {
            const { getByTestId } = render(_jsx(RDSThemeProvider, { themeMap: OriginalThemeMap, themeMode: "light", children: _jsx(AppBar, { "data-testid": "navbar", position: "static", children: _jsx(Toolbar, { children: _jsx(Typography, { variant: "h6", children: "Styled Chrome" }) }) }) }));
            const navbar = getByTestId("navbar");
            // Assuming the background color is set to blue by default
            expect(navbar).toHaveStyle("background-color: #121D26");
        });
    });
});
