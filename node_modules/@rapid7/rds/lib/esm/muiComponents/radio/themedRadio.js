import "@mui/material/Radio";
export const ThemedRadio = {
    styleOverrides: {
        root: ({ theme }) => ({
            "&:has(input:focus-visible)::before": {
                border: `2px solid ${theme.palette.primary.main}`,
                borderRadius: "99px",
                content: '""',
                inset: "7px",
                pointerEvents: "none",
                position: "absolute",
            },
            "&:hover": {
                backgroundColor: theme.palette.components?.radio?.hover.primary,
                borderRadius: "12.5px",
            },
            position: "relative",
        }),
    },
    variants: [
        {
            props: { color: "error" },
            style: ({ theme }) => ({
                "& + .MuiFormControlLabel-label": {
                    color: theme.palette.error.main,
                },
                "& svg": {
                    color: theme.palette.error.main,
                },
                "&:hover": {
                    backgroundColor: theme.palette.components?.radio?.hover.error,
                },
            }),
        },
        {
            props: { color: "success" },
            style: ({ theme }) => ({
                "&:hover": {
                    backgroundColor: theme.palette.components?.radio?.hover.success,
                },
            }),
        },
    ],
};
