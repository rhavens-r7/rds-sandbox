import "@mui/material/StepLabel";
import { getValue } from "../../tokens";
export const ThemedStepLabel = {
    styleOverrides: {
        label: ({ theme }) => ({
            "&.Mui-error": {
                color: `${theme.palette.text.primary}`,
            },
        }),
        labelContainer: {
            textAlign: "left",
        },
        root: ({ theme }) => ({
            "& .MuiTypography-body1": {
                ...theme.typography.subtitle2,
                fontWeight: getValue("typography", theme.palette.brand, theme.palette.mode).font.weight.n50,
            },
            "& .MuiTypography-caption": {
                ...theme.typography.caption,
                fontSize: "11px",
                fontWeight: getValue("typography", theme.palette.brand, theme.palette.mode).font.weight.n40,
            },
            "&.Mui-error .MuiSvgIcon-root": {
                color: theme.palette.error.main,
            },
        }),
    },
};
