import "@mui/material/Step";
export const ThemedStep = {
    styleOverrides: {
        root: ({ theme, ownerState }) => ({
            ...(ownerState.orientation === "vertical" && ownerState.alternativeLabel
                ? {
                    marginBottom: theme.spacing(3),
                }
                : {}),
        }),
    },
};
