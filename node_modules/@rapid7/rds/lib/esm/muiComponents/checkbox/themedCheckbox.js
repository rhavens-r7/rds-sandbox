import { jsx as _jsx } from "react/jsx-runtime";
import "@mui/material/Checkbox";
import { CheckboxEmpty, CheckboxChecked, CheckboxIndeterminate, } from "@rapid7/icons";
export const ThemedCheckbox = {
    defaultProps: {
        checkedIcon: _jsx(CheckboxChecked, {}),
        disableFocusRipple: true,
        disableRipple: true,
        disableTouchRipple: true,
        icon: _jsx(CheckboxEmpty, {}),
        indeterminateIcon: _jsx(CheckboxIndeterminate, {}),
    },
    styleOverrides: {
        root: ({ theme }) => ({
            "&:has(input:focus-visible)::before": {
                border: `2px solid ${theme.palette.primary.main}`,
                borderRadius: "9px",
                content: '""',
                inset: "7px",
                pointerEvents: "none",
                position: "absolute",
            },
            "&:hover": {
                backgroundColor: theme.palette.components?.checkbox?.hover.primary,
                borderRadius: "12.5px",
            },
            position: "relative",
        }),
    },
    variants: [
        {
            props: { size: "small" },
            style: {
                "&:has(input:focus-visible)::before": {
                    borderRadius: "7px",
                    inset: "7px",
                },
            },
        },
        {
            props: { color: "error" },
            style: ({ theme }) => ({
                "& + .MuiFormControlLabel-label": {
                    color: theme.palette.error.main,
                },
                "& svg": {
                    color: theme.palette.error.main,
                },
                "&:hover": {
                    backgroundColor: theme.palette.components?.checkbox?.hover.error,
                },
            }),
        },
        {
            props: { color: "success" },
            style: ({ theme }) => ({
                "&:hover": {
                    backgroundColor: theme.palette.components?.checkbox?.hover.success,
                },
            }),
        },
    ],
};
