import { jsx as _jsx, jsxs as _jsxs } from "react/jsx-runtime";
import { render } from "@testing-library/react";
import { FormGroup, FormControlLabel, Checkbox } from "@mui/material";
import { describe, it, expect } from "vitest";
import { RDSThemeProvider } from "../../../rdsThemeProvider";
import { OriginalThemeMap } from "../../../originalTheme";
import { CallistoThemeMap } from "../../../callistoTheme";
import "@testing-library/jest-dom/vitest";
export const testThemeConfigurations = [
    {
        themeMap: OriginalThemeMap,
        themeName: "OriginalThemeMap",
    },
    {
        themeMap: CallistoThemeMap,
        themeName: "CallistoThemeMap",
    },
];
testThemeConfigurations.forEach(({ themeName, themeMap }) => {
    const renderFormGroup = ({ withOutlined = false } = {}) => render(_jsx(RDSThemeProvider, { themeMap: themeMap, themeMode: "light", children: _jsxs(FormGroup, { children: [_jsx(FormControlLabel, { control: _jsx(Checkbox, {}), label: "Checkbox 2", variant: withOutlined ? "outlined" : "standard" }), _jsx(FormControlLabel, { control: _jsx(Checkbox, {}), label: "Checkbox 2", variant: withOutlined ? "outlined" : "standard" })] }) }));
    describe(`${themeName} FormGroup`, () => {
        it("does not apply extra spacing when no outlined variant is present", () => {
            renderFormGroup({});
            const formGroup = document.querySelector(".MuiFormGroup-root");
            expect(formGroup).not.toHaveStyle("gap: 8px");
        });
        it("applies 8px gap when an outlined FormControlLabel is present", () => {
            renderFormGroup({ withOutlined: true });
            const formGroup = document.querySelector(".MuiFormGroup-root");
            expect(formGroup).toHaveStyle("gap: 8px");
        });
    });
});
