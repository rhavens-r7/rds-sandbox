import { jsx as _jsx } from "react/jsx-runtime";
import { afterAll, describe, it, expect, vi } from "vitest";
import { Typography, Paper } from "@mui/material";
import { render } from "@testing-library/react";
import { RDSThemeProvider } from "../RDSThemeProvider";
import { THEME_MODES, BRAND_NAMES } from "@rapid7/rds-theme-metadata-provider";
import { createTheme } from "@mui/material/styles";
import { DarkOriginalTheme } from "../../originalTheme";
const AppContents = () => {
    return (_jsx(Paper, { "data-testid": "paper", sx: {
            backgroundColor: "background.default",
        }, children: _jsx(Typography, { color: "primary", variant: "h1", children: "Sample App" }) }));
};
describe("RDSThemeProvider", () => {
    describe("when just the mode is passed in", () => {
        it("has the correct default background color in light theme", () => {
            const { getByTestId } = render(_jsx(RDSThemeProvider, { themeMode: THEME_MODES.LIGHT, children: _jsx(AppContents, {}) }));
            const paper = getByTestId("paper");
            expect(paper).toHaveStyle("background-color: #f7fafc");
        });
        it("has the correct default background color in dark theme", () => {
            const { getByTestId } = render(_jsx(RDSThemeProvider, { themeMode: THEME_MODES.DARK, children: _jsx(AppContents, {}) }));
            const paper = getByTestId("paper");
            expect(paper).toHaveStyle("background-color: rgb(42, 57, 68)");
        });
        it("falls back to light mode of the original RDS palette if incorrect themeMode is provided", () => {
            const { getByTestId } = render(
            // @ts-ignore - testing the fallback behavior for incorrect mode props
            _jsx(RDSThemeProvider, { themeMode: "blue", children: _jsx(AppContents, {}) }));
            const paper = getByTestId("paper");
            expect(paper).toHaveStyle("background-color: #f7fafc");
        });
    });
    describe("when just the brand is passed in", () => {
        it("has the correct default background color in original theme", () => {
            const { getByTestId } = render(_jsx(RDSThemeProvider, { brandName: BRAND_NAMES.ORIGINAL, children: _jsx(AppContents, {}) }));
            const paper = getByTestId("paper");
            expect(paper).toHaveStyle("background-color: #f7fafc");
        });
        it("has the correct default background color in callisto theme", () => {
            const { getByTestId } = render(_jsx(RDSThemeProvider, { brandName: BRAND_NAMES.CALLISTO, children: _jsx(AppContents, {}) }));
            const paper = getByTestId("paper");
            expect(paper).toHaveStyle("background-color: #f4f6f7");
        });
        it("falls back to light mode of the original RDS palette if incorrect brandName is provided", () => {
            const { getByTestId } = render(
            // @ts-ignore - testing the fallback behavior for incorrect brand props
            _jsx(RDSThemeProvider, { brandName: "unknownBrand", children: _jsx(AppContents, {}) }));
            const paper = getByTestId("paper");
            expect(paper).toHaveStyle("background-color: #f7fafc");
        });
    });
    describe("when custom themeMap is provided", () => {
        const customThemeMap = {
            blue: createTheme({
                palette: {
                    background: {
                        default: "#041721FF",
                    },
                    mode: "dark",
                },
            }),
            light: createTheme(),
        };
        it("renders the custom background color", () => {
            const { getByTestId } = render(_jsx(RDSThemeProvider, { themeMap: customThemeMap, themeMode: "blue", children: _jsx(AppContents, {}) }));
            const paper = getByTestId("paper");
            expect(paper).toHaveStyle("background-color: #041721FF");
        });
        it("doesn't write a console log", () => {
            const consoleSpy = vi
                .spyOn(console, "log")
                .mockImplementation(() => undefined);
            afterAll(() => {
                consoleSpy.mockReset();
            });
            render(_jsx(RDSThemeProvider, { themeMap: customThemeMap, themeMode: "blue", children: _jsx(AppContents, {}) }));
            expect(consoleSpy).toHaveBeenCalledTimes(0);
        });
        describe("when brandName is also provided", () => {
            it("ignores brandName", () => {
                const { getByTestId } = render(_jsx(RDSThemeProvider, { brandName: BRAND_NAMES.CALLISTO, themeMap: customThemeMap, themeMode: "blue", children: _jsx(AppContents, {}) }));
                const paper = getByTestId("paper");
                expect(paper).toHaveStyle("background-color: #041721FF");
            });
            it("writes a console warning in development mode", () => {
                const originalEnv = process.env.NODE_ENV;
                // Change node env to development to test the warning message
                process.env.NODE_ENV = "development";
                const consoleSpy = vi
                    .spyOn(console, "warn")
                    .mockImplementation(() => undefined);
                afterAll(() => {
                    consoleSpy.mockReset();
                });
                render(_jsx(RDSThemeProvider, { brandName: BRAND_NAMES.CALLISTO, themeMap: customThemeMap, themeMode: "blue", children: _jsx(AppContents, {}) }));
                expect(consoleSpy).toHaveBeenCalledOnce();
                expect(consoleSpy).toHaveBeenCalledWith(`brandName ('${BRAND_NAMES.CALLISTO}') ignored when themeMap is provided`);
                process.env.NODE_ENV = originalEnv;
            });
            it("doesn't write a console warning in non-development mode", () => {
                const consoleSpy = vi
                    .spyOn(console, "warn")
                    .mockImplementation(() => undefined);
                afterAll(() => {
                    consoleSpy.mockReset();
                });
                render(_jsx(RDSThemeProvider, { brandName: BRAND_NAMES.CALLISTO, themeMap: customThemeMap, themeMode: "blue", children: _jsx(AppContents, {}) }));
                expect(consoleSpy).toHaveBeenCalledTimes(0);
            });
        });
        it("falls back to light mode of the original RDS palette if incorrect themeMode is provided for themeMap", () => {
            const customThemeMap = {
                blue: createTheme({
                    palette: {
                        background: {
                            default: "#041721FF",
                        },
                        mode: "dark",
                    },
                }),
            };
            const { getByTestId } = render(
            // @ts-ignore - testing the fallback behavior for incorrect themeMode for themeMap
            _jsx(RDSThemeProvider, { themeMap: customThemeMap, themeMode: "accessible", children: _jsx(AppContents, {}) }));
            const paper = getByTestId("paper");
            expect(paper).toHaveStyle("background-color: #f7fafc");
        });
        it("falls back to light mode of the original RDS palette if a non-map themeMap object is provided", () => {
            const { getByTestId } = render(
            // @ts-ignore - testing the fallback behavior for incorrect themeMap props
            _jsx(RDSThemeProvider, { themeMap: DarkOriginalTheme, themeMode: "light", children: _jsx(AppContents, {}) }));
            const paper = getByTestId("paper");
            expect(paper).toHaveStyle("background-color: #f7fafc");
        });
    });
    it("falls back to light mode of the original RDS palette if mode and brand are not provided", () => {
        const { getByTestId } = render(
        // @ts-ignore - testing the fallback behavior for no props
        _jsx(RDSThemeProvider, { children: _jsx(AppContents, {}) }));
        const paper = getByTestId("paper");
        expect(paper).toHaveStyle("background-color: #f7fafc");
    });
});
