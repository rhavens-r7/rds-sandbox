"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.RDSMenu = void 0;
const jsx_runtime_1 = require("react/jsx-runtime");
const material_1 = require("@mui/material");
const react_1 = require("react");
const RDSMenu = ({ anchorOrigin = { horizontal: "left", vertical: "bottom" }, TransitionComponent = material_1.Grow, autoWidth = false, maxHeight = false, ...props }) => {
    const menuSx = Array.isArray(props.sx) ? props.sx : [props.sx];
    (0, react_1.useEffect)(() => {
        if (TransitionComponent === material_1.Fade) {
            console.warn("Warning: Using Fade as the TransitionComponent in RDSMenu is deprecated and will be removed as an option in the next major release. Please use the default (Grow) transition instead.");
        }
    }, [TransitionComponent]);
    return ((0, jsx_runtime_1.jsx)(material_1.Menu, { anchorOrigin: anchorOrigin, sx: [
            (theme) => ({
                "& .MuiBackdrop-root": {
                    backgroundColor: "transparent",
                },
                "& .MuiMenuItem-root:focus-visible": {
                    paddingLeft: theme.spacing(1.5),
                    paddingRight: theme.spacing(1.5),
                },
                ...(!autoWidth && {
                    "& .MuiPaper-root": {
                        border: `1px solid ${theme.palette.components.menu.border}`,
                        minWidth: 200,
                    },
                }),
                ...(maxHeight
                    ? !autoWidth
                        ? {
                            "& .MuiPaper-root": {
                                border: `1px solid ${theme.palette.components.menu.border}`,
                                maxHeight: 125,
                                minWidth: 200,
                            },
                        }
                        : {
                            "& .MuiPaper-root": {
                                border: `1px solid ${theme.palette.components.menu.border}`,
                            },
                            maxHeight: 125,
                        }
                    : {
                        "& .MuiPaper-root": {
                            border: `1px solid ${theme.palette.components.menu.border}`,
                        },
                    }),
            }),
            ...menuSx,
        ], TransitionComponent: TransitionComponent, ...props, MenuListProps: { autoFocus: true }, children: props.children }));
};
exports.RDSMenu = RDSMenu;
