"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.formatTruncatedNumber = exports.formatNumber = void 0;
const formatNumber = (value, { digits = 1 } = {}) => {
    return (value || 0).toLocaleString("en-US", {
        maximumFractionDigits: digits,
    });
};
exports.formatNumber = formatNumber;
const formatTruncatedNumber = (number, { digits = 1 } = {}) => {
    const [THOUSAND, MILLION, BILLION, TRILLION, QUADRILLION] = [
        1e3, 1e6, 1e9, 1e12, 1e15,
    ];
    switch (true) {
        case number >= QUADRILLION:
            return `${(0, exports.formatNumber)(number / QUADRILLION, { digits: 1 })}q`;
        case number >= TRILLION:
            return `${(0, exports.formatNumber)(number / TRILLION, { digits: 1 })}t`;
        case number >= BILLION:
            return `${(0, exports.formatNumber)(number / BILLION, { digits: 1 })}b`;
        case number >= MILLION:
            return `${(0, exports.formatNumber)(number / MILLION, { digits: 1 })}m`;
        case number >= THOUSAND:
            return `${(0, exports.formatNumber)(number / THOUSAND, { digits: 1 })}k`;
        default:
            return (0, exports.formatNumber)(number, { digits });
    }
};
exports.formatTruncatedNumber = formatTruncatedNumber;
