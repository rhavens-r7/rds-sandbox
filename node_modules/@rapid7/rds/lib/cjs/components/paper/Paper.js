"use strict";
var __createBinding = (this && this.__createBinding) || (Object.create ? (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    var desc = Object.getOwnPropertyDescriptor(m, k);
    if (!desc || ("get" in desc ? !m.__esModule : desc.writable || desc.configurable)) {
      desc = { enumerable: true, get: function() { return m[k]; } };
    }
    Object.defineProperty(o, k2, desc);
}) : (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    o[k2] = m[k];
}));
var __setModuleDefault = (this && this.__setModuleDefault) || (Object.create ? (function(o, v) {
    Object.defineProperty(o, "default", { enumerable: true, value: v });
}) : function(o, v) {
    o["default"] = v;
});
var __importStar = (this && this.__importStar) || function (mod) {
    if (mod && mod.__esModule) return mod;
    var result = {};
    if (mod != null) for (var k in mod) if (k !== "default" && Object.prototype.hasOwnProperty.call(mod, k)) __createBinding(result, mod, k);
    __setModuleDefault(result, mod);
    return result;
};
var __importDefault = (this && this.__importDefault) || function (mod) {
    return (mod && mod.__esModule) ? mod : { "default": mod };
};
Object.defineProperty(exports, "__esModule", { value: true });
exports.RdsPaper = void 0;
const jsx_runtime_1 = require("react/jsx-runtime");
const react_1 = __importStar(require("react"));
const Paper_1 = __importDefault(require("@mui/material/Paper"));
const tokens_1 = require("../../tokens");
const buildVariantStyles = (variant, theme) => {
    const themeElevation = (0, tokens_1.getValue)("elevation", theme.palette.brand, theme.palette.mode);
    const getBackgroundColor = () => {
        switch (variant) {
            case "default":
                return themeElevation["elevation-0"];
            case "paper":
                return themeElevation["elevation-1"];
            case "overlay":
                return themeElevation["elevation-2"];
            default:
                return themeElevation["elevation-0"];
        }
    };
    const baseStyles = {
        "--Paper-shadow": "none",
        backgroundColor: getBackgroundColor(),
    };
    switch (variant) {
        case "default":
            return {
                ...baseStyles,
                boxShadow: "none",
            };
        case "paper": {
            const { shadow: { blur: { blur10, blur20, blur40 }, offset: { y10, y20, y30 }, "elevation-1": { shadowA, shadowB, shadowC }, }, } = (0, tokens_1.getValue)("semantic", theme.palette.brand, theme.palette.mode);
            return {
                ...baseStyles,
                boxShadow: `
          0 ${y10}px ${blur10}px 0 ${shadowA},
          0 ${y20}px ${blur20}px 0 ${shadowB},
          0 ${y30}px ${blur40}px 0 ${shadowC}
        `,
            };
        }
        case "overlay": {
            const { shadow: { blur: { blur50, blur80 }, offset: { y60, y80 }, "elevation-2": { shadowA, shadowB, shadowC }, }, } = (0, tokens_1.getValue)("semantic", theme.palette.brand, theme.palette.mode);
            return {
                ...baseStyles,
                boxShadow: `
          0 ${y60}px ${blur50}px 0 ${shadowA},
          0 ${y60}px ${blur50}px 0 ${shadowB},
          0 ${y80}px ${blur80}px 0 ${shadowC}
        `,
            };
        }
        default:
            return baseStyles;
    }
};
exports.RdsPaper = react_1.default.forwardRef(({ elevation, variant = "default", sx, ...props }, ref) => {
    const memoizedSx = (0, react_1.useMemo)(() => (Array.isArray(sx) ? sx : [sx]), [sx]);
    const getEffectiveVariant = () => {
        if (variant === "outlined") {
            if (process.env.NODE_ENV !== "production") {
                console.warn('Paper: variant="outlined" is deprecated. Use variant="default" instead.');
            }
            return "default";
        }
        if (variant === "elevation") {
            if (process.env.NODE_ENV !== "production") {
                console.warn('Paper: variant="elevation" is deprecated. Use variant="paper" instead.');
            }
            return "paper";
        }
        if (elevation !== undefined) {
            if (process.env.NODE_ENV !== "production") {
                console.warn('Paper: elevation prop is deprecated. Use variant="default|paper|overlay" instead.');
            }
            if (elevation === 0)
                return "default";
            if (elevation <= 8)
                return "paper";
            return "overlay";
        }
        return variant;
    };
    const effectiveVariant = getEffectiveVariant();
    return ((0, jsx_runtime_1.jsx)(Paper_1.default, { ...props, elevation: 0, ref: ref, sx: [
            (theme) => buildVariantStyles(effectiveVariant, theme),
            ...memoizedSx,
        ] }));
});
