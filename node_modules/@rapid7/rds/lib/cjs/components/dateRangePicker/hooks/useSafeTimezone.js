"use strict";
var __importDefault = (this && this.__importDefault) || function (mod) {
    return (mod && mod.__esModule) ? mod : { "default": mod };
};
Object.defineProperty(exports, "__esModule", { value: true });
exports.zoned = void 0;
const react_1 = require("react");
const dayjs_1 = __importDefault(require("dayjs"));
const zoned = (date, timezone) => timezone === "UTC" ? dayjs_1.default.utc(date) : dayjs_1.default.tz(date, timezone);
exports.zoned = zoned;
const useSafeTimezone = (timezone) => {
    const safeTimezone = (0, react_1.useMemo)(() => {
        if (!timezone) {
            return {
                formattedTimezone: "UTC+00:00",
                ianaTimezone: "UTC",
            };
        }
        try {
            const offset = (0, dayjs_1.default)().tz(timezone).format("Z");
            return {
                formattedTimezone: `UTC${offset}`,
                ianaTimezone: timezone,
            };
        }
        catch (error) {
            return {
                formattedTimezone: "UTC+00:00",
                ianaTimezone: "UTC",
            };
        }
    }, [timezone]);
    return safeTimezone;
};
exports.default = useSafeTimezone;
