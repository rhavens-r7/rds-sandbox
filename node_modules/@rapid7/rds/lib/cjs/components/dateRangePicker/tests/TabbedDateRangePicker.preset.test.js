"use strict";
var __importDefault = (this && this.__importDefault) || function (mod) {
    return (mod && mod.__esModule) ? mod : { "default": mod };
};
Object.defineProperty(exports, "__esModule", { value: true });
const jsx_runtime_1 = require("react/jsx-runtime");
const vitest_1 = require("vitest");
const react_1 = require("@testing-library/react");
const user_event_1 = require("@testing-library/user-event");
const dayjs_1 = __importDefault(require("dayjs"));
const rdsConfigProvider_1 = require("../../../rdsConfigProvider");
const TabbedDateRangePicker_1 = __importDefault(require("../TabbedDateRangePicker"));
const parseTimeFrame_1 = require("../util/parseTimeFrame");
(0, vitest_1.describe)("<TabbedDateRangePicker /> time frame tab", () => {
    const presets = [
        {
            label: "Last 20 minutes",
            onParse: (now) => ({
                from: now.subtract(20, "minute").valueOf(),
                timeFrame: "Last 20 minutes",
                to: now.valueOf(),
            }),
        },
        {
            label: "Last 1 hour",
            onParse: (now) => ({
                from: now.subtract(1, "hour").valueOf(),
                timeFrame: "Last 1 hour",
                to: now.valueOf(),
            }),
        },
        {
            label: "Yesterday",
            onParse: (now) => ({
                from: now.startOf("day").subtract(1, "day").valueOf(),
                timeFrame: "Yesterday",
                to: now.startOf("day").subtract(1, "day").endOf("day").valueOf(),
            }),
        },
        {
            label: "Today",
            onParse: (now) => ({
                from: now.startOf("day").valueOf(),
                timeFrame: "Today",
                to: now.valueOf(),
            }),
        },
        {
            label: "Last 1 week",
            onParse: (now) => ({
                from: now.startOf("day").subtract(1, "week").valueOf(),
                timeFrame: "Last 1 week",
                to: now.valueOf(),
            }),
        },
        {
            label: "Last 30 days",
            onParse: (now) => ({
                from: now.subtract(30, "day").valueOf(),
                timeFrame: "Last 30 days",
                to: now.valueOf(),
            }),
        },
    ];
    (0, vitest_1.beforeAll)(() => {
        vitest_1.vi.useFakeTimers();
        vitest_1.vi.setSystemTime((0, dayjs_1.default)("1 January 2025").startOf("day").toDate());
    });
    (0, vitest_1.afterAll)(() => {
        vitest_1.vi.useRealTimers();
    });
    (0, vitest_1.it)("deselects the selected preset when the custom preset unit is updated", async () => {
        // Setup fixture
        const user = user_event_1.userEvent.setup({ advanceTimers: vitest_1.vi.advanceTimersByTime });
        // Exercise SUT
        (0, react_1.render)((0, jsx_runtime_1.jsx)(rdsConfigProvider_1.RDSConfigProvider, { children: (0, jsx_runtime_1.jsx)(TabbedDateRangePicker_1.default, { onChange: vitest_1.vi.fn(), presets: [{ presets }], tabs: ["preset"], value: undefined }) }));
        // Exercise SUT
        await user.click(react_1.screen.getByPlaceholderText("Date Range"));
        await user.click(react_1.screen.getByLabelText("Unit of time"));
        await user.click(document.querySelectorAll(".MuiMenuItem-root")[1]);
        // Verify results
        (0, vitest_1.expect)(react_1.screen.queryByTestId("selected-preset")).not.toBeInTheDocument();
    });
    (0, vitest_1.it)("prevents submission of a custom date range that is larger the provided maximum range", async () => {
        // Setup fixture
        const user = user_event_1.userEvent.setup({ advanceTimers: vitest_1.vi.advanceTimersByTime });
        (0, react_1.render)((0, jsx_runtime_1.jsx)(rdsConfigProvider_1.RDSConfigProvider, { children: (0, jsx_runtime_1.jsx)(TabbedDateRangePicker_1.default, { maxCustomRange: { number: 1, unit: "day" }, onChange: vitest_1.vi.fn(), onParse: parseTimeFrame_1.parseTimeFrame, presets: [{ presets }], tabs: ["preset"], value: undefined }) }));
        // Exercise SUT
        await user.click(react_1.screen.getByPlaceholderText("Date Range"));
        await user.type(react_1.screen.getByPlaceholderText("#"), "25");
        await user.click(react_1.screen.getByLabelText("Unit of time"));
        await user.click((0, react_1.within)(document.querySelector(".MuiMenu-list")).getByText("Hours"));
        // Verify results
        (0, vitest_1.expect)(react_1.screen.getByText("Ok").getAttribute("disabled")).not.toBeNull();
        (0, vitest_1.expect)(react_1.screen.getByText("Time frame must be less than 1 day")).toBeInTheDocument();
    });
    (0, vitest_1.it)("allows submission of a custom date range that is equal to provided maximum range when using different units", async () => {
        // Setup fixture
        const user = user_event_1.userEvent.setup({ advanceTimers: vitest_1.vi.advanceTimersByTime });
        (0, react_1.render)((0, jsx_runtime_1.jsx)(rdsConfigProvider_1.RDSConfigProvider, { children: (0, jsx_runtime_1.jsx)(TabbedDateRangePicker_1.default, { maxCustomRange: { number: 1, unit: "day" }, onChange: vitest_1.vi.fn(), onParse: parseTimeFrame_1.parseTimeFrame, presets: [{ presets }], tabs: ["preset"], timezone: "Europe/Bucharest", value: undefined }) }));
        // Exercise SUT
        await user.click(react_1.screen.getByPlaceholderText("Date Range"));
        await user.type(react_1.screen.getByPlaceholderText("#"), "24");
        await user.click(react_1.screen.getByLabelText("Unit of time"));
        await user.click((0, react_1.within)(document.querySelector(".MuiMenu-list")).getByText("Hours"));
        // Verify results
        (0, vitest_1.expect)(react_1.screen.getByText("Ok").getAttribute("disabled")).toBeNull();
        (0, vitest_1.expect)(react_1.screen.queryByText("Time frame must be less than 1 day")).not.toBeInTheDocument();
    });
    (0, vitest_1.it)("recalculates the 'to' and 'from' values for a time frame that is submitted without a change", async () => {
        // Setup fixture
        const now = (0, dayjs_1.default)("1 January 2025").startOf("day");
        const onChangeSpy = vitest_1.vi.fn();
        const user = user_event_1.userEvent.setup({ advanceTimers: vitest_1.vi.advanceTimersByTime });
        (0, react_1.render)((0, jsx_runtime_1.jsx)(rdsConfigProvider_1.RDSConfigProvider, { children: (0, jsx_runtime_1.jsx)(TabbedDateRangePicker_1.default, { onChange: onChangeSpy, presets: [{ presets }], tabs: ["preset"], value: { timeFrame: presets[0].label } }) }));
        // Exercise SUT
        await user.click(react_1.screen.getByPlaceholderText("Date Range"));
        await user.click(react_1.screen.getByText("Ok"));
        // Verify behaviour
        (0, vitest_1.expect)(onChangeSpy).toHaveBeenNthCalledWith(1, {
            from: now.subtract(20, "minute").valueOf(),
            timeFrame: "Last 20 minutes",
            to: now.valueOf(),
        });
    });
});
