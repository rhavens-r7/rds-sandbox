"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.ThemedAlert = void 0;
exports.ThemedAlert = {
    defaultProps: {
        variant: "standard",
    },
    styleOverrides: {
        action: ({ theme, ownerState: { severity = "success" } }) => ({
            "& .MuiButton-text": {
                padding: 0,
            },
            alignItems: "center",
            button: {
                color: theme.palette.components.alert[severity].color,
            },
            gap: theme.spacing(1),
            marginRight: 0,
            padding: theme.spacing(0.5, 0, 0.5, 2),
        }),
        message: ({ theme, ownerState: { severity = "success" } }) => ({
            color: theme.palette.components.alert[severity].color,
        }),
        root: {
            variants: [
                {
                    props: { variant: "filled" },
                    style: ({ theme, severity = "success", }) => ({
                        color: theme.palette.components.alert[severity].color,
                    }),
                },
                {
                    props: { variant: "outlined" },
                    style: ({ theme, severity = "success", }) => ({
                        backgroundColor: theme.palette.components.alert[severity].background,
                        borderColor: theme.palette.components.alert[severity].border,
                        color: theme.palette.components.alert[severity].color,
                    }),
                },
                {
                    props: { variant: "standard" },
                    style: ({ theme, severity = "success", }) => ({
                        backgroundColor: theme.palette.components.alert[severity].background,
                        color: theme.palette.components.alert[severity].color,
                    }),
                },
            ],
        },
    },
};
