"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.ThemedTabs = void 0;
require("@mui/material/Tabs");
const icons_1 = require("@rapid7/icons");
exports.ThemedTabs = {
    defaultProps: {
        slots: {
            EndScrollButtonIcon: icons_1.NextChevronRightArrow,
            StartScrollButtonIcon: icons_1.PreviousChevronLeftArrow,
        },
    },
    styleOverrides: {
        indicator: ({ theme, ownerState }) => ({
            borderRadius: theme.shape.borderRadius,
            height: 3,
            width: 3,
            ...(ownerState.orientation === "vertical" && {
                left: 0,
                right: "auto",
            }),
        }),
        root: ({ ownerState }) => ({
            "&:has(.Mui-selected.Mui-focusVisible) .MuiTabs-indicator": {
                transition: "none",
                ...(ownerState.orientation === "vertical"
                    ? { left: 3 }
                    : { bottom: 3 }),
            },
            ...(ownerState.orientation === "vertical" && {
                minWidth: 120,
            }),
        }),
        scroller: ({ theme, ownerState }) => ({
            backgroundImage: `linear-gradient(to top, transparent 1px, ${theme.palette.divider} 1px, ${theme.palette.divider} 2px, transparent 2px)`,
            ...(ownerState.orientation === "vertical" && {
                backgroundImage: "none",
            }),
        }),
    },
};
