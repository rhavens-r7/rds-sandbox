import { NavSection, NavItem } from "../types/SettingsTemplate.types";
export interface SettingsNavItemProps {
    item: NavItem;
    isSelected: boolean;
}
export declare const SettingsNavItem: ({ item, isSelected }: SettingsNavItemProps) => JSX.Element;
export interface NavigationProps {
    navConfig: NavSection[];
    selectedId?: string;
}
export declare const SettingsNavigation: ({ navConfig, selectedId, }: NavigationProps) => JSX.Element;
//# sourceMappingURL=SettingsNavigation.d.ts.map