import type { ChangeEvent, ComponentType, ReactNode } from "react";
import { BoxProps, SxProps } from "@mui/material";
import { R7DetailsPageHeaderProps } from "../../../components/detailsPageHeader";
export type NavItem = {
    id: string;
    title: string;
    description?: string;
    to?: string;
    onClick?: () => void;
    slots?: {
        leftIcon?: ReactNode;
        rightValue?: ReactNode;
    };
};
export type NavSection = {
    header?: string;
    items: NavItem[];
};
export interface SettingsPageTemplateProps {
    children?: ReactNode;
    /**
     *  Pass in the props to render the Page Header
     */
    detailsPageHeaderProps?: R7DetailsPageHeaderProps;
    /**
     *  Custom slots for content and navigation.
     *
     *  You will need to pass in content to display based on the selected nav item.
     *  The content component will have the id of the selected item available
     *  as a prop if you pass it in via slotProps.
     *
     *  You must also pass in a custom navigation component.
     *  If you do not, you must pass in the navConfig to render the built-in navigation.
     */
    slots?: {
        Content?: ComponentType<{
            selectedId?: string;
        }>;
        Navigation?: ComponentType<{
            selectedId?: string;
        }>;
    };
    slotProps?: {
        Content?: {
            selectedId?: string;
        };
        Navigation?: {
            selectedId?: string;
        };
    };
    /**
     *  If you pass in a search function, the search box will render.
     *  Your search functionality should narrow down menu options and content based on matches.
     *  Only use this when it makes sense to do so. Do you really need it for 5 items? Probably not.
     */
    onSearch?: (event: ChangeEvent<HTMLInputElement | HTMLTextAreaElement>) => void;
    /**
     *  If you need to adjust the overall container of the page, you can use this.
     */
    pageContainerProps?: BoxProps;
    searchPlaceholder?: string;
    mainSectionSx?: SxProps;
}
//# sourceMappingURL=SettingsTemplate.types.d.ts.map