import type { ReactNode } from "react";
import { BoxProps, SvgIconProps, TypographyProps } from "@mui/material";
export interface R7AttributeProps {
    /**
     * Should you need to do some styling to the root box, like padding or margin, you can use AttributeRootProps
     *
     * From [MUI BoxProps](https://mui.com/material-ui/api/box/)
     */
    AttributeRootProps?: BoxProps;
    /**
     * The label, describing the value to follow. Not required, but you should use either label, icon or both!!
     */
    label?: string;
    /**
     * From [MUI TypographyProps](https://mui.com/material-ui/api/typography/)
     */
    LabelProps?: TypographyProps;
    /**
     * A string value to be passed into the label styling, e.g. "100px" or "20%"
     */
    labelWidth?: string;
    /**
     * An SVG icon component from the Rapid7 UI library (Link; TBD)
     */
    icon?: React.ComponentType<SvgIconProps>;
    IconProps?: SvgIconProps;
    /**
     * A node or array of nodes. This is open-ended, but please see Stories for suggested content! (Ex: string, Link, Chip, Button)
     *
     * `ReactNode` | `ReactNode[]`
     */
    content: ReactNode | ReactNode[];
    /**
     * One of the following layout variants
     *
     * `hug` | `column` | `vertical`
     *
     * Hug: (Default) Content begins immediately after the label string.
     *
     * Column: Label with is explicitly defined and content will be aligned at the end of the label "column", giving a column effect when stacked.
     *
     * Vertical: The Icon and/or Label will be stacked on top of the content.
     */
    variant?: "hug" | "column" | "vertical";
    /**
     * A boolean value to determine if the Attribute is in a loading state. If true, a Skeleton will be shown in place of the content.
     */
    isLoading?: boolean;
}
export interface AttributeOwnerState extends R7AttributeProps {
}
//# sourceMappingURL=attribute.types.d.ts.map