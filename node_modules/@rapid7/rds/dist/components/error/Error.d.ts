import React from "react";
import { SxProps, Theme } from "@mui/material";
import { ErrorCodesType } from "./errorStates.consts";
export interface ErrorProps {
    /**
     * The error code from the server; used to determine the illustration, messaging, and action (refresh or go back).
     * "default" is used as a general error state or fallback.
     */
    errorCode?: ErrorCodesType;
    /**
     *  Callback function to be executed when the button is clicked (refresh or go back).
     */
    onClick?: () => void;
    /**
     *  The illustration size.
     */
    size?: "small" | "medium";
    /**
     * Slots for customizing the error state text.
     * - `header`: The header text of the error state.
     * - `description`: The description text of the error state.
     * - `action`: A custom action (button) to be displayed below the description.
     */
    slots?: {
        header?: string;
        description?: string;
        action?: React.ReactNode;
    };
    /**
     *  define extra styles on Error component
     */
    sx?: SxProps<Theme>;
}
export declare const Error: ({ errorCode, onClick, size, slots, sx, }: ErrorProps) => JSX.Element;
//# sourceMappingURL=Error.d.ts.map