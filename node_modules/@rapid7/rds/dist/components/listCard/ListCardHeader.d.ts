import { ComponentType } from "react";
import { SvgIcon } from "@mui/material";
export interface ListCardHeaderProps {
    isSelectable?: boolean;
    isSelected?: boolean;
    Title: ComponentType;
    Subitem?: ComponentType;
    Icon?: typeof SvgIcon;
}
export declare const ListCardHeader: ({ isSelectable, isSelected, Title, Subitem, Icon, }: ListCardHeaderProps) => JSX.Element;
export default ListCardHeader;
//# sourceMappingURL=ListCardHeader.d.ts.map