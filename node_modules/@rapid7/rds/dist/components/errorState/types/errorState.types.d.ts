import type { ReactNode } from "react";
import { ERROR_CODES_LIST } from "../../errorCode/constants/errorCode.consts";
type ErrorCode = (typeof ERROR_CODES_LIST)[number];
export type R7ErrorStateProps = {
    /**
     * The text that you want to display in the button.
     */
    buttonText?: string;
    /**
     * The description for the Error State.
     */
    description: string | ReactNode;
    /**
     * An optional Error Code can be passed. This can be `401`, `403`, `404`, `406`, `409` or `500`.
     */
    errorCode?: ErrorCode;
    /**
     * The header text to display with the content.
     */
    header: string;
    /**
     * An optional image tag `<img>` can be passed which will overwrite the default image or an image that is chosen depending on the Error Code passed.
     */
    image?: ReactNode;
    /**
     * An optional button can be shown alongside the main button, this will provide the text.
     */
    subButtonText?: string;
    /**
     * An optional header to display under the main header.
     */
    subHeader?: string;
    /**
     * A function to handle what will happen when the button is clicked.
     */
    handleButtonOnClick: () => void;
    /**
     * An optional function to handle what will happen when the sub button is clicked.
     */
    handleSubButtonOnClick?: () => void;
    /**
     * Show an Error Icon when the ErrorState is at a smaller size. Defaults to the info icon.
     */
    hasErrorIcon?: boolean | undefined;
};
export {};
//# sourceMappingURL=errorState.types.d.ts.map