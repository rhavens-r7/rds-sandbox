import React from "react";
import { SxProps, Theme } from "@mui/material";
import { EmptyStateVariantsType } from "./constants/emptyStates.consts";
export interface EmptyStateProps {
    /**
     *  Will determine the default messaging of the empty state.
     *  - `NOTHING_FOUND`: Used when no items are found (usually for search).
     *  - `NO_MATCH`: Used when no items match the criteria (search and filters)
     *  - `ADD_ITEM`: Used when no items exist and the user can add an item.
     *  - `CREATE_ITEM`: Used when no items exist and the user can create an item.
     */
    variant?: EmptyStateVariantsType;
    /**
     * Will determine if the empty state should be displayed in grayscale.
     */
    isGrayscale?: boolean;
    /**
     * The item that the empty state is referring to. (eg. "user", "alert", etc.)
     * This is used in the `ADD_ITEM` and `CREATE_ITEM` variants.
     */
    item?: string;
    /**
     * The callback function to be called when the ADD or CREATE action button is clicked.
     */
    onClick?: () => void;
    /**
     * Slots for customizing the empty state text.
     * - `header`: The header text of the empty state.
     * - `description`: The description text of the empty state.
     * - `action`: A custom action (button) to be displayed below the description.
     */
    slots?: {
        header?: React.ReactNode;
        description?: React.ReactNode;
        action?: React.ReactNode;
    };
    /**
     *  define extra styles on EmptyState component
     */
    sx?: SxProps<Theme>;
}
export declare const EmptyState: ({ isGrayscale, variant, item, onClick, slots: slotsFromProps, sx: extraSx, }: EmptyStateProps) => JSX.Element;
//# sourceMappingURL=EmptyState.d.ts.map