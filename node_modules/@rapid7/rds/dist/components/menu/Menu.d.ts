import { MenuProps } from "@mui/material";
export interface RDSMenuProps extends MenuProps {
    /**
     * If true - sets the width to automatically fit the widest menu item.
     * If false or not provided - sets the width to a default of 200px
     */
    autoWidth?: boolean;
    /**
     * If true - sets the max-height to 125px with scroll behaviour
     */
    maxHeight?: boolean;
}
export declare const RDSMenu: ({ anchorOrigin, TransitionComponent, autoWidth, maxHeight, ...props }: RDSMenuProps) => JSX.Element;
//# sourceMappingURL=Menu.d.ts.map