import { type DateRange } from "@mui/x-date-pickers-pro";
import { type StateType } from "../stateMachine/dateRangePickerState";
import { type ManipulateType, type Dayjs } from "dayjs";
import type { TimeFrameParserType, PresetType, RelativeTimeFrameUnitType } from "../index.types";
import { type Duration } from "dayjs/plugin/duration";
export declare const useValidateValue: ({ clearable, currentCustomValue, currentTimeFrameValue, flattenedPresets, ianaTimezone, isDirty, maxCustomRange, minCustomRange, maxDate, minDate, now, onParse, state, units, }: {
    clearable: boolean;
    currentCustomValue: DateRange<Dayjs>;
    currentTimeFrameValue: {
        from?: number;
        timeFrame: string;
        to?: number;
    };
    flattenedPresets: PresetType[];
    ianaTimezone: string;
    isDirty: boolean;
    maxCustomRange?: {
        number: number;
        unit: ManipulateType;
    };
    minCustomRange?: {
        number: number;
        unit: ManipulateType;
    };
    maxDate?: Dayjs | undefined;
    minDate?: Dayjs | undefined;
    maxRange?: Duration;
    minRange?: Duration;
    now: Dayjs;
    onParse?: TimeFrameParserType;
    state: keyof StateType;
    units: RelativeTimeFrameUnitType[];
}) => string | undefined;
//# sourceMappingURL=useValidateValue.d.ts.map