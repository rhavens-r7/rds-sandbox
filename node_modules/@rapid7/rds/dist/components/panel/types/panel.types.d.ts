import type { ReactNode } from "react";
import { BoxProps } from "@mui/material";
export type PanelSize = "small" | "medium" | "large";
type R7PanelBaseProps = {
    /**
     * Should you need to do some styling to the root box, you can use PanelRootProps
     *
     * From [MUI BoxProps](https://mui.com/material-ui/api/box/)
     */
    PanelRootProps?: BoxProps;
    /**
     * The id of the element that labels this panel (typically the PanelHeader title id).
     * Used for accessibility to provide an accessible name to the panel region.
     */
    "aria-labelledby"?: string;
    /**
     * The children to render inside the Panel.
     */
    children: ReactNode;
    /**
     * Controls whether the panel is visible.
     */
    open: boolean;
    /**
     * Callback fired when the panel should close.
     */
    onClose?: () => void;
    /**
     * Whether to return focus to the trigger element when the panel closes.
     * @default true
     */
    returnFocusOnClose?: boolean;
    /**
     * The width of the panel.
     * @default "medium"
     */
    size?: PanelSize;
    /**
     * Reference to the element that triggered the panel opening.
     * Focus will be returned here when the panel closes if returnFocusOnClose is true.
     */
    triggerRef?: React.RefObject<HTMLElement>;
};
type ConditionalProps = {
    variant: "AI";
    isLoading?: boolean;
} | {
    variant?: "default";
    isLoading?: never;
};
export type R7PanelProps = R7PanelBaseProps & ConditionalProps;
export type R7PanelContentProps = {
    /**
     * Should you need to do some styling to the content box, you can use PanelContentRootProps
     *
     * From [MUI BoxProps](https://mui.com/material-ui/api/box/)
     */
    PanelContentRootProps?: BoxProps;
    /**
     * The children to render inside the PanelContent.
     */
    children: ReactNode;
};
export {};
//# sourceMappingURL=panel.types.d.ts.map