import type { ReactNode } from "react";
import { BoxProps, TypographyProps } from "@mui/material";
export type R7PanelHeaderProps = {
    /**
     * Should you need to do some styling to the root box, like padding or margin, you can use AttributeRootProps
     *
     * From [MUI BoxProps](https://mui.com/material-ui/api/box/)
     */
    PanelHeaderRootProps?: BoxProps;
    /**
     * Accessible label for the close button.
     * @default "Close panel"
     */
    closeButtonAriaLabel?: string;
    /**
     * The header will collapse on scrolling up.
     * @default false
     */
    collapseOnScroll?: boolean;
    /**
     * The id to apply to the page title element.
     * Used with Panel's aria-labelledby for accessibility.
     */
    id?: string;
    /**
     * Make the header sticky.
     */
    isSticky?: boolean;
    /**
     * What should happen when the back button is clicked. Button will not show if nothing is passed in.
     * @default false
     */
    onBack?: () => void;
    /**
     * What should happen when the close button is clicked. Button will not show if nothing is passed in.
     */
    onClose?: () => void;
    /**
     * Let the user know what they are looking at.
     */
    pageTitle: ReactNode;
    /**
     * From [MUI TypographyProps](https://mui.com/material-ui/api/typography/)
     */
    pageTitleProps?: TypographyProps;
    /**
     * Slots for actions, attributes and tags.
     * See implementation guidance above for more detail on usage.
     */
    slots?: {
        actions?: ReactNode | ReactNode[];
        attributes?: ReactNode | ReactNode[];
        tags?: ReactNode | ReactNode[];
    };
    /**
     * Hide the bottom divider.
     * @default true
     */
    hideDivider?: boolean;
};
//# sourceMappingURL=panelHeader.types.d.ts.map