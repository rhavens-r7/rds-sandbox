import { SxProps, Theme } from "@mui/material";
import React from "react";
export interface CheckboxGroupProps {
    /**
     * Checkbox components to be rendered within the group.
     */
    children?: React.ReactNode;
    /**
     * Optional test ID for the checkbox group container.
     */
    "data-testid"?: string;
    /**
     * Disables all checkboxes within the group.
     */
    disabled?: boolean;
    /**
     * Indicates if the checkbox group is in an error state.
     */
    error?: boolean;
    /**
     * Label to display above the checkbox group.
     */
    label?: string;
    /**
     * Help message to display below the checkbox group.
     */
    helpText?: string;
    /**
     * Custom styles to apply to the checkbox group container.
     */
    sx?: SxProps<Theme>;
}
export declare const CheckboxGroup: ({ children, "data-testid": dataTestId, disabled, error, helpText, label, sx, }: CheckboxGroupProps) => JSX.Element;
//# sourceMappingURL=CheckboxGroup.d.ts.map