import type { ReactNode } from "react";
export type SectionHeaderProps = {
    /**
     * The header text to display in the top left.
     */
    header: string;
    /**
     * Optional status component to render next to the header.
     */
    statusSlot?: ReactNode;
    /**
     * Optional actions component to render on the same row as header and status, aligned to the right.
     */
    actionsSlot?: ReactNode;
    /**
     * Optional subheader component to render beneath the header with 8px gap.
     */
    subheaderSlot?: ReactNode;
    /**
     * Whether to display a divider below the subheader slot.
     * @default false
     */
    divider?: boolean;
};
//# sourceMappingURL=sectionHeader.d.ts.map