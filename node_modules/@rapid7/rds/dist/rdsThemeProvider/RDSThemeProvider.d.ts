import { RDSThemeProviderProps } from "./types/rdsThemeProviderProps.types";
import type { ThemeMode } from "@rapid7/rds-theme-metadata-provider";
/**
 * The `RDSThemeProvider` is an abstraction on top of MUI's out-of-the-box ThemeProvider.
 * By default, it comes with the Original RDS theme (styling that is consistent with the Rapid7 UI repo and the RDS 2.0 Figma library).
 *
 * It can be used at the top level of an app or to wrap a section within.
 */
export declare function RDSThemeProvider<T extends string = ThemeMode>({ children, themeMode, brandName, themeMap, }: RDSThemeProviderProps<T>): JSX.Element;
//# sourceMappingURL=RDSThemeProvider.d.ts.map