import React from "react";
import { GenericThemeMap } from "./genericThemeMap.types";
import { Theme } from "@mui/material/styles";
import { BrandName } from "@rapid7/rds-theme-metadata-provider";
interface InitialGenericThemeProviderProps<T extends string> {
    themeMode: T;
    brandName?: BrandName;
    themeMap: GenericThemeMap<T>;
    children?: React.ReactNode;
}
type InferThemeMode<T> = T extends {
    themeMap: Record<infer K, Theme>;
} ? K : never;
export type GenericThemeProviderProps<T extends string> = InitialGenericThemeProviderProps<T> & {
    themeMode: InferThemeMode<InitialGenericThemeProviderProps<T>>;
};
export {};
//# sourceMappingURL=genericThemeProviderProps.types.d.ts.map